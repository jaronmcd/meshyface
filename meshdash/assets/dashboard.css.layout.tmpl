      font-family: "IBM Plex Mono", "Cascadia Mono", "Consolas", monospace;
      font-size: 10px;
      color: #1f4e35;
      background: #edf6ef;
      border: 1px solid #c9dece;
      border-radius: 6px;
      padding: 1px 5px;
      white-space: nowrap;
    }}
    .bots .chat-bot-cheatsheet {{
      border: 1px solid #ccddcf;
      background: #ffffff;
      border-radius: 8px;
      padding: 6px 7px;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }}
    .bots-command-shell {{
      display: grid;
      grid-template-columns: minmax(130px, var(--bots-command-list-pct)) var(--splitter-size) minmax(130px, 1fr);
      gap: 0;
      min-height: 0;
      flex: 1 1 auto;
    }}
    .bots-command-list-wrap {{
      display: flex;
      flex-direction: column;
      gap: 6px;
      min-height: 0;
      padding-right: 8px;
    }}
    .bots-command-splitter {{
      position: relative;
      border-radius: 8px;
      border: 1px solid var(--surface-tint-border);
      background: linear-gradient(180deg, var(--surface-tint-bg-soft), var(--surface-tint-bg-alt));
      cursor: col-resize;
      touch-action: none;
      user-select: none;
      min-width: 6px;
    }}
    .bots-command-splitter::before {{
      content: "";
      position: absolute;
      top: 8px;
      bottom: 8px;
      left: 50%;
      width: 2px;
      transform: translateX(-50%);
      background: var(--surface-tint-border-strong);
      border-radius: 1px;
    }}
    .bots-command-splitter:hover::before,
    .bots-command-splitter.active::before {{
      background: var(--accent);
    }}
    .bots .chat-bot-cheatsheet-title {{
      font-size: 10px;
      color: var(--surface-tint-text-soft);
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.2px;
    }}
    .bots .chat-bot-cheatsheet-row {{
      display: grid;
      grid-template-columns: auto minmax(0, 1fr);
      gap: 7px;
      align-items: baseline;
      font-size: 10px;
      line-height: 1.25;
      color: #2f4f3f;
    }}
    .bots .chat-bot-cheatsheet-row code {{
      font-family: "IBM Plex Mono", "Cascadia Mono", "Consolas", monospace;
      font-size: 10px;
      color: #1f4e35;
      background: #edf6ef;
      border: 1px solid #c9dece;
      border-radius: 6px;
      padding: 1px 5px;
      white-space: nowrap;
    }}
    .bots-command-list {{
      display: flex;
      flex-direction: column;
      gap: 6px;
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
      padding: 8px;
      border: 1px solid var(--surface-tint-border);
      border-radius: 10px;
      background: #f8fbf7;
    }}
    .bots-command-config {{
      border: 1px solid var(--surface-tint-border);
      border-radius: 10px;
      padding: 10px;
      background: #fdfefd;
      min-height: 0;
      display: flex;
      flex-direction: column;
      gap: 8px;
      padding-left: 12px;
    }}
    .bots-command-config-head {{
      display: flex;
      flex-direction: column;
      gap: 4px;
      min-width: 0;
    }}
    .bots-command-config-title-row {{
      display: flex;
      align-items: center;
      gap: 6px;
      flex-wrap: wrap;
      min-width: 0;
    }}
    .bots-command-config-usage {{
      font-family: "IBM Plex Mono", "Cascadia Mono", "Consolas", monospace;
      font-size: 12px;
      color: #1f4e35;
      word-break: break-word;
    }}
    .bots-command-config-name {{
      font-size: 10px;
      color: var(--surface-tint-text-soft);
      text-transform: uppercase;
      letter-spacing: 0.2px;
    }}
    .bots-command-config-description {{
      font-size: 11px;
      line-height: 1.35;
      color: var(--surface-tint-text-soft);
    }}
    .bots-command-config-toggle {{
      margin-top: 2px;
    }}
    .bots-command-config-section {{
      margin-top: 4px;
      padding-top: 8px;
      border-top: 1px solid var(--surface-tint-border);
      display: flex;
      flex-direction: column;
      gap: 6px;
    }}
    .bots-command-config-section[hidden] {{
      display: none !important;
    }}
    .bots-command-config-label {{
      font-size: 10px;
      color: var(--surface-tint-text-soft);
      text-transform: uppercase;
      letter-spacing: 0.2px;
    }}
    .bots-command-config-textarea {{
      width: 100%;
      min-height: 52px;
      border: 1px solid #c8d7c4;
      border-radius: 8px;
      padding: 6px 8px;
      background: #ffffff;
      color: #1e3a2b;
      font: 11px/1.35 "IBM Plex Mono", "Cascadia Mono", "Consolas", monospace;
      resize: vertical;
    }}
    .bots-command-config-input {{
      width: 100%;
      font: 11px/1.35 "IBM Plex Mono", "Cascadia Mono", "Consolas", monospace;
    }}
    .bots-command-config-actions {{
      display: flex;
      justify-content: flex-start;
      align-items: center;
      gap: 6px;
      flex-wrap: wrap;
    }}
    .bots-zork-actions .bots-setting-note {{
      min-height: 22px;
      display: inline-flex;
      align-items: center;
    }}
    .bots-history-panel .bots-command-config {{
      flex: 1 1 auto;
      overflow: hidden;
    }}
    .bots-zork-activity-section {{
      flex: 1 1 auto;
      min-height: 0;
    }}
    .bots-session-list {{
      border: 1px solid #ccddcf;
      border-radius: 8px;
      background: #ffffff;
      max-height: 180px;
      overflow: auto;
      padding: 6px;
      display: flex;
      flex-direction: column;
      gap: 6px;
    }}
    .bots-session-item {{
      border: 1px solid var(--surface-tint-border);
      border-radius: 8px;
      padding: 8px;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 8px;
      align-items: center;
      background: var(--surface-tint-bg-soft);
    }}
    .bots-session-main {{
      display: flex;
      flex-direction: column;
      gap: 4px;
      min-width: 0;
    }}
    .bots-session-peer {{
      font-size: 12px;
      color: #1f4e35;
      font-weight: 700;
      word-break: break-word;
    }}
    .bots-session-meta {{
      font-size: 10px;
      color: #5b7565;
      display: flex;
      gap: 7px;
      flex-wrap: wrap;
    }}
    .bots-command-item {{
      width: 100%;
      border: 1px solid #c8d7c4;
      border-radius: 10px;
      background: #ffffff;
      padding: 7px 8px;
      display: grid;
      grid-template-columns: auto minmax(0, 1fr) auto;
      gap: 8px;
      align-items: center;
      color: #1e3a2b;
      text-align: left;
      font: inherit;
      cursor: pointer;
      transition: border-color 0.16s ease, background 0.16s ease;
    }}
    .bots-command-item:hover {{
      border-color: #8daf99;
      background: #eef6f1;
    }}
    .bots-command-item:focus-visible {{
      outline: 2px solid #5ba67a;
      outline-offset: 1px;
    }}
    .bots-command-item.is-active {{
      border-color: var(--accent);
      box-shadow: 0 0 0 1px color-mix(in srgb, var(--accent) 24%, transparent);
      background: #e9f5ee;
      color: #1f5d3d;
    }}
    .bots-command-item.is-disabled {{
      opacity: 0.62;
    }}
    .bots-command-item-order {{
      font-size: 10px;
      color: var(--surface-tint-text-soft);
      min-width: 18px;
      white-space: nowrap;
    }}
    .bots-command-copy {{
      min-width: 0;
      display: flex;
      flex-direction: column;
      gap: 3px;
    }}
    .bots-command-heading {{
      min-width: 0;
      display: flex;
      align-items: center;
      gap: 6px;
    }}
    .bots-command-usage {{
      font-family: "IBM Plex Mono", "Cascadia Mono", "Consolas", monospace;
      font-size: 11px;
      color: #1f4e35;
      word-break: break-word;
    }}
    .bots-command-description {{
      font-size: 11px;
      line-height: 1.35;
      color: var(--surface-tint-text-soft);
    }}
    .bots-command-kind {{
      font-size: 9px;
      font-weight: 700;
      letter-spacing: 0.3px;
      text-transform: uppercase;
      color: #315844;
      border: 1px solid #c7d9cb;
      border-radius: 999px;
      background: #edf6ef;
      padding: 2px 6px;
      white-space: nowrap;
    }}
    .bots-command-state {{
      font-size: 10px;
      color: #355b44;
      text-transform: uppercase;
      letter-spacing: 0.2px;
      white-space: nowrap;
      min-width: 24px;
      text-align: right;
    }}
    .bots-command-state.is-enabled {{
      color: #1f6e42;
      font-weight: 700;
    }}
    .bots-command-state.is-disabled {{
      color: #8f4234;
      font-weight: 700;
    }}
    .bots-history-panel {{
      min-height: 0;
      overflow: visible;
    }}
    .bots-history-head {{
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
    }}
    .bots-review-head-copy {{
      display: flex;
      flex-direction: column;
      gap: 8px;
      min-width: 0;
    }}
    .bots-review-tabs {{
      display: inline-flex;
      align-items: center;
      gap: 6px;
      flex-wrap: wrap;
    }}
    .bots-review-tab {{
      border: 1px solid #c8d9cc;
      border-radius: 999px;
      background: #edf6ef;
      color: #365846;
      font-size: 11px;
      line-height: 1;
      padding: 6px 10px;
      cursor: pointer;
    }}
    .bots-review-tab.is-active {{
      background: var(--accent);
      border-color: var(--accent);
      color: #ffffff;
    }}
    .bots-history-clear-btn {{
      padding: 4px 10px;
      font-size: 11px;
    }}
    .bots-tab-panel {{
      display: flex;
      flex-direction: column;
      gap: 10px;
      min-height: 0;
      flex: 1 1 auto;
    }}
    .bots-tab-panel[hidden] {{
      display: none !important;
    }}
    .bots-history-stats {{
      display: flex;
      flex-wrap: wrap;
      gap: 8px 10px;
      font-size: 11px;
      color: #355b44;
    }}
    .bots-history-list {{
      border: 1px solid #ccddcf;
      border-radius: 8px;
      background: #ffffff;
      flex: 1 1 auto;
      max-height: none;
      min-height: 120px;
      overflow: auto;
      padding: 6px;
      display: flex;
      flex-direction: column;
      gap: 6px;
    }}
    .bots-sniffer-controls {{
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      align-items: end;
    }}
    .bots-sniffer-filter-wrap {{
      display: flex;
      flex-direction: column;
      gap: 6px;
      flex: 1 1 260px;
      min-width: 220px;
    }}
    .bots-sniffer-filter-label {{
      font-size: 10px;
      color: var(--surface-tint-text-soft);
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.2px;
    }}
    .bots-sniffer-command-input {{
      width: 100%;
      min-width: 0;
      border-radius: 8px;
      border: 1px solid #c8d9cc;
      background: #ffffff;
      color: #204231;
      font-size: 12px;
      padding: 8px 10px;
    }}
    .bots-sniffer-actions {{
      display: inline-flex;
      align-items: center;
      gap: 6px;
      flex: 0 0 auto;
    }}
    .bots-sniffer-btn {{
      padding: 6px 10px;
      font-size: 11px;
    }}
    .bots-history-empty {{
      border: 1px dashed #cfe0d2;
      border-radius: 8px;
      color: var(--surface-tint-text-soft);
      background: #f7fbf7;
      font-size: 12px;
      line-height: 1.35;
      padding: 10px;
      text-align: center;
    }}
    .bots-history-item {{
      border: 1px solid var(--surface-tint-border);
      border-radius: 8px;
      padding: 8px;
      display: flex;
      flex-direction: column;
      gap: 4px;
      background: var(--surface-tint-bg-soft);
    }}
    .bots-history-item-head {{
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 8px;
      flex-wrap: wrap;
    }}
    .bots-history-command {{
      font-family: "IBM Plex Mono", "Cascadia Mono", "Consolas", monospace;
      font-size: 11px;
      color: #1f4e35;
      word-break: break-word;
    }}
    .bots-history-meta {{
      font-size: 10px;
      color: #5b7565;
      display: inline-flex;
      align-items: center;
      gap: 7px;
      flex-wrap: wrap;
    }}
    .bots-history-status-pill {{
      display: inline-flex;
      align-items: center;
      border-radius: 999px;
      border: 1px solid #bfd4c3;
      background: #edf6ef;
      color: #204231;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.2px;
      text-transform: uppercase;
      line-height: 1.2;
      padding: 2px 7px;
    }}
    .bots-history-status-pill.pending {{
      border-color: #d3a856;
      background: #fff8e8;
      color: #7a5a1f;
    }}
    .bots-history-status-pill.responded {{
      border-color: var(--accent);
      background: #e9f8ee;
      color: #1d5a3b;
    }}
    .bots-history-response {{
      border-left: 2px solid #bfd4c3;
      padding-left: 7px;
      font-size: 11px;
      color: #274935;
      line-height: 1.3;
      white-space: pre-wrap;
      word-break: break-word;
    }}
    .bots-history-response strong {{
      color: #1f4e35;
    }}

    .settings-channels-table {{
      width: 100%;
      border-collapse: collapse;
      font-size: 12px;
      min-width: 620px;
    }}
    .settings-channels-table th,
    .settings-channels-table td {{
      padding: 6px 8px;
      border-bottom: 1px solid #e1ece0;
      vertical-align: middle;
    }}
    .settings-channels-table th {{
      font-size: 10px;
      letter-spacing: 0.2px;
      text-transform: uppercase;
      color: #355b44;
      white-space: nowrap;
    }}
    .settings-channels-table td {{
      color: #0f2018;
    }}
    .settings-channels-table td .settings-input {{
      width: 100%;
      box-sizing: border-box;
    }}

    /* Beginner-friendly default: hide MQTT bridge toggles unless Advanced is enabled. */
    body:not(.channels-advanced-ui) .settings-channel-mqtt {{
      display: none;
    }}

    .settings-channels-simple-actions {{
      border: 1px dashed var(--surface-tint-border);
      border-radius: 12px;
      padding: 10px;
      background: var(--surface-tint-bg-soft);
      display: flex;
      flex-direction: column;
      gap: 10px;
    }}
    .settings-channels-actions {{
      display: flex;
      gap: 8px;
      align-items: center;
      flex-wrap: wrap;
    }}
    .settings-channels-empty {{
      text-align: center;
      padding: 12px 8px;
      color: #3d5a49;
      opacity: 0.85;
      font-size: 12px;
    }}

    .map-data {{
      grid-column: 1 / span 3;
      grid-row: 7;
      display: flex;
      flex-direction: column;
      min-height: 0;
    }}
    .map-data .body {{
      flex: 1 1 auto;
      min-height: 0;
      display: flex;
      flex-direction: column;
      gap: 8px;
    }}
    .map-data-live, .map-data-node, .map-data-activity {{
      min-height: 0;
      display: flex;
      flex-direction: column;
      gap: 8px;
      flex: 1 1 auto;
    }}
    .history-activity-stack {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      gap: 8px;
      overflow: hidden;
    }}
    .history-activity-tabbar {{
      margin-bottom: 2px;
    }}
    .history-activity-panel {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      gap: 10px;
      overflow: auto;
      padding-right: 2px;
    }}
    .history-activity-panel[hidden] {{
      display: none !important;
    }}
    .history-activity-main-scroll {{
      display: flex;
      flex-direction: column;
      gap: 8px;
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
      padding-right: 2px;
    }}
    .history-activity-splitter {{
      display: none;
      position: relative;
      border-radius: 8px;
      border: 1px solid var(--surface-tint-border);
      background: linear-gradient(90deg, var(--surface-tint-bg-soft), var(--surface-tint-bg-alt));
      cursor: row-resize;
      touch-action: none;
      user-select: none;
      min-height: 6px;
      flex: 0 0 var(--splitter-size);
      margin: 8px 0;
    }}
    .history-activity-splitter::before {{
      content: "";
      position: absolute;
      left: 8px;
      right: 8px;
      top: 50%;
      height: 2px;
      transform: translateY(-50%);
      background: var(--surface-tint-border-strong);
      border-radius: 1px;
    }}
    .history-activity-splitter:hover::before,
    .history-activity-splitter.active::before {{
      background: var(--accent);
    }}
    .history-activity-weekly-panel {{
      display: flex;
      flex-direction: column;
      gap: 8px;
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
      padding-right: 2px;
    }}
    .history-activity-encryption-trend {{
      display: flex;
      flex-direction: column;
      gap: 8px;
      flex: 1 1 auto;
      min-height: 0;
    }}
    .history-activity-chat-trend,
    .history-activity-encryption-routes {{
      display: flex;
      flex-direction: column;
      gap: 8px;
      flex: 1 1 auto;
      min-height: 220px;
    }}
    .map-data-live[hidden], .map-data-node[hidden], .map-data-activity[hidden] {{
      display: none !important;
    }}
    .history-caption {{
      font-size: 12px;
      color: #3e5a46;
      line-height: 1.35;
    }}
    .history-tabs {{
      display: flex;
      gap: 6px;
      border-bottom: 1px solid var(--surface-tint-border);
      padding-bottom: 6px;
    }}
    .history-panel[hidden] {{
      display: none !important;
    }}
    #tab-panel-names,
    #tab-panel-overview {{
      display: flex;
      flex-direction: column;
      min-height: 0;
      gap: 8px;
    }}
    .node-name-history-host,
    .node-history-overview-host {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
      padding-right: 2px;
    }}
    #signal-chart-wrap,
    #node-link-quality-chart-wrap,
    #node-online-chart-wrap,
    #node-packets-chart-wrap,
    #online-activity-chart-wrap,
    #weekly-summary-chart-wrap,
    #network-overview-chart-wrap,
    #history-chat-trend-chart-wrap,
    #encription-trend-chart-wrap,
    #encription-route-chart-wrap {{
      position: relative;
      width: 100%;
      min-height: 220px;
      height: 220px;
      border: 1px solid var(--surface-tint-border);
      border-radius: 8px;
      background: linear-gradient(180deg, var(--panel) 0%, var(--surface-tint-bg-soft) 100%);
      overflow: hidden;
    }}
    #signal-chart,
    #node-link-quality-chart,
    #node-online-chart,
    #node-packets-chart,
    #online-activity-chart,
    #weekly-summary-chart,
    #network-overview-chart,
    #history-chat-trend-chart,
    #encription-trend-chart,
    #encription-route-chart {{
      width: 100%;
      height: 100%;
      display: block;
    }}
    .layout.view-history #map-data-activity .history-activity-stack {{
      min-height: 0;
      height: 100%;
      overflow: hidden;
    }}
    .layout.view-history #map-data-activity .history-activity-panel {{
      overflow: visible;
      padding-right: 0;
    }}
    .layout.view-history #map-data-activity .history-activity-weekly-panel {{
      flex: 0 0 auto;
      min-height: 0;
      overflow: visible;
      padding-right: 0;
    }}
    .layout.view-history #map-data-activity .history-activity-chat-trend,
    .layout.view-history #map-data-activity .history-activity-encryption-trend,
    .layout.view-history #map-data-activity .history-activity-encryption-routes {{
      flex: 0 0 auto;
      min-height: 0;
    }}
    .layout.view-history #map-data-activity #weekly-summary-chart-wrap {{
      flex: 0 0 auto;
      min-height: clamp(170px, 30vh, 260px);
      height: clamp(170px, 30vh, 260px);
    }}
    .layout.view-history #map-data-activity #history-chat-trend-chart-wrap,
    .layout.view-history #map-data-activity #encription-trend-chart-wrap {{
      min-height: clamp(140px, 24vh, 220px);
      height: clamp(140px, 24vh, 220px);
    }}
    .layout.view-history #map-data-activity #encription-route-chart-wrap {{
      min-height: clamp(140px, 24vh, 220px);
      height: clamp(140px, 24vh, 220px);
    }}
    .layout.view-history #map-data-activity #weekly-summary-overview {{
      flex: 0 0 auto;
      margin-top: auto;
    }}
    .history-metric-wrap {{
      display: inline-flex;
      align-items: center;
      gap: 8px;
      width: fit-content;
      max-width: 100%;
      border: 1px solid #d0dfd0;
      border-radius: 999px;
      background: var(--surface-tint-bg);
      padding: 4px 9px;
    }}
    .history-window-wrap {{
      margin-left: auto;
    }}
    .history-select-chip-hide-label > .history-metric-label,
    .history-select-chip-hide-label > label {{
      display: none;
    }}
    .history-select-chip-hide-label {{
      gap: 0;
      border-color: transparent;
      padding-left: 8px;
      padding-right: 8px;
      box-shadow: none;
    }}
    .history-group-chip-hide-title > .history-metric-label {{
      display: none;
    }}
    .history-group-chip-hide-title {{
      padding-left: 8px;
      padding-right: 8px;
    }}
    .history-metric-wrap[hidden] {{
      display: none !important;
    }}
    .history-metric-wrap-lines {{
      gap: 10px;
      padding-right: 10px;
      flex-wrap: wrap;
      row-gap: 6px;
    }}
    .history-metric-controls {{
      display: flex;
      align-items: center;
      flex-wrap: wrap;
      gap: 8px;
      width: 100%;
    }}
    .history-metric-inline {{
      display: inline-flex;
      align-items: center;
      gap: 4px;
      font-size: 11px;
      color: #325744;
      font-weight: 600;
      line-height: 1;
      white-space: nowrap;
      cursor: pointer;
    }}
    .history-metric-label {{
      font-size: 10px;
      line-height: 1.1;
      letter-spacing: 0.18px;
      text-transform: uppercase;
      color: #3f5f4f;
      font-weight: 700;
      white-space: nowrap;
    }}
    .history-metric-select {{
      border: 0;
      border-radius: 999px;
      background: #ffffff;
      color: #274935;
      font-size: 11px;
      line-height: 1.2;
      padding: 2px 8px;
      min-height: 24px;
      min-width: 160px;
      max-width: 100%;
      box-shadow: none;
    }}
    .layout.view-network .history-select-chip-hide-label,
    .layout.view-network .map-heatmap-mode-wrap {{
      position: relative;
      min-height: 27px;
      height: 27px;
      border: 1px solid color-mix(in srgb, var(--line) 74%, var(--accent));
      border-radius: 999px;
      background:
        linear-gradient(
          180deg,
          color-mix(in srgb, var(--panel) 94%, var(--accent) 6%),
          color-mix(in srgb, var(--panel) 84%, var(--accent) 16%)
        );
      color: color-mix(in srgb, var(--ink) 86%, var(--accent-2));
      padding: 0 23px 0 7px;
      box-shadow: none;
    }}
    .layout.view-network .history-select-chip-hide-label::after,
    .layout.view-network .map-heatmap-mode-wrap::after {{
      content: "";
      position: absolute;
      right: 9px;
      top: 50%;
      width: 0;
      height: 0;
      border-left: 3px solid transparent;
      border-right: 3px solid transparent;
      border-top: 5px solid color-mix(in srgb, var(--accent-2) 64%, var(--ink));
      pointer-events: none;
      transform: translateY(-35%);
    }}
    .layout.view-network .history-select-chip-hide-label .history-metric-select,
    .layout.view-network .history-select-chip-hide-label .env-metric-select,
    .layout.view-network .map-heatmap-mode {{
      -webkit-appearance: none;
      appearance: none;
      color-scheme: light;
      border: 0;
      background: transparent;
      color: inherit;
      min-height: 25px;
      height: 25px;
      padding: 0;
      box-shadow: none;
      cursor: pointer;
    }}
    .layout.view-network .history-select-chip-hide-label .history-metric-select option,
    .layout.view-network .history-select-chip-hide-label .env-metric-select option,
    .layout.view-network .map-heatmap-mode option {{
      color: var(--ink);
      background: color-mix(in srgb, var(--panel) 92%, var(--bg) 8%);
    }}
    .layout.view-network .history-select-chip-hide-label .history-metric-select option:checked,
    .layout.view-network .history-select-chip-hide-label .env-metric-select option:checked,
    .layout.view-network .map-heatmap-mode option:checked {{
      color: color-mix(in srgb, var(--ink) 86%, var(--accent-2) 14%);
      background: color-mix(in srgb, var(--panel) 76%, var(--accent) 24%);
    }}
    [data-theme="dark"] .layout.view-network .history-select-chip-hide-label .history-metric-select option,
    [data-theme="dark"] .layout.view-network .history-select-chip-hide-label .env-metric-select option,
    [data-theme="dark"] .layout.view-network .map-heatmap-mode option {{
      color: var(--workspace-shell-text);
      background: var(--workspace-shell-bg-alt);
    }}
    [data-theme="dark"] .layout.view-network .history-select-chip-hide-label .history-metric-select option:checked,
    [data-theme="dark"] .layout.view-network .history-select-chip-hide-label .env-metric-select option:checked,
    [data-theme="dark"] .layout.view-network .map-heatmap-mode option:checked {{
      color: var(--workspace-shell-active-text);
      background: var(--workspace-shell-active-bg);
    }}
    .history-metric-toggle {{
      width: 14px;
      height: 14px;
      accent-color: var(--accent-2);
      cursor: pointer;
    }}
    #signal-chart,
    #node-online-chart,
    #node-packets-chart {{
      cursor: zoom-in;
      touch-action: none;
    }}
    #signal-chart.zoomed,
    #node-online-chart.zoomed,
    #node-packets-chart.zoomed {{
      cursor: grab;
    }}
    #signal-chart.chart-dragging,
    #node-online-chart.chart-dragging,
    #node-packets-chart.chart-dragging {{
      cursor: grabbing;
    }}
    #online-activity-chart,
    #weekly-summary-chart,
    #network-overview-chart {{
      cursor: zoom-in;
      touch-action: none;
    }}
    #online-activity-chart.zoomed,
    #weekly-summary-chart.zoomed,
    #network-overview-chart.zoomed {{
      cursor: grab;
    }}
    #online-activity-chart.chart-dragging,
    #weekly-summary-chart.chart-dragging,
    #network-overview-chart.chart-dragging {{
      cursor: grabbing;
    }}
    #signal-chart text,
    #node-online-chart text,
    #node-packets-chart text,
    #online-activity-chart text,
    #weekly-summary-chart text,
    #network-overview-chart text,
    #history-chat-trend-chart text,
    #encription-trend-chart text,
    #encription-route-chart text,
    #env-metrics-chart text {{
      font-family: "IBM Plex Sans", "Segoe UI", sans-serif;
      font-variant-numeric: tabular-nums;
    }}
    .chart-axis-label {{
      font-size: 10px;
      font-weight: 600;
      letter-spacing: 0.01em;
    }}
    .signal-empty {{
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #5d7467;
      font-size: 12px;
      text-align: center;
      padding: 10px;
      background: rgba(250, 255, 251, 0.9);
    }}
    .signal-empty[hidden] {{
      display: none !important;
    }}
    .signal-legend {{
      margin-top: 6px;
      font-size: 11px;
      color: #284a37;
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 5px;
    }}
    .signal-timeline {{
      position: relative;
      height: 44px;
      margin-top: 6px;
    }}
    .signal-timeline-track {{
      position: absolute;
      top: 10px;
      height: 1px;
      background: var(--surface-tint-border);
      border-radius: 1px;
      pointer-events: none;
    }}
    .signal-timeline-mark {{
      position: absolute;
      top: 0;
      transform: translateX(-50%);
      pointer-events: none;
    }}
    .signal-timeline-mark.edge-start {{
      transform: translateX(0);
    }}
    .signal-timeline-mark.edge-end {{
      transform: translateX(-100%);
    }}
    .signal-timeline-tick {{
      width: 1px;
      height: 7px;
      margin-top: 6px;
      background: #6d8577;
      border-radius: 1px;
    }}
    .signal-timeline-tick-major {{
      height: 9px;
      background: #5b7668;
    }}
    .signal-timeline-tick-minor {{
      height: 4px;
      margin-top: 8px;
      background: #9aafa2;
      opacity: 0.66;
    }}
    .signal-timeline-label {{
      margin-top: 4px;
      font-size: 10px;
      color: #4f6759;
      white-space: nowrap;
      font-variant-numeric: tabular-nums;
    }}
    .signal-timeline-label-major {{
      font-weight: 600;
      color: #3f5a4b;
    }}
    .signal-timeline-empty {{
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #5d7467;
      font-size: 11px;
      text-align: center;
      padding: 2px 4px;
    }}
    .legend-chip {{
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }}
    .signal-legend .legend-chip.has-dynamic-quality-gradient {{
      position: relative;
      display: grid;
      grid-template-columns: minmax(86px, max-content) auto minmax(90px, 1fr) auto;
      gap: 6px;
      width: 100%;
      align-items: center;
    }}
    .signal-legend .legend-chip.is-primary,
    .activity-legend-nodes {{
      color: #009E73;
    }}
    .signal-legend .legend-chip.is-compare {{
      color: #56B4E9;
    }}
    .signal-legend .legend-chip.is-warn {{
      color: #E69F00;
    }}
    .signal-legend .legend-chip.is-danger {{
      color: #D55E00;
    }}
    .legend-chip::before {{
      content: "";
      width: 11px;
      height: 2px;
      border-radius: 1px;
      background: currentColor;
    }}
    .signal-legend .legend-chip.has-dynamic-quality-gradient::before {{
      display: none;
    }}
    .signal-legend .legend-chip.has-dynamic-quality-gradient .quality-scale-track {{
      position: relative;
      display: block;
      width: 100%;
      height: 6px;
      border-radius: 999px;
      background: var(--signal-legend-quality-gradient, currentColor);
      box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.26);
    }}
    .signal-legend .legend-chip.has-dynamic-quality-gradient .quality-scale-label {{
      color: var(--workspace-shell-text-soft, currentColor);
      font-size: 9px;
      line-height: 1;
      text-transform: uppercase;
      opacity: 0.78;
    }}
    .signal-legend .legend-chip.has-dynamic-quality-gradient .legend-chip-label {{
      min-width: 0;
    }}
    .signal-legend .legend-chip.has-dynamic-quality-gradient.has-average-marker .quality-scale-track::after {{
      content: "";
      position: absolute;
      left: var(--signal-legend-quality-marker-left, 50%);
      top: 50%;
      width: 2px;
      height: 10px;
      border-radius: 999px;
      background: var(--workspace-shell-text, currentColor);
      box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.45);
      transform: translate(-1px, -50%);
    }}
    .overview-grid {{
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
      gap: 6px;
    }}
    .overview-item {{
      border: 1px solid var(--surface-tint-border);
      border-radius: 8px;
      background: var(--surface-tint-bg-soft);
      padding: 6px 7px;
    }}
    .overview-item .k {{
      color: #5e6e64;
      font-size: 10px;
      text-transform: uppercase;
    }}
    .overview-item .v {{
      margin-top: 2px;
      font-size: 12px;
      font-weight: 600;
      color: #193a28;
      line-height: 1.2;
    }}
    .history-chat {{
      display: none;
      flex-direction: column;
      min-height: 0;
    }}
    .history-chat .body {{
      display: flex;
      flex-direction: column;
      gap: 8px;
      min-height: 0;
    }}
    .history-chat-tabbar {{
      display: flex;
      gap: 6px;
      border-bottom: 1px solid var(--surface-tint-border);
      padding-bottom: 6px;
    }}
    .history-chat-tab-btn {{
      border: 1px solid var(--surface-tint-border);
      background: #f3faf5;
      color: #1f5d40;
      border-radius: 999px;
      font-size: 11px;
      padding: 4px 10px;
      cursor: pointer;
    }}
    .history-chat-tab-btn.is-active,
    .history-chat-tab-btn[aria-selected="true"] {{
      background: #dff1e6;
      border-color: #87b99a;
      color: #14442d;
      font-weight: 600;
    }}
    .history-chat-panel {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      gap: 8px;
    }}
    .history-chat-panel[hidden] {{
      display: none !important;
    }}
    .history-chat .scroll {{
      flex: 1 1 auto;
      min-height: 0;
      max-height: none;
    }}
    .history-chat-stack {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      gap: 0;
    }}
    .history-chat-main-scroll {{
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
    }}
    .history-chat-splitter {{
      display: none;
      position: relative;
      border-radius: 8px;
      border: 1px solid var(--surface-tint-border);
      background: linear-gradient(90deg, var(--surface-tint-bg-soft), var(--surface-tint-bg-alt));
      cursor: row-resize;
      touch-action: none;
      user-select: none;
      min-height: 6px;
      flex: 0 0 var(--splitter-size);
      margin: 8px 0;
    }}
    .history-chat-splitter::before {{
      content: "";
      position: absolute;
      left: 8px;
      right: 8px;
      top: 50%;
      height: 2px;
      transform: translateY(-50%);
      background: var(--surface-tint-border-strong);
      border-radius: 1px;
    }}
    .history-chat-splitter:hover::before,
    .history-chat-splitter.active::before {{
      background: var(--accent);
    }}
    .history-notification-section {{
      display: flex;
      flex-direction: column;
      gap: 6px;
      flex: 1 1 auto;
      min-height: 0;
    }}
    .history-notification-head {{
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
    }}
    .history-notification-clear-btn {{
      border: 1px solid #b8d0bf;
      background: #ffffff;
      color: #28533c;
      border-radius: 999px;
      padding: 2px 8px;
      font-size: 10px;
      line-height: 1.3;
      cursor: pointer;
      white-space: nowrap;
      flex: 0 0 auto;
    }}
    .history-notification-clear-btn:hover {{
      background: #eaf5ed;
      border-color: #94b7a0;
    }}
    .history-notification-list {{
      display: flex;
      flex-direction: column;
      gap: 4px;
      flex: 1 1 auto;
      min-height: 0;
      max-height: none;
      overflow: auto;
      padding-right: 2px;
    }}
    .history-notification-empty {{
      border: 1px dashed #c5d7ca;
      border-radius: 8px;
      padding: 10px 9px;
      color: #4f6a5b;
      font-size: 11px;
      line-height: 1.35;
      text-align: center;
      background: var(--panel);
    }}
    .history-notification-item {{
      border: 1px solid #d3e3d6;
      background: var(--panel);
      color: #214836;
      border-radius: 8px;
      padding: 6px 8px;
      display: flex;
      flex-direction: column;
      gap: 3px;
      line-height: 1.3;
    }}
    .history-notification-item.is-new {{
      box-shadow: inset 2px 0 0 var(--accent);
    }}
    .history-notification-meta {{
      display: flex;
      align-items: center;
      flex-wrap: wrap;
      gap: 4px;
      font-size: 10px;
      color: #5e7a69;
      line-height: 1.2;
    }}
    .history-notification-pill {{
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 999px;
      border: 1px solid #afc9b7;
      background: #e6f3ea;
      color: #2f6448;
      font-size: 9px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.32px;
      padding: 1px 6px;
      line-height: 1.2;
    }}
    .history-notification-pill.status-new {{
      background: #d7f0de;
      border-color: #84b696;
      color: #205238;
    }}
    .history-notification-pill.kind-reaction {{
      background: #f4e8cb;
      border-color: #c2a46b;
      color: #6a4e14;
    }}
    .history-notification-pill.kind-status {{
      background: #dceaf6;
      border-color: #90b6d8;
      color: #244f73;
    }}
    .history-notification-text {{
      font-size: 11px;
      color: #244734;
      line-height: 1.35;
      word-break: break-word;
    }}
    .history-notification-time {{
      font-size: 10px;
      color: #5d7666;
      line-height: 1.25;
    }}
    .history-encription-section {{
      display: flex;
      flex-direction: column;
      gap: 8px;
      flex: 1 1 auto;
      min-height: 0;
      border-top: 1px solid var(--surface-tint-border);
      padding-top: 8px;
    }}
    .history-encription-panels {{
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: repeat(2, minmax(0, 1fr));
      gap: 8px;
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
    }}
    .history-encription-panel {{
      display: flex;
      flex-direction: column;
      min-height: 0;
    }}
    .history-encription-panel h3 {{
      margin: 0 0 6px 0;
      font-size: 11px;
      font-weight: 700;
      letter-spacing: 0.03em;
      text-transform: uppercase;
      opacity: 0.82;
    }}
    .history-encription-panel .scroll {{
      flex: 1 1 auto;
      min-height: 0;
      max-height: none;
    }}
    .nodes {{
      grid-column: 1 / span 3;
      grid-row: 4;
      display: flex;
      flex-direction: column;
      min-height: 0;
    }}
    .favorites {{
      grid-column: 1 / span 3;
      grid-row: 4;
      display: none;
      flex-direction: column;
      min-height: 0;
    }}
    .favorites .body {{
      display: flex;
      flex-direction: column;
      gap: 8px;
      flex: 1 1 auto;
      min-height: 0;
    }}
    .favorites-toolbar {{
      display: flex;
      align-items: center;
      gap: 6px;
    }}
    .favorites-caption {{
      font-size: 11px;
      color: var(--surface-tint-text-soft);
      line-height: 1.25;
    }}
    .favorites-list {{
      flex: 1 1 auto;
      min-height: 200px;
      max-height: none;
      display: flex;
      flex-direction: column;
      gap: 6px;
    }}
    .favorites-empty {{
      border: 1px dashed var(--surface-tint-border);
      border-radius: 8px;
      padding: 10px;
      font-size: 11px;
      color: var(--surface-tint-text-soft);
      background: color-mix(in srgb, var(--surface-tint-bg-soft) 76%, transparent);
      line-height: 1.3;
    }}
    .favorite-node-item {{
      border: 1px solid var(--surface-tint-border);
      background: color-mix(in srgb, var(--surface-tint-bg-alt) 82%, transparent);
      color: var(--surface-tint-text);
      border-radius: 8px;
      padding: 6px 7px;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 7px;
      align-items: start;
      cursor: pointer;
    }}
    .favorite-node-item:hover {{
      background: color-mix(in srgb, var(--surface-tint-bg-hover) 86%, transparent);
      border-color: var(--surface-tint-border-strong);
    }}
    .favorite-node-item.status-warn {{
      background: #fff9e7;
      color: #5d4a1b;
      border-color: #ecd89c;
    }}
    .favorite-node-item.status-stale {{
      background: #fff0f0;
      color: #6a3333;
      border-color: #e6b5b5;
    }}
    .favorite-node-item.status-unknown {{
      background: color-mix(in srgb, var(--surface-tint-bg-soft) 72%, transparent);
      color: var(--surface-tint-text-soft);
      border-color: color-mix(in srgb, var(--surface-tint-border) 76%, transparent);
    }}
    .favorite-node-item.selected-node {{
      background: color-mix(in srgb, var(--surface-tint-bg-hover) 86%, var(--surface-tint-border-strong) 14%);
      border-color: var(--surface-tint-border-strong);
      color: var(--surface-tint-text);
      box-shadow: inset 2px 0 0 var(--surface-tint-border-strong);
    }}
    .favorite-node-main {{
      min-width: 0;
      display: flex;
      flex-direction: column;
      gap: 2px;
    }}
    .favorite-node-name-row {{
      display: flex;
      align-items: center;
      gap: 6px;
      min-width: 0;
    }}
    .favorite-node-name {{
      font-size: 12px;
      font-weight: 600;
      color: inherit;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      min-width: 0;
    }}
    .favorite-node-id {{
      font-size: 10px;
      color: var(--surface-tint-text-soft);
      opacity: 0.95;
      white-space: nowrap;
      flex: 0 0 auto;
    }}
    .favorite-node-meta {{
      font-size: 10px;
      color: var(--surface-tint-text-soft);
      line-height: 1.25;
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }}
    .nodes .body {{
      display: flex;
      flex-direction: column;
      gap: 8px;
      flex: 1 1 auto;
      min-height: 0;
      overflow: hidden;
    }}
    .network-node-stack {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      gap: 0;
    }}
    .nodes-table-scroll {{
      flex: 1 1 auto;
      max-height: none;
      min-height: 0;
    }}
    .network-node-splitter {{
      display: none;
      position: relative;
      border-radius: 8px;
      border: 1px solid var(--surface-tint-border);
      background: linear-gradient(90deg, var(--surface-tint-bg-soft), var(--surface-tint-bg-alt));
      cursor: row-resize;
      touch-action: none;
      user-select: none;
      min-height: 6px;
      flex: 0 0 var(--splitter-size);
      margin: 8px 0;
    }}
    .network-node-splitter::before {{
      content: "";
      position: absolute;
      left: 8px;
      right: 8px;
      top: 50%;
      height: 2px;
      transform: translateY(-50%);
      background: var(--surface-tint-border-strong);
      border-radius: 1px;
    }}
    .network-node-splitter:hover::before,
    .network-node-splitter.active::before {{
      background: var(--accent);
    }}
    .network-node-history-host {{
      display: none;
      flex-direction: column;
      gap: 8px;
      min-height: 0;
      padding-top: 0;
      border-top: none;
    }}
    .network-node-history-host:not([hidden]) {{
      display: flex;
    }}
    .network-node-history-host.empty::before {{
      content: "Select a node to view signal history and rollups.";
      display: block;
      border: 1px dashed #c3d8c8;
      border-radius: 8px;
      background: #f7fcf8;
      color: #466656;
      font-size: 11px;
      line-height: 1.3;
      padding: 8px 10px;
    }}
    .packets {{ grid-column: 1; grid-row: 8; }}
    .channels-view {{
      display: none;
      flex-direction: column;
      min-height: 0;
    }}
    .channels-view .body {{
      display: flex;
      flex-direction: column;
      gap: 10px;
      min-height: 0;
      overflow: hidden;
    }}
    .channels-dashboard-grid {{
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: auto minmax(0, 1fr);
      gap: 10px;
      flex: 1 1 auto;
      min-height: 0;
      align-items: stretch;
    }}
    .channels-dashboard-panel {{
      display: flex;
      flex-direction: column;
      min-height: 0;
      gap: 10px;
    }}
    .channels-dashboard-panel.settings-panel {{
      background: #f7fcf8;
      border: 1px solid var(--surface-tint-border);
      border-radius: 14px;
      padding: 12px;
    }}
    .channels-dashboard-panel-summary {{
      grid-column: 1;
      grid-row: 1;
      gap: 8px;
    }}
    .channels-dashboard-panel-activity {{
      grid-row: 2;
      display: grid;
      grid-template-rows: auto minmax(0, 1fr);
      row-gap: 8px;
      align-items: stretch;
      min-height: 0;
    }}
    .channels-dashboard-panel-activity.settings-panel {{
      background: #fcfffc;
    }}
    .channels-section-head {{
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 12px;
      flex-wrap: wrap;
    }}
    .channels-section-head h3 {{
      margin: 0;
      font-size: 11px;
      font-weight: 700;
      letter-spacing: 0.03em;
      text-transform: uppercase;
      opacity: 0.82;
    }}
    .channels-section-actions {{
      display: flex;
      gap: 8px;
      align-items: center;
      flex-wrap: wrap;
    }}
    .channels-summary-actions {{
      align-items: center;
      justify-content: flex-end;
    }}
    .channels-summary-toggle {{
      margin: 0;
      white-space: nowrap;
    }}
    .channels-panel-copy {{
      color: #466656;
      font-size: 12px;
      line-height: 1.45;
      margin-top: 3px;
    }}
    .channels-current-note {{
      margin: 0;
    }}
    .channels-local-fields {{
      display: flex;
      flex-direction: column;
      gap: 10px;
      flex: 1 1 auto;
      min-height: 0;
    }}
    .channels-local-table-row {{
      flex: 1 1 auto;
      min-height: 0;
    }}
    .channels-local-table-wrap {{
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
    }}
    .channels-status-row {{
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      flex-wrap: wrap;
      min-height: 22px;
    }}
    .channels-status {{
      flex: 1 1 auto;
      min-width: 0;
    }}
    .channels-activity-table-wrap {{
      flex: 1 1 auto;
      align-self: stretch;
      min-height: 0;
      max-height: none;
      --channels-fill-row-height: 46px;
      --channels-fill-row-odd: #f8fbf8;
      --channels-fill-row-even: #eef5ef;
      --channels-fill-row-border: #e1ece0;
      background:
        repeating-linear-gradient(
          to bottom,
          var(--channels-fill-row-odd) 0,
          var(--channels-fill-row-odd) calc(var(--channels-fill-row-height) - 1px),
          var(--channels-fill-row-border) calc(var(--channels-fill-row-height) - 1px),
          var(--channels-fill-row-border) var(--channels-fill-row-height),
          var(--channels-fill-row-even) var(--channels-fill-row-height),
          var(--channels-fill-row-even) calc((var(--channels-fill-row-height) * 2) - 1px),
          var(--channels-fill-row-border) calc((var(--channels-fill-row-height) * 2) - 1px),
          var(--channels-fill-row-border) calc(var(--channels-fill-row-height) * 2)
        );
    }}
    .settings-channels-filler-row td {{
      color: transparent;
      user-select: none;
      height: 46px;
      padding-top: 0;
      padding-bottom: 0;
      line-height: 0;
    }}
    .channels-activity-summary {{
      display: flex;
      flex-direction: column;
      gap: 10px;
    }}
    .channels-summary-footer {{
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      flex-wrap: wrap;
    }}
    .channels-settings-compact {{
      gap: 8px;
    }}
    .channels-settings-compact .settings-row {{
      margin-bottom: 0;
    }}
    .channels-settings-compact .settings-field {{
      min-width: 0;
    }}
    .channels-dashboard-panel-summary .channels-panel-copy {{
      margin-top: 0;
    }}
    .channels-dashboard-panel-summary .overview-grid {{
      gap: 8px;
    }}
    .channels-dashboard-panel-summary .overview-item {{
      min-height: 0;
    }}
    .channels-app-routing {{
      border: 1px solid var(--surface-tint-border);
      border-radius: 12px;
      background: #fcfffc;
      padding: 8px;
      display: flex;
      flex-direction: column;
      gap: 6px;
    }}
    .channels-app-routing-head {{
      display: flex;
      flex-direction: column;
      gap: 2px;
    }}
    .channels-app-routing-head-main {{
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
    }}
    .channels-app-routing-head h4 {{
      margin: 0;
      font-size: 11px;
      font-weight: 700;
      letter-spacing: 0.03em;
      text-transform: uppercase;
      opacity: 0.82;
    }}
    .channels-app-routing-toggle.btn {{
      padding: 3px 9px;
      min-height: 0;
      font-size: 10px;
      line-height: 1.2;
    }}
    .channels-app-routing-summary {{
      font-size: 11px;
      color: #355948;
      line-height: 1.35;
      display: flex;
      flex-wrap: wrap;
      gap: 6px 10px;
      align-items: center;
    }}
    .channels-app-routing-summary-item {{
      display: inline-flex;
      align-items: center;
      gap: 5px;
      min-width: 0;
    }}
    .channels-app-routing-summary-label {{
      font-weight: 600;
      color: inherit;
    }}
    .channels-app-routing-summary-mode {{
      font-size: 10px;
      opacity: 0.82;
      color: inherit;
      white-space: nowrap;
    }}
    .channels-app-routing-summary-badge,
    .channels-app-route-effective-badge {{
      vertical-align: middle;
    }}
    .channels-app-routing-copy {{
      color: #466656;
      font-size: 10px;
      line-height: 1.4;
    }}
    .channels-app-routing-grid {{
      display: flex;
      flex-direction: column;
      gap: 6px;
    }}
    .channels-app-route-placeholder {{
      color: #597767;
      font-size: 11px;
      line-height: 1.4;
    }}
    .channels-app-route-row {{
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(120px, 160px) minmax(120px, 160px) minmax(0, 1fr);
      gap: 8px;
      align-items: end;
      border: 1px solid #dce9d9;
      border-radius: 10px;
      background: #f8fcf8;
      padding: 6px 8px;
    }}
    .channels-app-route-meta {{
      display: flex;
      flex-direction: column;
      gap: 1px;
      min-width: 0;
    }}
    .channels-app-route-title {{
      font-size: 12px;
      font-weight: 700;
      color: #214233;
    }}
    .channels-app-route-copy {{
      color: #597767;
      font-size: 10px;
      line-height: 1.35;
      display: none;
    }}
    .channels-app-route-field {{
      margin: 0;
      min-width: 0;
    }}
    .channels-app-route-field .settings-label {{
      font-size: 10px;
    }}
    .channels-app-route-field .settings-select {{
      min-width: 0;
    }}
    .channels-app-route-effective {{
      display: flex;
      flex-direction: column;
      gap: 1px;
      min-width: 0;
      align-self: stretch;
      justify-content: center;
    }}
    .channels-app-route-effective-label {{
      font-size: 10px;
      color: #567061;
      text-transform: uppercase;
      letter-spacing: 0.03em;
    }}
    .channels-app-route-effective-value {{
      font-size: 12px;
      font-weight: 700;
      color: #214233;
      display: inline-flex;
      align-items: center;
      gap: 4px;
      min-height: 16px;
    }}
    .channels-app-route-effective-copy {{
      font-size: 10px;
      color: #4b6858;
      line-height: 1.35;
    }}
    .channels-app-routing:not(.is-expanded) .channels-app-routing-copy {{
      display: none;
    }}
    .channels-app-routing:not(.is-expanded) .channels-app-routing-grid {{
      display: none;
    }}
    @media (max-width: 1120px) {{
      .channels-app-route-row {{
        grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
      }}
      .channels-app-route-effective {{
        grid-column: 1 / -1;
      }}
    }}
    #settings-channels-table {{
      min-width: 1320px;
    }}
    .channels-action-placeholder {{
      color: #597767;
      font-size: 11px;
      white-space: nowrap;
    }}
    .channels-local-cell {{
      display: flex;
      flex-direction: column;
      gap: 1px;
      min-width: 0;
      line-height: 1.2;
    }}
    .channels-local-primary {{
      color: inherit;
      font-weight: 600;
      white-space: normal;
      overflow-wrap: anywhere;
      word-break: break-word;
    }}
    .channels-local-secondary {{
      color: inherit;
      opacity: 0.74;
      font-size: 10px;
      white-space: normal;
      overflow-wrap: anywhere;
      word-break: break-word;
    }}
    .channels-evidence-pill {{
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 999px;
      border: 1px solid #bfd4c3;
      background: #edf6ef;
      color: #204231;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.2px;
      line-height: 1.2;
      padding: 2px 7px;
      white-space: nowrap;
    }}
    .channels-evidence-pill.kind-observed-local {{
      border-color: #8eb89c;
      background: #e6f4ea;
      color: #23583c;
    }}
    .channels-evidence-pill.kind-observed-only {{
      border-color: #8db7d8;
      background: #e8f1f8;
      color: #2a5876;
    }}
    .channels-evidence-pill.kind-local-only {{
      border-color: #c8b37a;
      background: #f8f1df;
      color: #6d5721;
    }}
    .channels-evidence-pill.kind-disabled {{
      border-color: #d5aaaa;
      background: #faecec;
      color: #7a2f2f;
    }}
    .environment {{
      grid-column: 3;
      grid-row: 8;
      display: flex;
      flex-direction: column;
      min-height: 0;
    }}
    .environment .body {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      overflow-x: hidden;
      overflow-y: auto;
    }}
    .raw {{ grid-column: 3; grid-row: 8; }}
    .console {{ grid-column: 1 / span 3; grid-row: 10; }}
    .layout:not(.view-environment) .environment {{
      display: none !important;
    }}
    .splitter {{
      grid-column: 2;
      position: relative;
      border-radius: 8px;
      border: 1px solid var(--surface-tint-border);
      background: linear-gradient(180deg, var(--surface-tint-bg-soft), var(--surface-tint-bg-alt));
      cursor: col-resize;
      touch-action: none;
      user-select: none;
      min-width: 6px;
    }}
    .splitter[data-row="8"] {{ grid-row: 8; }}
    .splitter::before {{
      content: "";
      position: absolute;
      left: 50%;
      top: 8px;
      bottom: 8px;
      width: 2px;
      transform: translateX(-50%);
      background: var(--surface-tint-border-strong);
      border-radius: 1px;
    }}
    .splitter:hover::before, .splitter.active::before {{
      background: var(--accent);
    }}
    .hsplitter {{
      grid-column: 1 / span 3;
      position: relative;
      border-radius: 8px;
      border: 1px solid var(--surface-tint-border);
      background: linear-gradient(90deg, var(--surface-tint-bg-soft), var(--surface-tint-bg-alt));
      cursor: row-resize;
      touch-action: none;
      user-select: none;
      min-height: 6px;
    }}
    .hsplitter[data-target="top"] {{ grid-row: 3; }}
    .hsplitter[data-target="mid"] {{ grid-row: 5; }}
    .hsplitter[data-target="low"] {{ grid-row: 9; }}
    .hsplitter::before {{
      content: "";
      position: absolute;
      left: 8px;
      right: 8px;
      top: 50%;
      height: 2px;
      transform: translateY(-50%);
      background: var(--surface-tint-border-strong);
      border-radius: 1px;
    }}
    .hsplitter:hover::before, .hsplitter.active::before {{
      background: var(--accent);
    }}
    body.resizing-panels-x, body.resizing-panels-x * {{
      cursor: col-resize !important;
      user-select: none !important;
    }}
    body.resizing-panels-y, body.resizing-panels-y * {{
      cursor: row-resize !important;
      user-select: none !important;
    }}
    .layout .card.chatlabs {{
      display: none;
    }}
    .layout.view-chatlabs {{
      height: 100%;
      min-height: 0;
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-chatlabs > :not(.chatlabs) {{
      display: none !important;
    }}
    .layout.view-chatlabs .chatlabs {{
      grid-column: 1;
      grid-row: 1;
      display: flex;
      flex-direction: column;
      min-height: 0;
      background: #0b1411;
      border-color: #24513c;
      box-shadow: inset 0 0 0 1px rgba(56, 126, 93, 0.25);
    }}
    .layout.view-chatlabs .chatlabs .chatlabs-head {{
      display: flex;
      flex-wrap: wrap;
      align-items: baseline;
      justify-content: space-between;
      gap: 10px;
      padding: 12px 14px 10px;
      border-bottom: 1px solid #1f3f31;
      background: linear-gradient(180deg, #0f1d17 0%, #0b1411 100%);
    }}
    .layout.view-chatlabs .chatlabs .chatlabs-head h2 {{
      margin: 0;
      font-size: 1rem;
      letter-spacing: 0.02em;
      color: #d9ffe9;
    }}
    .layout.view-chatlabs .chatlabs .chatlabs-caption {{
      display: flex;
      align-items: center;
      gap: 8px;
      margin: 0;
      font-size: 0.8rem;
      color: #93c8ad;
    }}
    .layout.view-chatlabs .chatlabs .chatlabs-filter-row {{
      flex: 1 1 100%;
      width: 100%;
      margin-top: 8px;
    }}
    .layout.view-chatlabs .chatlabs .chatlabs-channel-pill-strip {{
      width: 100%;
      max-width: none;
      gap: 7px;
    }}
    .layout.view-chatlabs .chatlabs .chatlabs-count {{
      display: inline-flex;
      align-items: center;
      min-height: 20px;
      padding: 0 8px;
      border-radius: 999px;
      border: 1px solid #2d654b;
      background: rgba(45, 101, 75, 0.35);
      color: #b8f7d4;
      font-size: 0.72rem;
      letter-spacing: 0.02em;
      text-transform: uppercase;
    }}
    .layout.view-chatlabs .chatlabs .body {{
      padding: 10px;
      min-height: 0;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      background: radial-gradient(circle at 15% 0%, rgba(45, 100, 73, 0.2), transparent 58%);
    }}
    .layout.view-chatlabs .chatlabs-terminal {{
      position: relative;
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
      border-radius: 8px;
      border: 1px solid #285741;
      background: #07100d;
      box-shadow: inset 0 0 0 1px rgba(67, 152, 111, 0.17);
      font-family: "Segoe UI", "Inter", "Segoe UI Emoji", "Apple Color Emoji", "Noto Color Emoji", sans-serif;
      scrollbar-width: thin;
    }}
    .layout.view-chatlabs .chatlabs-feed {{
      display: flex;
      flex-direction: column;
      gap: 2px;
      min-height: 100%;
      width: 100%;
      margin: 0;
      padding: 8px 8px 12px;
    }}
    .layout.view-chatlabs .chatlabs-message-row {{
      display: flex;
      align-items: flex-start;
      gap: 0;
      width: 100%;
      max-width: 100%;
    }}
    .layout.view-chatlabs .chatlabs-message-row.is-continuation {{
      margin-top: -1px;
    }}
    .layout.view-chatlabs .chatlabs-message-row.is-self {{
      justify-content: flex-end;
    }}
    .layout.view-chatlabs .chatlabs-message-main {{
      display: flex;
      flex-direction: column;
      gap: 1px;
      max-width: min(88%, 940px);
      min-width: 0;
    }}
    .layout.view-chatlabs .chatlabs-message-row.is-self .chatlabs-message-main {{
      align-items: flex-end;
    }}
    .layout.view-chatlabs .chatlabs-meta {{
      display: inline-flex;
      align-items: center;
      flex-wrap: nowrap;
      gap: 6px;
      font-family: "Segoe UI", "Inter", "Helvetica Neue", Arial, "Noto Sans", sans-serif;
      font-size: 0.72rem;
      line-height: 1.2;
      color: #9dc7b4;
      margin-bottom: 1px;
      max-width: 100%;
      min-width: 0;
      white-space: nowrap;
      overflow: hidden;
    }}
    .layout.view-chatlabs .chatlabs-meta.chatlabs-meta-compact {{
      opacity: 0.9;
    }}
    .layout.view-chatlabs .chatlabs-meta-name {{
      flex: 1 1 auto;
      min-width: 0;
      font-size: 0.84rem;
      font-weight: 700;
      color: #d7ffe9;
      letter-spacing: 0;
      max-width: 30ch;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }}
    .layout.view-chatlabs .chatlabs-message-row.is-self .chatlabs-meta-name {{
      color: #d8eeff;
    }}
    .layout.view-chatlabs .chatlabs-meta-status {{
      flex: 0 0 auto;
      border: 1px solid rgba(113, 163, 139, 0.52);
      border-radius: 999px;
      padding: 0 6px;
      min-height: 16px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 0.62rem;
      font-weight: 600;
      letter-spacing: 0.01em;
      background: rgba(28, 59, 44, 0.86);
      color: #bcebd2;
    }}
    .layout.view-chatlabs .chatlabs-meta-status.status-online {{
      border-color: rgba(99, 172, 133, 0.58);
      background: rgba(21, 61, 40, 0.86);
      color: #c8f7dc;
    }}
    .layout.view-chatlabs .chatlabs-meta-status.status-warn {{
      border-color: rgba(184, 157, 96, 0.58);
      background: rgba(71, 58, 27, 0.86);
      color: #ffe6ae;
    }}
    .layout.view-chatlabs .chatlabs-meta-status.status-stale {{
      border-color: rgba(189, 103, 103, 0.58);
      background: rgba(74, 33, 33, 0.86);
      color: #ffd0d0;
    }}
    .layout.view-chatlabs .chatlabs-meta-status.status-unknown {{
      border-color: rgba(114, 132, 122, 0.58);
      background: rgba(33, 44, 39, 0.86);
      color: #d6e5dd;
    }}
    .layout.view-chatlabs .chatlabs-meta-route {{
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border: 1px solid rgba(116, 170, 143, 0.56);
      border-radius: 999px;
      padding: 0 6px;
      min-height: 16px;
      font-size: 0.62rem;
      font-weight: 600;
      letter-spacing: 0.01em;
      text-transform: uppercase;
      background: rgba(26, 56, 42, 0.86);
      color: #bcebd2;
    }}
    .layout.view-chatlabs .chatlabs-meta-time {{
      flex: 0 0 auto;
      color: #8fb6a5;
      font-size: 0.67rem;
      font-weight: 500;
      opacity: 0.95;
    }}
    .layout.view-chatlabs .chatlabs-react-meta {{
      margin-left: 4px;
      min-width: 16px;
      min-height: 16px;
      height: 16px;
      padding: 0 4px;
      border-radius: 999px;
      border: 1px solid rgba(97, 148, 122, 0.42);
      background: rgba(20, 42, 32, 0.9);
      color: #c8f5de;
      font-size: 0.58rem;
      line-height: 1;
      opacity: 0.65;
    }}
    .layout.view-chatlabs .chatlabs-message-row:hover .chatlabs-react-meta,
    .layout.view-chatlabs .chatlabs-message-row:focus-within .chatlabs-react-meta {{
      opacity: 1;
    }}
    .layout.view-chatlabs .chatlabs-message-row.is-continuation .chatlabs-react-meta {{
      opacity: 0.45;
    }}
    .layout.view-chatlabs .chatlabs-react-meta:hover {{
      border-color: rgba(146, 210, 175, 0.6);
      background: rgba(32, 67, 50, 0.95);
      color: #f5fffb;
    }}
    .layout.view-chatlabs .chatlabs-react-meta.has-reactions {{
      border-color: rgba(168, 146, 92, 0.58);
      background: rgba(59, 46, 19, 0.86);
      color: #ffe5a9;
      opacity: 0.9;
    }}
    .layout.view-chatlabs .chatlabs-react-meta.has-reactions:hover {{
      border-color: rgba(214, 180, 103, 0.72);
      background: rgba(80, 62, 25, 0.92);
      color: #ffefc8;
    }}
    .layout.view-chatlabs .chatlabs-bubble {{
      display: inline-block;
      font-family: "Helvetica Neue", Arial, "Noto Sans", sans-serif !important;
      letter-spacing: 0 !important;
      word-spacing: normal !important;
      font-kerning: normal;
      font-variant-ligatures: normal;
      font-feature-settings: normal;
      font-variant-numeric: normal;
      text-align: left;
      color: #ddfff0;
      font-size: 0.86rem;
      line-height: 1.28;
      white-space: normal !important;
      word-break: break-word;
      border: 1px solid rgba(62, 130, 97, 0.48);
      border-radius: 14px 14px 14px 6px;
      background: linear-gradient(180deg, rgba(15, 44, 33, 0.94), rgba(10, 28, 22, 0.94));
      padding: 5px 9px;
      box-shadow: inset 0 0 0 1px rgba(104, 184, 146, 0.12), 0 1px 5px rgba(0, 0, 0, 0.2);
    }}
    .layout.view-chatlabs .chatlabs-message-row.is-self .chatlabs-bubble {{
      border-color: rgba(86, 154, 199, 0.5);
      border-radius: 14px 14px 6px 14px;
      background: linear-gradient(180deg, rgba(30, 58, 83, 0.92), rgba(21, 43, 62, 0.92));
      box-shadow: inset 0 0 0 1px rgba(132, 186, 225, 0.16), 0 3px 10px rgba(0, 0, 0, 0.22);
    }}
    .layout.view-chatlabs .chatlabs-bubble.is-reaction {{
      border-color: rgba(181, 150, 84, 0.5);
      color: #ffeab7;
      background: linear-gradient(180deg, rgba(59, 46, 21, 0.9), rgba(38, 30, 14, 0.9));
    }}
    .layout.view-chatlabs .chatlabs-bubble.is-emoji-only {{
      display: inline-flex;
      align-items: center;
      min-width: 0;
      font-size: 1.8rem;
      line-height: 1.25;
      padding: 8px 12px;
      letter-spacing: 0.01em;
    }}
    .layout.view-chatlabs .chatlabs-bubble .chatlabs-bubble-text {{
      display: block;
      text-align: left !important;
      text-justify: auto !important;
      white-space: normal !important;
      letter-spacing: 0 !important;
      word-spacing: normal !important;
      font-family: "Helvetica Neue", Arial, "Noto Sans", sans-serif !important;
      unicode-bidi: plaintext;
    }}
    .layout.view-chatlabs .chatlabs-inline-header {{
      display: inline-flex;
      align-items: center;
      gap: 4px;
      max-width: 100%;
      min-width: 0;
      white-space: nowrap;
      vertical-align: baseline;
    }}
    .layout.view-chatlabs .chatlabs-inline-name {{
      display: inline-block;
      max-width: 30ch;
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-weight: 700;
      color: #d7ffe9;
      letter-spacing: 0;
    }}
    .layout.view-chatlabs .chatlabs-message-row.is-self .chatlabs-inline-name {{
      color: #d8eeff;
    }}
    .layout.view-chatlabs .chatlabs-inline-time {{
      color: #8fb6a5;
      font-size: 0.67rem;
      font-weight: 500;
      opacity: 0.95;
      white-space: nowrap;
    }}
    .layout.view-chatlabs .chatlabs-inline-sep {{
      color: #9bc3b2;
      opacity: 0.88;
      padding: 0 2px 0 1px;
      font-weight: 600;
    }}
    .layout.view-chatlabs .chatlabs-inline-body {{
      display: inline;
    }}
    .layout.view-chatlabs .chatlabs-bubble a {{
      color: #9edcff;
      text-decoration-color: rgba(158, 220, 255, 0.4);
    }}
    .layout.view-chatlabs .chatlabs-message-tools {{
      display: inline-flex;
      align-items: center;
      gap: 4px;
      min-height: 0;
      margin-top: 0;
    }}
    .layout.view-chatlabs .chatlabs-message-row.is-self .chatlabs-message-tools {{
      justify-content: flex-end;
    }}
    .layout.view-chatlabs .chatlabs-message-tools .chat-reaction-summary {{
      border-color: rgba(106, 160, 132, 0.44);
      background: rgba(20, 46, 34, 0.9);
      color: #d2ffea;
    }}
    .layout.view-chatlabs .chatlabs-message-tools .chat-reaction-summary:hover {{
      border-color: rgba(139, 204, 171, 0.54);
      background: rgba(29, 61, 46, 0.95);
    }}
    .layout.view-chatlabs .chatlabs-empty {{
      color: #7ea994;
      font-size: 0.82rem;
      padding: 6px 2px;
    }}
    .layout.view-chatlabs .chatlabs-line {{
      color: #d8fbe9;
      font-size: 0.82rem;
      line-height: 1.45;
      white-space: pre-wrap;
      word-break: break-word;
    }}
    .workspace-main > .layout.view-chat,
    .workspace-main > .layout.view-console,
    .workspace-main > .layout.view-settings {{
      grid-row: 1 / -1;
    }}
    .layout.view-chat {{
      height: 100%;
      min-height: 0;
      grid-template-rows: minmax(0, 1fr);
      background: transparent;
      overflow: visible;
    }}
    .workspace-shell[data-layout-view="chat"] .workspace-main {{
      background: transparent;
      overflow: visible;
    }}
    .layout.view-chat .chat {{
      grid-row: 1;
      display: flex;
      flex-direction: column;
      gap: 8px;
      min-height: 0;
      background: transparent;
      border: 0;
      box-shadow: none;
      overflow: visible;
    }}
    .layout.view-chat .chat .body {{
      display: flex;
      flex: 1 1 auto;
      flex-direction: column;
      min-height: 0;
      background: transparent;
      padding: 0;
      overflow: visible;
    }}
    .layout.view-chat .chat-left-panel {{
      background: transparent;
      border: 0;
      box-shadow: none;
      overflow: visible;
      gap: 8px;
    }}
    .layout.view-chat .chat-left-section.chat-users-section {{
      padding: 0;
      border: 0;
      background: transparent;
      gap: 8px;
    }}
    .workspace-shell[data-layout-view="chat"] .chat-users-head {{
      border: 0;
      border-radius: 0;
      background: transparent;
      padding: 0;
      min-height: 27px;
      align-items: center;
      width: 100%;
    }}
    .workspace-shell[data-layout-view="chat"] .chat-users-head-launcher-shell {{
      display: flex;
      width: 100%;
      min-width: 0;
    }}
    .workspace-shell[data-layout-view="chat"] .chat-users-head-launcher-shell .topbar-view-menu-btn {{
      flex: 1 1 auto;
      min-height: 27px;
      height: 27px;
      transition: none;
    }}
    .workspace-shell[data-layout-view="chat"] .chat-users-head-launcher-shell .topbar-view-menu-btn:hover,
    .workspace-shell[data-layout-view="chat"] .chat-users-head-launcher-shell .topbar-view-menu-btn[aria-expanded="true"] {{
      border-color: color-mix(in srgb, var(--line) 74%, var(--accent));
      background:
        linear-gradient(
          180deg,
          color-mix(in srgb, var(--panel) 94%, var(--accent) 6%),
          color-mix(in srgb, var(--panel) 84%, var(--accent) 16%)
        );
      box-shadow:
        inset 0 1px 0 rgba(255, 255, 255, 0.4),
        0 8px 18px color-mix(in srgb, var(--accent-2) 14%, transparent);
    }}
    .workspace-shell[data-layout-view="chat"] .chat-users-head-action-btn {{
      min-width: 28px;
      width: 28px;
      height: 28px;
    }}
    .layout.view-chat .chat-card-head.workspace-chrome-bar {{
      flex: 0 0 auto;
      margin: 0;
      box-sizing: border-box;
    }}
    .layout.view-chat .chat-card-head.workspace-chrome-bar .chat-card-head-controls {{
      min-height: 0;
      align-items: center;
    }}
    .layout.view-chat .chat-card-head.workspace-chrome-bar .workspace-chrome-row {{
      flex-wrap: nowrap;
      gap: 8px;
      align-items: center;
    }}
    .layout.view-chat .chat-card-head .chat-card-head-actions,
    .layout.view-chat .chat-card-head .chat-mesh-channel-wrap,
    .layout.view-chat .chat-card-head .mesh-channel-wrap {{
      min-width: 0;
      max-width: 100%;
    }}
    .layout.view-chat .chat-card-head .chat-card-head-actions {{
      flex: 1 1 auto;
      width: 100%;
      display: flex;
      justify-content: flex-start;
      align-items: center;
      position: relative;
      gap: 6px;
      padding-right: 0;
      min-width: 0;
      box-sizing: border-box;
    }}
    .layout.view-chat .chat-card-head .chat-card-head-actions::before {{
      content: none;
      display: none;
    }}
    .layout.view-chat .chat-card-head .chat-mesh-channel-wrap {{
      flex: 0 1 auto;
      width: auto;
      max-width: 100%;
      border: 0;
      border-radius: 0;
      background: transparent;
      padding: 0;
      min-height: 27px;
      display: flex;
      align-items: center;
      gap: 6px;
      box-sizing: border-box;
      color: inherit;
      box-shadow: none;
    }}
    .layout.view-chat .chat-card-head .mesh-channel-pill-strip {{
      flex: 0 1 auto;
      width: auto;
      min-width: 0;
      max-width: 100%;
      min-height: 27px;
      align-items: center;
      justify-content: flex-start;
      flex-wrap: nowrap;
      overflow-x: auto;
      overflow-y: hidden;
      scrollbar-width: none;
    }}
    .layout.view-chat .chat-card-head .mesh-channel-pill {{
      min-height: 27px;
    }}
    .layout.view-chat .chat-card-head .mesh-channel-pill:hover,
    .layout.view-chat .chat-card-head .mesh-channel-pill.active {{
      border-color: #c9dccf;
      background: rgba(248, 255, 251, 0.12);
      box-shadow: none;
    }}
    .workspace-shell[data-layout-view="chat"] .chat-left-panel .chat-member-list {{
      border: 0;
      border-radius: 0;
      background: transparent;
      overflow: auto;
    }}
    .layout.view-chat .chat-shell {{
      display: flex;
      flex: 1 1 auto;
      flex-direction: column;
      min-height: 0;
      padding: 0;
      margin-top: 0;
      overflow: visible;
    }}
    .layout.view-chat .chat-compose-notices:not([hidden]) + .chat-shell {{
      margin-top: 0;
    }}
    .layout.view-chat .chat-compose-notices {{
      padding: 0 0 6px 0;
    }}
    .layout.view-chat .chat-main-pane {{
      display: flex;
      flex: 1 1 auto;
      flex-direction: column;
      min-height: 0;
      row-gap: 8px;
    }}
    .layout.view-chat .chat-log-scroll {{
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
    }}
    .layout.view-chat .chat-compose-shell {{
      flex: 0 0 auto;
      margin-top: 0;
    }}
    @media (max-width: 760px) {{
      .layout.view-chat .chat-card-head.workspace-chrome-bar {{
        padding: 8px 8px 6px 8px;
      }}
      .layout.view-chat .chat-card-head .chat-card-head-actions {{
        --chat-header-compose-trailing-space: 0px;
        flex-wrap: wrap;
        align-items: stretch;
        gap: 6px;
        padding-right: 0;
      }}
      .layout.view-chat .chat-card-head .chat-mesh-channel-wrap {{
        flex: 1 1 100%;
        width: 100%;
      }}
      .layout.view-chat .chat-card-head .mesh-channel-pill-strip {{
        flex: 1 1 100%;
        width: 100%;
        gap: 5px;
        padding-bottom: 2px;
        scroll-snap-type: x proximity;
        -webkit-overflow-scrolling: touch;
      }}
      .layout.view-chat .chat-card-head .mesh-channel-pill {{
        scroll-snap-align: start;
      }}
      .layout.view-chat .chat-compose-shell {{
        padding: 6px;
      }}
      .chat-feed-item {{
        max-width: min(92%, 100%);
        padding: 7px 8px;
      }}
      .chat-feed-line {{
        flex-direction: column;
        gap: 4px;
      }}
      .chat-feed-summary {{
        width: 100%;
      }}
      .chat-feed-side {{
        width: 100%;
        margin-left: 0;
        justify-content: flex-end;
        flex-wrap: wrap;
        gap: 6px;
      }}
      .chat-feed-time {{
        margin-left: auto;
      }}
      .chat-reply-inline {{
        flex-wrap: wrap;
        gap: 4px;
      }}
      .chat-reply-inline .chat-reply-inline-label,
      .chat-reply-inline .chat-reply-inline-text,
      .chat-reply-inline .chat-reply-inline-meta {{
        white-space: normal;
      }}
      .chat-reaction-row {{
        gap: 6px;
      }}
    }}
    .layout.view-chat .map,
    .layout.view-chat .map-data,
    .layout.view-chat .summary,
    .layout.view-chat .nodes,
    .layout.view-chat .bots,
    .layout.view-chat .packets,
    .layout.view-chat .raw,
    .layout.view-chat .console,
    .layout.view-chat .splitter,
    .layout.view-chat .hsplitter {{
      display: none !important;
    }}

    .layout.view-macros {{
      height: 100%;
      min-height: 0;
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-macros .chat {{
      grid-row: 1;
      min-height: 0;
    }}
    .layout.view-macros .map,
    .layout.view-macros .map-data,
    .layout.view-macros .summary,
    .layout.view-macros .nodes,
    .layout.view-macros .bots,
    .layout.view-macros .packets,
    .layout.view-macros .raw,
    .layout.view-macros .console,
    .layout.view-macros .splitter,
    .layout.view-macros .hsplitter {{
      display: none !important;
    }}
    .layout.view-macros .card.chat {{
      background: #191c22 !important;
      border-color: #3a3f48 !important;
      box-shadow: none !important;
      display: flex;
      flex-direction: column;
      min-height: 0;
      overflow: hidden;
    }}
    .layout.view-macros .card.chat .chat-card-head {{
      display: none !important;
    }}
    .layout.view-macros .card.chat .body {{
      display: grid;
      grid-template-columns: minmax(360px, var(--macros-panel-width, 460px)) 12px minmax(280px, 1fr);
      align-items: stretch;
      justify-content: stretch;
      padding: 6px;
      background: #191c22 !important;
      column-gap: 0;
      row-gap: 0;
      min-height: 0;
      flex: 1 1 auto;
      overflow: hidden;
    }}
    .macros-panel-splitter {{
      display: none;
    }}
    .macros-right-pane {{
      display: none;
    }}
    .layout.view-macros .card.chat .chat-shell {{
      position: relative;
      width: auto;
      min-width: 0;
      max-width: none;
      height: auto;
      min-height: 0;
      border: 1px solid #444;
      border-radius: 8px;
      background: #222 !important;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      box-sizing: border-box;
    }}
    .layout.view-macros .card.chat .macros-panel-splitter {{
      display: block;
      position: relative;
      width: 12px;
      min-width: 12px;
      max-width: 12px;
      height: auto;
      min-height: 0;
      align-self: stretch;
      cursor: col-resize;
      touch-action: none;
      user-select: none;
      z-index: 2;
    }}
    .layout.view-macros .card.chat .macros-panel-splitter::before {{
      content: "";
      position: absolute;
      left: 5px;
      top: 0;
      bottom: 0;
      width: 2px;
      background: #596276;
      border-radius: 1px;
      opacity: 0.9;
    }}
    .layout.view-macros .card.chat .macros-panel-splitter::after {{
      content: "";
      position: absolute;
      left: 1px;
      top: 50%;
      width: 9px;
      height: 56px;
      transform: translateY(-50%);
      border-radius: 999px;
      border: 1px solid #6e778b;
      background: rgba(118, 130, 152, 0.26);
      box-shadow: inset 0 0 0 1px rgba(11, 16, 24, 0.22);
    }}
    .layout.view-macros .card.chat .macros-panel-splitter:hover::before,
    .layout.view-macros .card.chat .macros-panel-splitter.active::before {{
      background: #4ab0ff;
      opacity: 1;
    }}
    .layout.view-macros .card.chat .macros-panel-splitter:hover::after,
    .layout.view-macros .card.chat .macros-panel-splitter.active::after {{
      border-color: #4ab0ff;
      background: rgba(74, 176, 255, 0.28);
    }}
    .layout.view-macros .card.chat .macros-right-pane {{
      display: block;
      min-width: 0;
      min-height: 0;
      height: auto;
      border: 1px solid #343a46;
      border-radius: 8px;
      background: linear-gradient(180deg, #1a1f28, #141a24);
      overflow: hidden;
      box-sizing: border-box;
    }}
    .layout.view-macros .card.chat .macros-right-pane-inner {{
      height: 100%;
      min-height: 0;
      padding: 14px;
      color: #90a4c0;
      font-size: 12px;
      line-height: 1.45;
      display: flex;
      flex-direction: column;
      gap: 10px;
      overflow: auto;
    }}
    .layout.view-macros .card.chat .macros-right-pane-inner h3 {{
      margin: 0 0 6px;
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 0.3px;
      color: #d7e2f3;
    }}
    .layout.view-macros .card.chat .macros-right-pane-inner p {{
      margin: 0;
      color: #8ea2bc;
    }}
    .layout.view-macros .card.chat .macros-studio-head {{
      display: flex;
      flex-direction: column;
      gap: 3px;
    }}
    .layout.view-macros .card.chat .macros-studio-head p {{
      font-size: 11px;
      line-height: 1.35;
      color: #8ea2bc;
    }}
    .layout.view-macros .card.chat .macros-left-view-toggle {{
      display: inline-flex;
      align-items: center;
      gap: 6px;
      flex-wrap: wrap;
    }}
    .layout.view-macros .card.chat .macros-left-view-toggle .btn {{
      min-height: 26px;
      padding: 4px 10px;
      font-size: 11px;
      line-height: 1.1;
    }}
    .layout.view-macros .card.chat .macros-left-view-toggle .btn.is-active {{
      border-color: #4ab0ff;
      color: #dff1ff;
      box-shadow: inset 0 0 0 1px rgba(74, 176, 255, 0.35);
      background: linear-gradient(180deg, rgba(27, 52, 79, 0.9), rgba(18, 35, 56, 0.9));
    }}
    .layout.view-macros .card.chat .macros-send-strip {{
      display: inline-flex;
      align-items: center;
      gap: 8px;
      flex-wrap: wrap;
    }}
    .layout.view-macros .card.chat .macros-send-primary {{
      border-color: #8c2b2b !important;
      background: linear-gradient(180deg, #5d1b1b, #451414) !important;
      color: #ffdcdc !important;
      font-weight: 700;
      letter-spacing: 0.2px;
      box-shadow: inset 0 0 0 1px rgba(255, 147, 147, 0.25);
    }}
    .layout.view-macros .card.chat .macros-send-primary:hover {{
      background: linear-gradient(180deg, #702121, #531919) !important;
    }}
    .layout.view-macros .card.chat .macros-send-strip-note {{
      font-size: 10px;
      line-height: 1.25;
      color: #f0a7ad;
      max-width: 360px;
    }}
    .layout.view-macros .card.chat .macros-studio-grid {{
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 8px;
      min-height: 0;
      flex: 0 0 auto;
    }}
    .layout.view-macros .card.chat .macros-tool-card,
    .layout.view-macros .card.chat .macros-output-card {{
      border: 1px solid #3a4250;
      border-radius: 8px;
      background: linear-gradient(180deg, rgba(24, 31, 43, 0.98), rgba(18, 25, 36, 0.98));
      padding: 9px;
      display: flex;
      flex-direction: column;
      gap: 7px;
      min-height: 0;
    }}
    .layout.view-macros .card.chat .macros-tool-card h4,
    .layout.view-macros .card.chat .macros-output-card h4 {{
      margin: 0;
      font-size: 11px;
      line-height: 1.2;
      letter-spacing: 0.25px;
      text-transform: uppercase;
      color: #d3dfef;
      font-weight: 700;
    }}
    .layout.view-macros .card.chat .macros-tool-row {{
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 7px;
      align-items: end;
    }}
    .layout.view-macros .card.chat .macros-tool-field {{
      display: flex;
      flex-direction: column;
      gap: 4px;
      min-width: 0;
      font-size: 10px;
      color: #9eb0c8;
      text-transform: uppercase;
      letter-spacing: 0.18px;
    }}
    .layout.view-macros .card.chat .macros-tool-field input,
    .layout.view-macros .card.chat .macros-tool-field select {{
      width: 100%;
      min-width: 0;
      border: 1px solid #4b5668;
      border-radius: 6px;
      background: #121923;
      color: #deebff;
      padding: 5px 7px;
      font-size: 11px;
      line-height: 1.2;
      box-sizing: border-box;
    }}
    .layout.view-macros .card.chat .macros-tool-field input[type="file"] {{
      padding: 4px 6px;
      font-size: 10px;
      color: #9eb6d7;
    }}
    .layout.view-macros .card.chat .macros-tool-check {{
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-size: 11px;
      color: #a9bad1;
    }}
    .layout.view-macros .card.chat .macros-tool-check input {{
      margin: 0;
    }}
    .layout.view-macros .card.chat .macros-tool-actions {{
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      align-items: center;
    }}
    .layout.view-macros .card.chat .macros-mini-meta {{
      font-size: 10px;
      color: #8ea4c2;
      line-height: 1.25;
    }}
    .layout.view-macros .card.chat .macros-output-card {{
      flex: 1 1 auto;
      overflow: hidden;
    }}
    .layout.view-macros .card.chat .macros-output-head {{
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
    }}
    .layout.view-macros .card.chat .macros-ascii-output {{
      width: 100%;
      flex: 1 1 auto;
      min-height: 120px;
      border: 1px solid #4b5668;
      border-radius: 6px;
      background: #0f1620;
      color: #e8f1ff;
      padding: 8px;
      font: 11px/1.2 ui-monospace, Menlo, Consolas, monospace;
      resize: vertical;
      box-sizing: border-box;
      white-space: pre;
      overflow: auto;
    }}
    .layout.view-macros .card.chat .macros-output-actions {{
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      align-items: center;
    }}
    .layout.view-macros .card.chat .macros-status {{
      min-height: 14px;
      font-size: 10px;
      line-height: 1.25;
      color: #9eb2cf;
      word-break: break-word;
    }}
    .layout.view-macros .card.chat .macros-status.is-error {{
      color: #f39ca4;
    }}
    .layout.view-macros .card.chat .chat-shell::before {{
      content: "Log";
      position: absolute;
      top: 0;
      left: 25%;
      transform: translateX(-50%);
      height: 24px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #ddd;
      font-size: 12px;
      line-height: 1;
      pointer-events: none;
      z-index: 2;
    }}
    .layout.view-macros .card.chat .chat-shell::after {{
      content: "MediumFast";
      position: absolute;
      top: 0;
      left: 62%;
      transform: translateX(-50%);
      height: 24px;
      display: inline-flex;
      align-items: center;
      color: #2f9cff;
      font-size: 12px;
      line-height: 1;
      pointer-events: none;
      z-index: 2;
    }}
    .layout.view-macros .card.chat .chat-main-pane {{
      row-gap: 0 !important;
      height: auto;
      padding-top: 24px;
      min-height: 0;
      overflow: hidden;
    }}
    .layout.view-macros .card.chat .chat-compose-shell {{
      display: none !important;
    }}
    .layout.view-macros .card.chat .chat-log-scroll {{
      background: #222 !important;
      border: 0 !important;
      border-top: 1px solid #2f9cff !important;
      border-radius: 0 !important;
      box-shadow: none !important;
      min-height: 0;
      overflow: auto;
    }}
    .layout.view-macros .card.chat .chat-feed {{
      background: #222 !important;
      font-family: ui-monospace, Menlo, Consolas, monospace !important;
      margin-top: 0 !important;
      padding: 6px !important;
    }}
    .layout.view-macros .card.chat .chat-feed-item-monitor-exact {{
      border: 0 !important;
      padding: 0 !important;
      background: transparent !important;
      box-shadow: none !important;
      color: #bbb !important;
    }}
    .layout.view-macros .card.chat .chat-feed[data-macros-feed-mode="preview"] .chat-feed-item:not(.chat-feed-item-monitor-exact),
    .layout.view-macros .card.chat .chat-feed[data-macros-feed-mode="preview"] .chat-feed-side,
    .layout.view-macros .card.chat .chat-feed[data-macros-feed-mode="preview"] .chat-reaction-summary-row,
    .layout.view-macros .card.chat .chat-feed[data-macros-feed-mode="preview"] .chat-reaction-row,
    .layout.view-macros .card.chat .chat-feed[data-macros-feed-mode="preview"] .chat-feed-colon,
    .layout.view-macros .card.chat .chat-feed[data-macros-feed-mode="preview"] .chat-feed-status-pill,
    .layout.view-macros .card.chat .chat-feed[data-macros-feed-mode="preview"] .chat-feed-new-pill,
    .layout.view-macros .card.chat .chat-feed[data-macros-feed-mode="preview"] .chat-feed-line,
    .layout.view-macros .card.chat .chat-feed[data-macros-feed-mode="preview"] .chat-feed-summary {{
      display: none !important;
    }}
    .layout.view-macros .card.chat .chat-feed-item-monitor-exact .chat-entry-msg {{
      display: block;
      font-family: ui-monospace, Menlo, Consolas, monospace !important;
      font-size: 12px !important;
      line-height: 1.35 !important;
      color: #bbb !important;
      white-space: normal !important;
      overflow-wrap: anywhere !important;
      margin: 0 !important;
    }}
    .layout.view-macros .card.chat .chat-feed-item-macro-preview .chat-entry-msg {{
      white-space: normal !important;
      line-height: 1.35 !important;
    }}
    .layout.view-macros .card.chat .chat-feed-item-macro-preview .chat-entry-copy {{
      white-space: pre !important;
    }}
    .layout.view-macros .card.chat .chat-monitor-prefix {{
      color: #bbb !important;
      font-weight: 700 !important;
      margin-right: 0 !important;
    }}
    .layout.view-macros .card.chat .chat-monitor-network-tag {{
      color: #bbb !important;
      margin-right: 2px !important;
      font-weight: 700 !important;
    }}
    .layout.view-macros .card.chat .short-name {{
      margin-right: 2px !important;
      padding: 0 2px !important;
      border-radius: 4px !important;
    }}
    .layout.view-macros .card.chat .chat-entry-reply {{
      color: #999 !important;
      font-style: italic !important;
      margin-right: 4px !important;
    }}
    .layout.view-macros .card.chat .chat-monitor-text {{
      color: #bbb !important;
    }}
    .layout.view-macros .card.chat .chat-link {{
      color: #9bd !important;
    }}
    @media (max-width: 1100px) {{
      .layout.view-macros .card.chat .body {{
        grid-template-columns: minmax(0, 1fr);
      }}
      .layout.view-macros .card.chat .chat-shell {{
        width: auto;
        max-width: none;
        min-width: 0;
        height: auto;
      }}
      .layout.view-macros .card.chat .macros-panel-splitter,
      .layout.view-macros .card.chat .macros-right-pane {{
        display: none !important;
      }}
      .layout.view-macros .card.chat .chat-shell::before {{
        left: 24%;
      }}
      .layout.view-macros .card.chat .chat-shell::after {{
        left: 62%;
      }}
    }}

    .layout.view-files {{
      height: 100%;
      min-height: 0;
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-files .files {{
      grid-column: 1;
      grid-row: 1;
      display: flex;
      height: 100%;
      min-height: 0;
      max-height: 100%;
      background: transparent;
      border: 0;
      box-shadow: none;
      overflow: visible;
    }}
    .layout.view-files .files .body {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      height: 100%;
      min-height: 0;
      overflow: hidden;
      background: transparent;
      padding: 0;
    }}
    .layout.view-files .chat,
    .layout.view-files .settings,
    .layout.view-files .bots,
    .layout.view-files .games,
    .layout.view-files .nodes,
    .layout.view-files .map,
    .layout.view-files .map-data,
    .layout.view-files .favorites,
    .layout.view-files .history-chat,
    .layout.view-files .channels-view,
    .layout.view-files .packets,
    .layout.view-files .encription,
    .layout.view-files .raw,
    .layout.view-files .console,
    .layout.view-files .summary,
    .layout.view-files .splitter,
    .layout.view-files .hsplitter[data-target="top"],
    .layout.view-files .hsplitter[data-target="mid"],
    .layout.view-files .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-network {{
      --network-pane-head-bg: transparent;
      --network-pane-body-bg: transparent;
      --network-pane-head-border: var(--surface-tint-border);
      --network-pane-divider-bg: transparent;
      --network-pane-divider-border: var(--surface-tint-border);
      --network-pane-divider-line: var(--surface-tint-divider-line, var(--surface-tint-border-strong));
      --network-pane-divider-line-active: var(--accent);
      height: var(--layout-available-height, 100%);
      max-height: 100%;
      min-height: 0;
      grid-template-columns: minmax(320px, var(--split-left-pct)) var(--splitter-size) minmax(320px, 1fr);
      grid-template-rows: minmax(0, 1fr);
      align-content: stretch;
      overflow: hidden;
    }}
    .layout.view-network .summary {{
      display: block;
      grid-column: 1 / span 3;
      grid-row: 1;
    }}
    .layout.view-network .nodes {{
      display: none;
    }}
    .layout.view-network .nodes h2 {{
      border-bottom-color: var(--network-pane-head-border);
      background: var(--network-pane-head-bg);
    }}
    .layout.view-network .map {{
      grid-column: 1 / span 3;
      grid-row: 1;
      height: 100%;
      min-height: 0;
      border-color: var(--network-pane-head-border);
    }}
    .layout.view-network .nodes .body {{
      min-height: 0;
      overflow: hidden;
      background: var(--network-pane-body-bg);
    }}
    .layout.view-network .network-node-stack {{
      flex: 1 1 auto;
      min-height: 0;
      gap: 0;
    }}
    .layout.view-network .nodes-table-scroll {{
      flex: 1 1 auto !important;
      min-height: 0;
      max-height: none !important;
      height: 100%;
    }}
    .layout.view-network .network-node-splitter {{
      display: none !important;
      margin: 0 !important;
      min-height: 0 !important;
      max-height: 0 !important;
      border: 0 !important;
      padding: 0 !important;
    }}
    .layout.view-network .network-node-history-host {{
      display: none !important;
      margin: 0 !important;
      min-height: 0 !important;
      max-height: 0 !important;
      border: 0 !important;
      padding: 0 !important;
    }}
    .layout.view-network .map .body {{
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: minmax(280px, 1fr);
      flex: 1 1 auto;
      height: auto;
      min-height: 0;
      background: transparent;
      padding: 0;
      box-sizing: border-box;
      overflow: hidden;
    }}
    .layout.view-network .map-card-head {{
      display: none;
    }}
    .layout.view-network .map-frame {{
      display: block;
      width: 100%;
      height: 100%;
      min-height: 0;
      border-color: var(--network-pane-head-border);
      border-top: 1px solid var(--network-pane-head-border);
      border-radius: 8px;
      background: transparent;
      padding: 1px;
      box-sizing: border-box;
    }}
    .layout.view-network .network-map-chrome {{
      display: flex;
    }}
    .layout.view-network #network-map-panel-overview > .network-overview-panel {{
      overflow: hidden;
      padding: 0;
    }}
    .layout.view-network #network-map-panel-overview .network-overview-card {{
      flex: 1 1 auto;
      min-height: 0;
      height: 100%;
      padding: 64px 18px 14px;
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
      backdrop-filter: none;
      box-sizing: border-box;
    }}
    .layout.view-network #network-map-panel-graph > .network-overview-panel {{
      overflow: hidden;
      padding: 0;
    }}
    .layout.view-network #network-map-panel-graph .network-graph-card {{
      flex: 1 1 auto;
      min-height: 0;
      height: 100%;
      padding: 0;
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
      backdrop-filter: none;
      box-sizing: border-box;
    }}
    .layout.view-network #network-map-panel-graph .network-graph-stage {{
      border: 0;
      border-radius: 0;
      box-shadow: none;
    }}
    .layout.view-network #network-map-panel-graph #network-graph-svg {{
      height: 100%;
      margin-top: 0;
      --network-graph-toolbar-safe-strip: 0px;
    }}
    .layout.view-network #network-map-panel-top10 > .network-overview-panel {{
      overflow: hidden;
      padding: 0;
    }}
    .layout.view-network #network-map-panel-top10 .network-top-nodes-card {{
      flex: 1 1 auto;
      min-height: 0;
      height: 100%;
      padding: 64px 18px 14px;
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
      backdrop-filter: none;
      box-sizing: border-box;
    }}
    .layout.view-network #network-map-panel-top10 .network-top-nodes-list {{
      overflow: auto;
      padding-right: 2px;
    }}
    .layout.view-network #network-map-panel-routes > .network-overview-panel {{
      overflow: hidden;
      padding: 0;
    }}
    .layout.view-network #network-map-panel-routes .network-routes-card {{
      flex: 1 1 auto;
      min-height: 0;
      height: 100%;
      padding: 56px 18px 14px;
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
      backdrop-filter: none;
      box-sizing: border-box;
    }}
    .layout.view-network #network-map-panel-routes .network-routes-result {{
      flex: 1 1 auto;
    }}
    .layout.view-network #network-map-panel-sensors > .network-overview-panel {{
      overflow: hidden;
      padding: 0;
    }}
    .layout.view-network #network-map-panel-sensors .network-sensors-host {{
      flex: 1 1 auto;
      min-height: 0;
    }}
    .layout.view-network #network-map-panel-sensors .env-metrics-explorer {{
      flex: 1 1 auto;
      min-height: 0;
      padding: 64px 18px 14px;
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
      backdrop-filter: none;
      box-sizing: border-box;
    }}
    .layout.view-network #network-map-panel-sensors #env-metrics-chart-wrap {{
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
    }}
    .layout.view-network #network-overview-chart-wrap {{
      flex: 1 1 auto;
      min-height: clamp(280px, 48vh, 460px);
      height: auto;
    }}
    .layout.view-network #network-map-panel-overview #network-overview-chart-wrap {{
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
    }}
    .layout.view-network #network-overview-overview {{
      display: none;
    }}
    .layout.view-network .map-data,
    .layout.view-network .chat,
    .layout.view-network .bots,
    .layout.view-network .packets,
    .layout.view-network .raw,
    .layout.view-network .console,
    .layout.view-network .hsplitter[data-target="top"],
    .layout.view-network .hsplitter[data-target="mid"],
    .layout.view-network .hsplitter[data-target="low"] {{
      display: none !important;
    }}
    .layout.view-network .splitter {{
      display: none !important;
    }}
    .layout.view-network .splitter::before {{
      background: var(--network-pane-divider-line);
    }}
    .layout.view-network .splitter:hover::before,
    .layout.view-network .splitter.active::before {{
      background: var(--network-pane-divider-line-active);
    }}

    .layout.view-history {{
      --history-pane-head-bg: var(--surface-tint-vignette), var(--surface-tint-bg-alt);
      --history-pane-body-bg: var(--surface-tint-vignette), var(--surface-tint-bg);
      --history-pane-head-border: var(--surface-tint-border);
      --history-pane-divider-bg: var(--surface-tint-divider-bg);
      --history-pane-divider-border: var(--surface-tint-border);
      --history-pane-divider-line: var(--surface-tint-divider-line, var(--surface-tint-border-strong));
      --history-pane-divider-line-active: var(--accent);
      height: 100%;
      max-height: 100%;
      min-height: 0;
      grid-template-columns: minmax(320px, var(--split-left-pct)) var(--splitter-size) minmax(320px, 1fr);
      grid-template-rows: auto minmax(0, 1fr);
      overflow: hidden;
    }}
    .layout.view-history .summary {{
      display: block;
      grid-column: 1 / span 3;
      grid-row: 1;
    }}
    .layout.view-history .map-data {{
      grid-column: 1;
      grid-row: 2;
      min-height: 0;
      border-color: var(--history-pane-head-border);
    }}
    .layout.view-history .map-data > h2 {{
      border-bottom-color: var(--history-pane-head-border);
      background: var(--history-pane-head-bg);
    }}
    .layout.view-history .map-data .body {{
      min-height: 0;
      background: var(--history-pane-body-bg);
      overflow-y: auto;
      overflow-x: hidden;
    }}
    .layout.view-history .history-activity-splitter {{
      display: block;
    }}
    .layout.view-history .history-activity-main-scroll {{
      flex: 0 0 var(--history-activity-top-pct);
    }}
    .layout.view-history .history-chat-splitter {{
      display: none;
    }}
    .layout.view-history .history-chat-main-scroll {{
      flex: 0 0 auto;
      overflow: visible;
    }}
    .layout.view-history .history-notification-section {{
      flex: 0 0 auto;
      min-height: 0;
    }}
    .layout.view-history .history-chat {{
      display: flex;
      grid-column: 3;
      grid-row: 2;
      min-height: 0;
      max-height: 100%;
      overflow: hidden;
      border-color: var(--history-pane-head-border);
    }}
    .layout.view-history .history-chat > h2 {{
      border-bottom-color: var(--history-pane-head-border);
      background: var(--history-pane-head-bg);
    }}
    .layout.view-history .history-chat .body {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      background: var(--history-pane-body-bg);
      overflow: hidden;
    }}
    .layout.view-history .history-chat-stack {{
      overflow-y: auto;
      overflow-x: hidden;
      padding-right: 2px;
    }}
    .layout.view-history .history-notification-list {{
      overflow: visible;
      padding-right: 0;
    }}
    .layout.view-history .splitter {{
      display: block;
      grid-row: 2;
      border-color: var(--history-pane-divider-border);
      background: var(--history-pane-divider-bg);
    }}
    .layout.view-history .splitter::before {{
      background: var(--history-pane-divider-line);
    }}
    .layout.view-history .splitter:hover::before,
    .layout.view-history .splitter.active::before {{
      background: var(--history-pane-divider-line-active);
    }}
    .layout.view-history .chat,
    .layout.view-history .bots,
    .layout.view-history .settings,
    .layout.view-history .nodes,
    .layout.view-history .map,
    .layout.view-history .favorites,
    .layout.view-history .packets,
    .layout.view-history .raw,
    .layout.view-history .console,
    .layout.view-history .hsplitter[data-target="top"],
    .layout.view-history .hsplitter[data-target="mid"],
    .layout.view-history .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-packets {{
      height: 100%;
      min-height: 0;
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-packets .packets {{
      grid-column: 1 / span 3;
      grid-row: 1;
      display: flex;
      flex-direction: column;
      min-height: 0;
    }}
    .layout.view-packets .packets .body {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
    }}
    .layout.view-packets .packets .body.scroll {{
      max-height: none;
      height: 100%;
    }}
    .layout.view-packets .chat,
    .layout.view-packets .bots,
    .layout.view-packets .nodes,
    .layout.view-packets .map,
    .layout.view-packets .map-data,
    .layout.view-packets .summary,
    .layout.view-packets .raw,
    .layout.view-packets .console,
    .layout.view-packets .splitter,
    .layout.view-packets .hsplitter[data-target="top"],
    .layout.view-packets .hsplitter[data-target="mid"],
    .layout.view-packets .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-channels {{
      height: 100%;
      min-height: 0;
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-channels .channels-view {{
      grid-column: 1 / span 3;
      grid-row: 1;
      display: flex;
      flex-direction: column;
      height: 100%;
      min-height: 0;
    }}
    .layout.view-channels .channels-view .body {{
      flex: 1 1 auto;
      height: 100%;
      min-height: 0;
    }}
    .layout.view-channels .channels-dashboard-grid {{
      flex: 1 1 auto;
      height: 100%;
      min-height: 0;
    }}
    .layout.view-channels .channels-dashboard-panel-activity {{
      height: 100%;
      min-height: 0;
    }}
    .layout.view-channels .channels-activity-table-wrap {{
      flex: 1 1 auto;
      height: 100%;
      min-height: 0;
    }}
    .layout.view-channels .chat,
    .layout.view-channels .bots,
    .layout.view-channels .nodes,
    .layout.view-channels .map,
    .layout.view-channels .map-data,
    .layout.view-channels .summary,
    .layout.view-channels .favorites,
    .layout.view-channels .packets,
    .layout.view-channels .encription,
    .layout.view-channels .raw,
    .layout.view-channels .console,
    .layout.view-channels .splitter,
    .layout.view-channels .hsplitter[data-target="top"],
    .layout.view-channels .hsplitter[data-target="mid"],
    .layout.view-channels .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-data {{
      height: 100%;
      min-height: 0;
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-data .raw {{
      grid-column: 1 / span 3;
      grid-row: 1;
      display: flex;
      flex-direction: column;
      min-height: 0;
    }}
    .layout.view-data .raw .body {{
      display: block;
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
    }}
    .layout.view-console {{
      height: 100%;
      max-height: 100%;
      min-height: 0;
      grid-template-columns: minmax(0, 1fr) !important;
      grid-template-rows: minmax(0, 1fr) !important;
      gap: 0;
      overflow: hidden;
    }}
    .layout.view-console .console {{
      grid-column: 1 / -1 !important;
      grid-row: 1 / -1 !important;
      display: flex;
      flex-direction: column;
      min-height: 0;
      height: 100%;
      max-height: 100%;
      overflow: hidden;
      align-self: stretch;
      justify-self: stretch;
    }}
    .layout.view-console .console .body {{
      display: flex;
      flex-direction: column;
      min-height: 0;
      width: 100%;
      flex: 1 1 auto;
      height: 100%;
      background: transparent;
      padding: 0;
      overflow: hidden;
    }}
    .layout.view-console .console-controls {{
      flex: 0 0 auto;
    }}
    .layout.view-console .console-terminal-screen {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      width: 100%;
      height: 100%;
      max-height: none;
    }}
    .layout.view-console #live-console {{
      flex: 1 1 auto;
      min-height: 0;
      max-height: none;
    }}
    .layout.view-data .chat,
    .layout.view-data .bots,
    .layout.view-data .nodes,
    .layout.view-data .map,
    .layout.view-data .map-data,
    .layout.view-data .summary,
    .layout.view-data .packets,
    .layout.view-data .console,
    .layout.view-data .splitter,
    .layout.view-data .hsplitter[data-target="top"],
    .layout.view-data .hsplitter[data-target="mid"],
    .layout.view-data .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-environment {{
      height: 100%;
      min-height: 0;
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-environment .environment {{
      grid-column: 1 / span 3;
      grid-row: 1;
      display: flex;
      flex-direction: column;
      min-height: 0;
    }}
    .layout.view-environment .environment .body {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      overflow: auto;
    }}
    .layout.view-environment .chat,
    .layout.view-environment .bots,
    .layout.view-environment .nodes,
    .layout.view-environment .map,
    .layout.view-environment .map-data,
    .layout.view-environment .summary,
    .layout.view-environment .packets,
    .layout.view-environment .encription,
    .layout.view-environment .raw,
    .layout.view-environment .console,
    .layout.view-environment .splitter,
    .layout.view-environment .hsplitter[data-target="top"],
    .layout.view-environment .hsplitter[data-target="mid"],
    .layout.view-environment .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-console .chat,
    .layout.view-console .bots,
    .layout.view-console .nodes,
    .layout.view-console .map,
    .layout.view-console .map-data,
    .layout.view-console .packets,
    .layout.view-console .raw,
    .layout.view-console .splitter,
    .layout.view-console .hsplitter[data-target="top"],
    .layout.view-console .hsplitter[data-target="mid"],
    .layout.view-console .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-settings {{
      height: 100%;
      min-height: 0;
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: minmax(0, 1fr);
      background: transparent;
      overflow: visible;
    }}
    .layout.view-settings .settings {{
      grid-column: 1;
      grid-row: 1;
      display: flex;
      flex-direction: column;
      gap: 8px;
      height: 100%;
      min-height: 0;
      max-height: 100%;
      width: 100%;
      background: transparent;
      border: 0;
      box-shadow: none;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      overflow: visible;
    }}
    .layout.view-settings .settings .body {{
      display: flex;
      flex: 1 1 auto;
      flex-direction: column;
      gap: 8px;
      min-height: 0;
      width: 100%;
      overflow: hidden;
      background: transparent;
      padding: 0;
    }}
    .layout.view-settings .settings-chrome {{
      position: relative;
      top: auto;
      flex: 0 0 auto;
      margin: 0;
    }}
    .layout.view-settings .settings-grid {{
      flex: 1 1 auto;
      min-height: 0;
      overflow-y: auto;
      overflow-x: hidden;
      padding-right: 2px;
    }}
    .layout.view-settings .settings-grid > .settings-panel {{
      flex: 0 0 auto;
    }}
    .layout.view-settings .chat,
    .layout.view-settings .bots,
    .layout.view-settings .nodes,
    .layout.view-settings .map,
    .layout.view-settings .map-data,
    .layout.view-settings .favorites,
    .layout.view-settings .packets,
    .layout.view-settings .raw,
    .layout.view-settings .splitter,
    .layout.view-settings .hsplitter[data-target="top"],
    .layout.view-settings .hsplitter[data-target="mid"],
    .layout.view-settings .hsplitter[data-target="low"] {{
      display: none !important;
    }}
    .layout.view-settings .console {{
      display: none !important;
    }}

    .layout.view-bots {{
      height: 100%;
      min-height: 0;
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-bots .bots {{
      grid-column: 1;
      grid-row: 1;
      display: flex;
      flex-direction: column;
      min-height: 0;
      height: 100%;
      max-height: 100%;
      background: transparent;
      border: 0;
      box-shadow: none;
      overflow: hidden;
    }}
    .layout.view-bots .bots .body {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      overflow: hidden;
      background: transparent;
      padding: 0;
    }}
    .layout.view-bots .chat,
    .layout.view-bots .files,
    .layout.view-bots .bbs,
    .layout.view-bots .settings,
    .layout.view-bots .games,
    .layout.view-bots .nodes,
    .layout.view-bots .map,
    .layout.view-bots .map-data,
    .layout.view-bots .favorites,
    .layout.view-bots .history-chat,
    .layout.view-bots .channels-view,
    .layout.view-bots .packets,
    .layout.view-bots .encription,
    .layout.view-bots .raw,
    .layout.view-bots .console,
    .layout.view-bots .remote,
    .layout.view-bots .summary,
    .layout.view-bots .splitter,
    .layout.view-bots .hsplitter[data-target="top"],
    .layout.view-bots .hsplitter[data-target="mid"],
    .layout.view-bots .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-games {{
      height: 100%;
      min-height: 0;
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-games .games {{
      grid-column: 1;
      grid-row: 1;
      display: flex;
      min-height: 0;
      max-height: 100%;
      background: transparent;
      border: 0;
      box-shadow: none;
      overflow: visible;
    }}
    .layout.view-games .games .body {{
      display: flex;
      flex-direction: column;
      min-height: 0;
      overflow: hidden;
      background: transparent;
      padding: 0;
    }}
    .layout.view-games .chat,
    .layout.view-games .settings,
    .layout.view-games .bots,
    .layout.view-games .nodes,
    .layout.view-games .map,
    .layout.view-games .map-data,
    .layout.view-games .favorites,
    .layout.view-games .history-chat,
    .layout.view-games .channels-view,
    .layout.view-games .packets,
    .layout.view-games .encription,
    .layout.view-games .raw,
    .layout.view-games .console,
    .layout.view-games .summary,
    .layout.view-games .splitter,
    .layout.view-games .hsplitter[data-target="top"],
    .layout.view-games .hsplitter[data-target="mid"],
    .layout.view-games .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-bbs {{
      height: 100%;
      min-height: 0;
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-bbs .bbs {{
      grid-column: 1;
      grid-row: 1;
      display: flex;
      flex-direction: column;
      min-height: 0;
      height: 100%;
      max-height: 100%;
      background: transparent;
      border: 0;
      box-shadow: none;
      overflow: hidden;
    }}
    .layout.view-bbs .bbs .body {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      overflow: hidden;
      background: transparent;
      padding: 0;
    }}
    .layout.view-bbs .chat,
    .layout.view-bbs .files,
    .layout.view-bbs .settings,
    .layout.view-bbs .bots,
    .layout.view-bbs .games,
    .layout.view-bbs .nodes,
    .layout.view-bbs .map,
    .layout.view-bbs .map-data,
    .layout.view-bbs .favorites,
    .layout.view-bbs .history-chat,
    .layout.view-bbs .channels-view,
    .layout.view-bbs .packets,
    .layout.view-bbs .encription,
    .layout.view-bbs .raw,
    .layout.view-bbs .console,
    .layout.view-bbs .summary,
    .layout.view-bbs .splitter,
    .layout.view-bbs .hsplitter[data-target="top"],
    .layout.view-bbs .hsplitter[data-target="mid"],
    .layout.view-bbs .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-remote {{
      height: 100%;
      min-height: 0;
      grid-template-columns: minmax(0, 1fr);
      grid-template-rows: minmax(0, 1fr);
    }}
    .layout.view-remote .remote {{
      grid-column: 1;
      grid-row: 1;
      display: flex;
      min-height: 0;
      max-height: 100%;
    }}
    .layout.view-remote .remote .body {{
      display: flex;
      flex-direction: column;
      min-height: 0;
      overflow: hidden;
    }}
    .layout.view-remote .chat,
    .layout.view-remote .files,
    .layout.view-remote .settings,
    .layout.view-remote .bots,
    .layout.view-remote .games,
    .layout.view-remote .nodes,
    .layout.view-remote .map,
    .layout.view-remote .map-data,
    .layout.view-remote .favorites,
    .layout.view-remote .history-chat,
    .layout.view-remote .channels-view,
    .layout.view-remote .packets,
    .layout.view-remote .encription,
    .layout.view-remote .raw,
    .layout.view-remote .console,
    .layout.view-remote .summary,
    .layout.view-remote .splitter,
    .layout.view-remote .hsplitter[data-target="top"],
    .layout.view-remote .hsplitter[data-target="mid"],
    .layout.view-remote .hsplitter[data-target="low"] {{
      display: none !important;
    }}

    .layout.view-saved {{
      height: 100%;
      min-height: 0;
      grid-template-columns: minmax(350px, var(--split-left-pct)) var(--splitter-size) minmax(320px, 1fr);
      grid-template-rows: auto minmax(0, 1fr);
    }}
    .layout.view-saved .summary {{
      display: block;
      grid-column: 1 / span 3;
      grid-row: 1;
    }}
    .layout.view-saved .favorites {{
      display: none !important;
    }}
    .layout.view-saved .map {{
      --saved-pane-head-bg: var(--surface-tint-vignette), var(--surface-tint-bg-alt);
      --saved-pane-body-bg: var(--surface-tint-vignette), var(--surface-tint-bg);
      --saved-pane-head-border: var(--surface-tint-border);
      --saved-pane-divider-bg: var(--surface-tint-divider-bg);
      --saved-pane-divider-border: var(--surface-tint-border);
      --saved-pane-divider-line: var(--surface-tint-divider-line, var(--surface-tint-border-strong));
      --saved-pane-divider-line-active: var(--accent);
      display: flex;
      grid-column: 1 / span 3;
      grid-row: 2;
      min-height: 0;
      border: 0;
      background: transparent;
      box-shadow: none;
      overflow: visible;
    }}
    .layout.view-saved .map-card-head {{
      position: relative;
      display: grid;
      grid-template-columns: minmax(320px, var(--saved-node-pane-pct)) var(--splitter-size) minmax(320px, 1fr);
      align-items: stretch;
      gap: 8px;
      border-bottom: 0;
      background: transparent;
      padding: 0 10px 0 10px;
    }}
    .layout.view-saved .map-card-head::before {{
      content: none;
    }}
    .layout.view-saved .saved-map-splitter-head {{
      display: block;
      grid-column: 2;
      grid-row: 1;
      min-height: 0;
      border: 1px solid var(--saved-pane-divider-border);
      border-bottom: 0;
      border-radius: 8px 8px 0 0;
      background: var(--saved-pane-divider-bg);
      z-index: 2;
    }}
    .layout.view-saved .saved-map-splitter-head::before {{
      top: 8px;
      bottom: 0;
      background: var(--saved-pane-divider-line);
    }}
    .layout.view-saved .saved-map-splitter-head:hover::before,
    .layout.view-saved .saved-map-splitter-head.active::before {{
      background: var(--saved-pane-divider-line-active);
    }}
    .layout.view-saved .map-card-head .pane-head-title,
    .layout.view-saved .map-card-head .pane-head-actions {{
      min-height: 32px;
      margin: 0;
      border: 1px solid var(--saved-pane-head-border);
      border-bottom: 1px solid var(--saved-pane-head-border);
      border-radius: 8px 8px 0 0;
      background: var(--saved-pane-head-bg);
    }}
    .layout.view-saved .map-card-head .pane-head-title {{
      grid-column: 1;
      display: flex;
      align-items: center;
      justify-content: flex-start;
      gap: 8px;
      padding: 8px 10px 7px 10px;
    }}
    .layout.view-saved .map-card-head #map-card-title-text {{
      order: 2;
      flex: 1 1 auto;
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }}
    .layout.view-saved .map-card-head .map-title-favorite-btn {{
      order: 1;
      margin-left: 0;
      margin-right: 2px;
      transform: none;
    }}
    .layout.view-saved .map-card-head .pane-head-actions {{
      grid-column: 3;
      display: inline-flex;
      align-items: center;
      justify-content: flex-end;
      gap: 8px;
      flex-wrap: wrap;
      padding: 6px 8px;
    }}
    .layout.view-saved .map .body {{
      padding: 0 10px 8px 10px;
      min-height: 0;
      display: grid;
      grid-template-columns: minmax(320px, var(--saved-node-pane-pct)) var(--splitter-size) minmax(320px, 1fr);
      gap: 8px;
    }}
    .layout.view-saved .map-frame {{
      display: block;
      grid-column: 3;
      grid-row: 1;
      order: 0;
      flex: 0 0 auto;
      min-height: 260px;
      border-color: var(--saved-pane-head-border);
      border-top: 0;
      border-radius: 0 0 8px 8px;
    }}
    .layout.view-saved .saved-node-pane {{
      display: flex;
      grid-column: 1;
      grid-row: 1;
      order: 0;
      flex: 0 0 auto;
      min-height: 0;
      gap: 8px;
      padding: 8px 10px;
      border: 1px solid var(--saved-pane-head-border);
      border-top: 0;
      border-radius: 0 0 8px 8px;
      background: var(--saved-pane-body-bg);
    }}
    .layout.view-saved .saved-node-search-wrap {{
      display: block;
      padding: 0;
    }}
    .layout.view-saved .saved-node-details {{
      display: flex;
      flex: 1 1 auto;
      min-height: 0;
      border: 0;
      border-radius: 0;
      background: transparent;
      padding: 0;
    }}
    .layout.view-saved .saved-map-splitter {{
      display: block;
      grid-column: 2;
      grid-row: 1;
      min-height: 0;
      border-color: var(--saved-pane-divider-border);
      background: var(--saved-pane-divider-bg);
      border-top: 0;
      border-radius: 0 0 8px 8px;
    }}
    .layout.view-saved .saved-map-splitter::before {{
      background: var(--saved-pane-divider-line);
      top: 0;
      bottom: 8px;
    }}
    .layout.view-saved .saved-map-splitter:hover::before,
    .layout.view-saved .saved-map-splitter.active::before {{
      background: var(--saved-pane-divider-line-active);
    }}
    .layout.view-saved .saved-node-stack {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      gap: 0;
    }}
    .layout.view-saved .saved-node-main-scroll {{
      flex: 0 0 var(--saved-node-main-pct);
      min-height: 0;
      overflow: auto;
    }}
    .layout.view-saved .saved-node-history-splitter {{
      display: block;
    }}
    .layout.view-saved .saved-node-history-host {{
      display: flex;
      flex: 1 1 auto;
      min-height: 0;
    }}
    .layout.view-saved .saved-node-history-host #map-data-node {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      gap: 8px;
    }}
    .layout.view-saved .saved-node-history-host #tab-panel-signal,
    .layout.view-saved .saved-node-history-host #tab-panel-link,
    .layout.view-saved .saved-node-history-host #tab-panel-online,
    .layout.view-saved .saved-node-history-host #tab-panel-packets,
    .layout.view-saved .saved-node-history-host #tab-panel-names,
    .layout.view-saved .saved-node-history-host #tab-panel-overview {{
      display: flex;
      flex-direction: column;
      min-height: 0;
      gap: 8px;
    }}
    .layout.view-saved .saved-node-history-host #tab-panel-signal,
    .layout.view-saved .saved-node-history-host #tab-panel-link,
    .layout.view-saved .saved-node-history-host #tab-panel-online,
    .layout.view-saved .saved-node-history-host #tab-panel-packets,
    .layout.view-saved .saved-node-history-host #tab-panel-names,
    .layout.view-saved .saved-node-history-host #tab-panel-overview {{
      flex: 1 1 auto;
      min-height: 0;
    }}
    .layout.view-saved .saved-node-history-host #signal-chart-wrap,
    .layout.view-saved .saved-node-history-host #node-link-quality-chart-wrap,
    .layout.view-saved .saved-node-history-host #node-online-chart-wrap,
    .layout.view-saved .saved-node-history-host #node-packets-chart-wrap {{
      height: 260px;
      min-height: 220px;
    }}
    .layout.view-saved .map-data,
    .layout.view-saved .chat,
    .layout.view-saved .bots,
    .layout.view-saved .nodes,
    .layout.view-saved .packets,
    .layout.view-saved .raw,
    .layout.view-saved .console,
    .layout.view-saved .hsplitter[data-target="top"],
    .layout.view-saved .hsplitter[data-target="mid"],
    .layout.view-saved .hsplitter[data-target="low"] {{
      display: none !important;
    }}
    .layout.view-saved .splitter {{
      display: none !important;
      grid-row: 1;
    }}
    table {{
      width: 100%;
      border-collapse: collapse;
      table-layout: fixed;
      font-size: 11px;
    }}
    th, td {{
      text-align: left;
      border-bottom: 1px solid #e7efe4;
      padding: 4px 5px;
      vertical-align: middle;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }}
    th {{
      position: sticky;
      top: 0;
      z-index: 2;
      background: var(--surface-tint-bg-soft);
      color: #204231;
      font-size: 10px;
      letter-spacing: 0.2px;
      text-transform: uppercase;
    }}
    th.sortable {{
      cursor: pointer;
      user-select: none;
    }}
    th.sortable::after {{
      content: " \2195";
      font-size: 9px;
      opacity: 0.35;
    }}
    th.sortable.sorted-asc::after {{
      content: " \25B2";
      opacity: 0.9;
    }}
    th.sortable.sorted-desc::after {{
      content: " \25BC";
      opacity: 0.9;
    }}
    #nodes-table tbody tr.node-selectable {{
      cursor: pointer;
    }}
    #nodes-table tbody tr.node-selectable:hover {{
      background: var(--surface-tint-bg-soft);
    }}
    #encription-packets-table tbody tr.node-selectable,
    #encription-traffic-table tbody tr.node-selectable {{
      cursor: pointer;
    }}
    #encription-packets-table tbody tr.node-selectable:hover,
    #encription-traffic-table tbody tr.node-selectable:hover {{
      background: var(--surface-tint-bg-soft);
    }}
    #history-chat-table tbody tr.history-node-selectable.selected-node td {{
      background: color-mix(in srgb, var(--surface-tint-bg-hover) 86%, var(--surface-tint-border-strong) 14%);
      color: var(--surface-tint-text);
    }}
    #history-chat-table tbody tr.history-node-selectable.selected-node td:first-child {{
      box-shadow: inset 3px 0 0 var(--surface-tint-border-strong);
    }}
    .node-status-dot {{
      display: inline-block;
      width: 10px;
      margin-right: 4px;
      font-size: 10px;
      line-height: 1;
      opacity: 0.85;
      transform: translateY(-0.5px);
    }}
    .node-status-dot.status-online {{ color: #2f855a; }}
    .node-status-dot.status-warn {{ color: #d1a02d; }}
    .node-status-dot.status-stale {{ color: #b22222; }}
    .node-status-dot.status-unknown {{ color: var(--surface-tint-text-soft); }}
    #nodes-table tbody tr.selected-node {{
      background: color-mix(in srgb, var(--surface-tint-bg-hover) 86%, var(--surface-tint-border-strong) 14%);
    }}
    #nodes-table tbody tr.selected-node td {{
      background: color-mix(in srgb, var(--surface-tint-bg-hover) 86%, var(--surface-tint-border-strong) 14%);
      color: var(--surface-tint-text);
    }}
    #nodes-table tbody tr.selected-node td:first-child {{
      box-shadow: inset 3px 0 0 var(--surface-tint-border-strong);
    }}
    #encription-packets-table tbody tr.selected-node td,
    #encription-traffic-table tbody tr.selected-node td {{
      background: color-mix(in srgb, var(--surface-tint-bg-hover) 86%, var(--surface-tint-border-strong) 14%);
      color: var(--surface-tint-text);
    }}
    #encription-packets-table tbody tr.selected-node td:first-child,
    #encription-traffic-table tbody tr.selected-node td:first-child {{
      box-shadow: inset 3px 0 0 var(--surface-tint-border-strong);
    }}
    .mono {{ font-family: "IBM Plex Mono", "Consolas", "Menlo", monospace; }}
    #nodes-table th:nth-child(10), #nodes-table td:nth-child(10) {{
      text-align: right;
    }}
    #nodes-table td:nth-child(10) {{
      padding-right: 4px;
      overflow: visible;
      text-overflow: clip;
    }}
    #encription-packets-table td:nth-child(7) {{
      font-family: "IBM Plex Mono", "Consolas", "Menlo", monospace;
      letter-spacing: 0.01em;
    }}
    .history-node-cell {{
      display: flex;
      flex-direction: column;
      gap: 1px;
      min-width: 0;
      line-height: 1.2;
    }}
    #history-chat-table td.history-node-clickable {{
      cursor: pointer;
    }}
    #history-chat-table td.history-node-clickable:hover .history-node-primary {{
      text-decoration: underline;
      text-underline-offset: 1px;
    }}
    .history-node-primary {{
      color: inherit;
      font-weight: 600;
      white-space: normal;
      overflow-wrap: anywhere;
      word-break: break-word;
    }}
    .history-node-secondary {{
      color: inherit;
      opacity: 0.74;
      font-size: 10px;
      white-space: normal;
      overflow-wrap: anywhere;
      word-break: break-word;
    }}
    .history-message-cell {{
      display: flex;
      flex-direction: column;
      gap: 4px;
      min-width: 0;
    }}
    .history-message-pill {{
      display: inline-flex;
      align-items: center;
      align-self: flex-start;
      border: 1px solid #bdd0dd;
      background: #eef5f9;
      color: #35576f;
      border-radius: 999px;
      padding: 1px 7px;
      font-size: 9px;
      line-height: 1.35;
      letter-spacing: 0.22px;
      font-weight: 700;
      text-transform: uppercase;
      white-space: nowrap;
    }}
    .history-message-pill.history-message-pill-name-change {{
      border-color: #b8d5c2;
      background: #edf7ef;
      color: #2d6143;
    }}
    .history-message-body {{
      min-width: 0;
    }}
    #nodes-table th:nth-child(10) {{
      color: #6a856f;
      font-size: 9px;
      letter-spacing: 0;
      text-transform: none;
    }}
    #nodes-table td:nth-child(3) {{
      overflow: visible;
      text-overflow: clip;
    }}
    .node-name-row {{
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: center;
      gap: 6px;
      min-width: 0;
      max-width: 100%;
      width: 100%;
    }}
    .node-name-label {{
      flex: 1 1 auto;
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      max-width: 100%;
    }}
    .chat .body {{
      display: flex;
      flex-direction: column;
      flex: 1;
      min-height: 0;
      gap: 0;
      padding: 0;
      background: var(--surface-tint-bg);
      overflow: hidden;
    }}
    .chat-shell {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      padding: 8px 10px;
      overflow: hidden;
    }}
    .chat-member-pane {{
      grid-area: members;
      grid-column: 2;
      border: 1px solid #d6e5d2;
      border-radius: 7px;
      background: #f8fcf8;
      min-height: 0;
      overflow: hidden;
      display: flex;
      flex-direction: column;
    }}
    .chat-member-list {{
      padding: 0;
      overflow: auto;
      overflow-anchor: none;
      min-height: 0;
      display: flex;
      flex-direction: column;
      gap: 0;
    }}
    .chat-roster-load-row {{
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 7px;
      padding: 6px 8px 8px 8px;
      color: #5f7a6a;
      font-size: 10px;
      line-height: 1.2;
      border-bottom: 1px solid #d3e0d0;
      background: color-mix(in srgb, #f8fcf8 92%, #dcead8 8%);
    }}
    .chat-roster-load-btn {{
      border: 1px solid #9abda5;
      background: #eff8f1;
      color: #214434;
      border-radius: 999px;
      min-height: 23px;
      padding: 3px 9px;
      font-size: 10px;
      line-height: 1.2;
      cursor: pointer;
      white-space: nowrap;
    }}
    .chat-roster-load-btn:hover {{
      background: #e0f0e5;
    }}
    .chat-roster-load-detail {{
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }}
    .chat-node-details-splitter {{
      display: none;
      position: relative;
      border-radius: 8px;
      border: 1px solid var(--surface-tint-border);
      background: linear-gradient(90deg, var(--surface-tint-bg-soft), var(--surface-tint-bg-alt));
      cursor: row-resize;
      touch-action: none;
      user-select: none;
      min-height: 6px;
      flex: 0 0 var(--splitter-size);
      margin: 0;
    }}
    .chat-node-details-splitter::before {{
      content: "";
      position: absolute;
      left: 8px;
      right: 8px;
      top: 50%;
      height: 2px;
      transform: translateY(-50%);
      background: var(--surface-tint-border-strong);
      border-radius: 1px;
    }}
    .chat-node-details-splitter:hover::before,
    .chat-node-details-splitter.active::before {{
      background: var(--accent);
    }}
    .chat-left-section.chat-users-section.has-node-details .chat-member-pinned-shell,
    .chat-left-section.chat-users-section.has-node-details .chat-member-list {{
      display: none !important;
    }}
    .chat-left-section.chat-users-section.has-node-details .chat-node-details-splitter {{
      display: none;
    }}
    .chat-node-details-inline-host {{
      display: flex;
      flex: 0 0 auto;
      flex-direction: column;
      min-height: 0;
      width: 100%;
      box-sizing: border-box;
    }}
    .chat-left-section.chat-users-section.has-node-details .chat-node-details-inline-host {{
      flex: 1 1 auto;
      min-height: 0;
      height: 100%;
      max-height: none;
      margin-top: 0;
    }}
    .chat-left-section.chat-users-section.has-node-details .chat-node-details-drawer {{
      flex: 1 1 auto;
      min-height: 0;
      height: 100%;
      max-height: none;
      margin-top: 0;
    }}
    .chat-left-section.chat-users-section > .chat-node-details-inline-host[data-dock="shared"] {{
      padding: 6px 5px 6px 5px;
      background:
        linear-gradient(
          180deg,
          color-mix(in srgb, var(--surface-tint-bg-hover) 34%, transparent),
          transparent 54px
        );
    }}
    .chat-left-section.chat-users-section > .chat-node-details-inline-host[data-dock="shared"] .chat-node-details-drawer {{
      width: 100%;
      border-top-left-radius: 8px;
      border-top-right-radius: 8px;
    }}
    .chat-node-details-drawer {{
      border: 1px solid var(--surface-tint-border);
      border-radius: 8px;
      background: var(--surface-tint-bg);
      display: flex;
      flex-direction: column;
      min-height: 220px;
      height: clamp(220px, 42vh, 560px);
      max-height: 68vh;
      overflow: hidden;
      flex: 0 0 auto;
      margin-top: 2px;
    }}
    .chat-node-details-drawer[hidden] {{
      display: none !important;
    }}
    .chat-node-details-promoted-shell {{
      position: absolute;
      inset: 0;
      z-index: 28;
      display: flex;
      align-items: stretch;
      justify-content: center;
      min-width: 0;
      min-height: 0;
      padding: 8px;
      background: color-mix(in srgb, #020812 72%, transparent);
      box-sizing: border-box;
      pointer-events: none;
    }}
    .chat-node-details-promoted-shell[hidden] {{
      display: none !important;
    }}
    .chat-node-details-promoted-host {{
      pointer-events: auto;
      display: flex;
      flex-direction: column;
      width: 100%;
      max-width: none;
      min-width: 0;
      min-height: 0;
      height: 100%;
      padding: 0;
      box-sizing: border-box;
      background:
        linear-gradient(
          180deg,
          color-mix(in srgb, var(--workspace-shell-bg-alt, var(--panel)) 78%, #07121f 22%),
          color-mix(in srgb, var(--workspace-shell-bg, var(--panel)) 90%, #050b13 10%)
        ),
        var(--workspace-shell-bg, var(--panel));
      border-radius: 8px;
    }}
    .chat-node-details-promoted-host .chat-node-details-drawer {{
      flex: 1 1 auto;
      width: 100%;
      min-height: 0;
      height: 100%;
      max-height: none;
      margin-top: 0;
      background:
        linear-gradient(
          180deg,
          color-mix(in srgb, var(--workspace-shell-bg-alt, var(--panel)) 78%, #07121f 22%),
          color-mix(in srgb, var(--workspace-shell-bg, var(--panel)) 90%, #050b13 10%)
        ),
        var(--workspace-shell-bg, var(--panel));
      box-shadow:
        0 18px 48px color-mix(in srgb, var(--shadow-color, #0d1c16) 24%, transparent),
        0 0 0 1px color-mix(in srgb, var(--surface-tint-border-strong) 46%, transparent);
    }}
    .chat-node-details-promoted-host .chat-node-details-content-host,
    .chat-node-details-promoted-host .chat-node-details-location-host,
    .chat-node-details-promoted-host .chat-node-details-chat-host,
    .chat-node-details-promoted-host .chat-node-details-links-host,
    .chat-node-details-promoted-host .chat-node-details-telemetry-host,
    .chat-node-details-promoted-host .chat-node-details-notes-host,
    .chat-node-details-promoted-host .chat-node-details-messages-host,
    .chat-node-details-promoted-host .chat-node-details-history-host,
    .chat-node-details-promoted-host .chat-node-details-tag-host {{
      padding: 9px 10px 10px 10px;
      gap: 8px;
      background: var(--workspace-shell-bg, var(--panel));
    }}
    .chat-node-details-promoted-host .chat-node-details-drawer .saved-node-details {{
      padding: 10px;
    }}
    .chat-node-details-promote-btn.active {{
      border-color: rgba(73, 117, 176, 0.72);
      background: rgba(96, 154, 223, 0.18);
      color: #1f4e78;
      opacity: 1;
    }}
    .chat-node-details-head {{
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto auto;
      align-items: center;
      gap: 8px;
      padding: 7px 8px 6px 8px;
      border-bottom: 1px solid var(--surface-tint-border);
      background: var(--surface-tint-bg-alt);
    }}
    .chat-node-details-head-main {{
      min-width: 0;
      width: 100%;
      display: grid;
      grid-template-columns: auto auto auto auto minmax(0, 1fr);
      align-items: center;
      gap: 8px;
      justify-self: stretch;
    }}
    .chat-node-details-icon-btn {{
      width: 22px;
      height: 22px;
      padding: 0;
      border: 1px solid #4f6f63;
      border-radius: 999px;
      background: rgba(28, 50, 61, 0.94);
      color: #b9ddd0;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      box-shadow: inset 0 1px 0 rgba(173, 214, 197, 0.08);
    }}
    .chat-node-details-icon-btn:hover {{
      border-color: #6d9484;
      background: rgba(34, 59, 70, 0.98);
      color: #d5efe4;
    }}
    .chat-node-details-icon-btn:focus-visible {{
      outline: 2px solid rgba(77, 132, 102, 0.38);
      outline-offset: 2px;
    }}
    .chat-node-details-icon-chip {{
      width: 100%;
      height: 100%;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border: 0;
      background: transparent;
      filter: none;
      color: inherit;
      font-size: 12px;
      font-weight: 700;
      line-height: 1;
    }}
    .chat-node-details-icon-chip.is-empty {{
      border: 0;
      background: transparent;
      filter: none;
      color: inherit;
    }}
    .chat-node-details-icon-chip.has-emoji {{
      padding: 0;
    }}
    .chat-node-details-icon-glyph {{
      display: inline-block;
      font-size: 13px;
      line-height: 1;
      transform: translateY(-0.5px);
    }}
    .chat-node-details-title {{
      min-width: 0;
      width: 100%;
      font-size: 11px;
      font-weight: 700;
      color: #264734;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      text-align: center;
      text-transform: uppercase;
      letter-spacing: 0.2px;
    }}
    .chat-node-details-head-actions {{
      display: inline-flex;
      align-items: center;
      justify-content: flex-end;
      gap: 6px;
      min-width: 0;
      justify-self: end;
    }}
    .chat-node-details-close-btn {{
      border: 1px solid #bfd5c3;
      background: #f5fbf6;
      color: #244b37;
      border-radius: 999px;
      width: 22px;
      min-width: 22px;
      height: 22px;
      padding: 0;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 14px;
      line-height: 1;
      font-weight: 700;
      cursor: pointer;
      white-space: nowrap;
    }}
    .chat-node-details-close-btn:hover {{
      background: #e8f3eb;
    }}
    .chat-node-details-tabs {{
      display: flex;
      align-items: center;
      flex-wrap: wrap;
      gap: 4px;
      padding: 6px 8px 4px 8px;
      border-bottom: 1px solid #d8e6d5;
      background: #f2faf3;
    }}
    .chat-node-details-tab-spacer {{
      flex: 1 1 auto;
      min-width: 6px;
    }}
    .chat-node-details-action-btn {{
      border: 1px solid #c3d8c7;
      background: #f8fdf8;
      color: #2a4b3b;
      border-radius: 999px;
      padding: 3px 10px;
      font-size: 10px;
      line-height: 1.2;
      font-weight: 600;
      white-space: nowrap;
      cursor: pointer;
      text-decoration: none;
      min-height: 20px;
      opacity: 0.95;
    }}
    .chat-node-details-action-btn:hover {{
      background: #e9f6eb;
      border-color: #95bca1;
      color: #19442f;
      opacity: 1;
    }}
    .chat-node-details-action-btn:focus-visible {{
      outline: 1px solid #6fae8d;
      outline-offset: 1px;
    }}
    .chat-node-details-mute-btn.active {{
      border-color: rgba(162, 70, 70, 0.78);
      background: rgba(215, 102, 102, 0.22);
      color: #7b2020;
      opacity: 1;
    }}
    .chat-node-details-pin-btn.active {{
      border-color: rgba(73, 117, 176, 0.78);
      background: rgba(96, 154, 223, 0.2);
      color: #1f4e78;
      opacity: 1;
    }}
    .chat-node-details-reset-btn {{
      border-color: rgba(186, 122, 80, 0.58);
      background: rgba(244, 234, 223, 0.96);
      color: #6f3f22;
    }}
    .chat-node-details-reset-btn:hover {{
      border-color: rgba(171, 101, 58, 0.74);
      background: rgba(247, 226, 207, 0.98);
      color: #5f3014;
    }}
    .chat-node-details-tab-btn {{
      border: 1px solid #c3d8c7;
      background: #f8fdf8;
      color: #2a4b3b;
      border-radius: 999px;
      padding: 3px 10px;
      font-size: 10px;
      line-height: 1.2;
      font-weight: 600;
      cursor: pointer;
      white-space: nowrap;
    }}
    .chat-node-details-tab-btn.is-active {{
      background: #dff1e6;
      border-color: #89b89a;
      color: #18442f;
    }}
    .chat-node-details-tab-tag-btn {{
      display: inline-flex;
      align-items: center;
      min-width: 0;
    }}
    .chat-node-details-head-tag-btn {{
      justify-self: start;
      max-width: 100%;
    }}
    .chat-node-details-status-chip {{
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 20px;
      padding: 3px 9px;
      border: 1px solid rgba(112, 154, 91, 0.74);
      border-radius: 999px;
      background: rgba(97, 170, 92, 0.18);
      color: #254f2d;
      font-size: 10px;
      line-height: 1.2;
      font-weight: 700;
      white-space: nowrap;
      text-transform: none;
    }}
    .chat-node-details-status-chip[hidden] {{
      display: none !important;
    }}
    .chat-node-details-tab-tag-label {{
      min-width: 0;
      max-width: 120px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }}
    .chat-node-details-panel {{
      flex: 1 1 auto;
      min-height: 0;
      display: flex;
      flex-direction: column;
      overflow: hidden;
    }}
    .chat-node-details-panel[hidden] {{
      display: none !important;
    }}
    .chat-node-details-content-host,
    .chat-node-details-location-host,
    .chat-node-details-chat-host,
    .chat-node-details-links-host,
    .chat-node-details-telemetry-host,
    .chat-node-details-notes-host,
    .chat-node-details-messages-host,
    .chat-node-details-history-host,
    .chat-node-details-tag-host {{
      flex: 1 1 auto;
      min-height: 0;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      padding: 6px 7px 7px 7px;
      gap: 6px;
    }}
    .chat-node-details-location-host,
    .chat-node-details-chat-host,
    .chat-node-details-links-host,
    .chat-node-details-telemetry-host,
    .chat-node-details-notes-host,
    .chat-node-details-messages-host,
    .chat-node-details-tag-host {{
      overflow: auto;
    }}
    .chat-node-details-telemetry-host {{
      --telemetry-surface-bg: var(--surface-tint-bg-soft, color-mix(in srgb, var(--panel, #ffffff) 84%, var(--bg, var(--surface-tint-bg-soft)) 16%));
      --telemetry-surface-bg-alt: var(--surface-tint-bg-alt, color-mix(in srgb, var(--panel, #ffffff) 72%, var(--surface-tint-bg-soft) 28%));
      --telemetry-surface-border: var(--surface-tint-border, color-mix(in srgb, var(--ui-border, var(--surface-tint-border)) 78%, var(--ui-accent, var(--accent)) 22%));
      --telemetry-surface-border-strong: var(--surface-tint-border-strong, color-mix(in srgb, var(--telemetry-surface-border) 68%, var(--ui-accent, var(--accent)) 32%));
      --telemetry-surface-text: var(--surface-tint-text, var(--ui-text, var(--surface-tint-text)));
      --telemetry-surface-text-soft: var(--surface-tint-text-soft, var(--ui-text-soft, var(--surface-tint-text-soft)));
      --telemetry-success-accent: color-mix(in srgb, var(--ui-accent, var(--accent)) 72%, #7ac99e 28%);
      --telemetry-running-accent: color-mix(in srgb, var(--ui-accent, var(--accent)) 28%, #5a7fb8 72%);
      --telemetry-error-accent: color-mix(in srgb, var(--ui-accent, var(--accent)) 24%, #c25656 76%);
    }}
    .chat-node-telemetry-empty {{
      border: 1px dashed var(--telemetry-surface-border);
      border-radius: 8px;
      background: linear-gradient(
        135deg,
        color-mix(in srgb, var(--telemetry-surface-bg) 84%, white 16%) 0%,
        var(--telemetry-surface-bg) 100%
      );
      color: var(--telemetry-surface-text-soft);
      font-size: 11px;
      line-height: 1.35;
      padding: 10px;
    }}
    .chat-node-telemetry-head {{
      display: flex;
      flex-direction: column;
      gap: 2px;
    }}
    .chat-node-telemetry-title {{
      font-size: 11px;
      font-weight: 700;
      color: var(--telemetry-surface-text);
      letter-spacing: 0.2px;
      text-transform: uppercase;
    }}
    .chat-node-telemetry-subtitle {{
      font-size: 10px;
      line-height: 1.25;
      color: var(--telemetry-surface-text-soft);
      word-break: break-word;
    }}
    .chat-node-telemetry-summary {{
      border: 1px solid var(--telemetry-surface-border);
      border-radius: 7px;
      background: linear-gradient(
        180deg,
        color-mix(in srgb, var(--telemetry-surface-bg-alt) 84%, white 16%) 0%,
        color-mix(in srgb, var(--telemetry-surface-bg) 92%, white 8%) 100%
      );
      color: var(--telemetry-surface-text);
      font-size: 11px;
      line-height: 1.3;
      padding: 6px 7px;
      word-break: break-word;
    }}
    .chat-node-telemetry-summary.is-error {{
      border-color: color-mix(in srgb, var(--telemetry-error-accent) 64%, var(--telemetry-surface-border) 36%);
      background: linear-gradient(
        140deg,
        color-mix(in srgb, var(--telemetry-error-accent) 18%, var(--telemetry-surface-bg-alt) 82%) 0%,
        color-mix(in srgb, var(--telemetry-error-accent) 10%, var(--telemetry-surface-bg) 90%) 100%
      );
      color: color-mix(in srgb, var(--telemetry-error-accent) 70%, var(--telemetry-surface-text) 30%);
      font-weight: 600;
    }}
    .chat-node-telemetry-tools {{
      display: flex;
      flex-direction: column;
      gap: 6px;
      min-height: 0;
    }}
    .chat-node-telemetry-tool-row {{
      border: 1px solid var(--telemetry-surface-border);
      border-radius: 8px;
      background: linear-gradient(
        138deg,
        color-mix(in srgb, var(--telemetry-surface-bg-alt) 78%, white 22%) 0%,
        color-mix(in srgb, var(--telemetry-surface-bg) 88%, white 12%) 100%
      );
      padding: 7px;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 4px 8px;
      align-items: center;
    }}
    .chat-node-telemetry-tool-row.is-running {{
      border-color: color-mix(in srgb, var(--telemetry-running-accent) 66%, var(--telemetry-surface-border-strong) 34%);
      background: linear-gradient(
        140deg,
        color-mix(in srgb, var(--telemetry-running-accent) 18%, var(--telemetry-surface-bg-alt) 82%) 0%,
        color-mix(in srgb, var(--telemetry-running-accent) 10%, var(--telemetry-surface-bg) 90%) 100%
      );
    }}
    .chat-node-telemetry-tool-row.is-success {{
      border-color: color-mix(in srgb, var(--telemetry-success-accent) 64%, var(--telemetry-surface-border-strong) 36%);
      background: linear-gradient(
        140deg,
        color-mix(in srgb, var(--telemetry-success-accent) 20%, var(--telemetry-surface-bg-alt) 80%) 0%,
        color-mix(in srgb, var(--telemetry-success-accent) 12%, var(--telemetry-surface-bg) 88%) 100%
      );
    }}
    .chat-node-telemetry-tool-row.is-error {{
      border-color: color-mix(in srgb, var(--telemetry-error-accent) 62%, var(--telemetry-surface-border-strong) 38%);
      background: linear-gradient(
        140deg,
        color-mix(in srgb, var(--telemetry-error-accent) 20%, var(--telemetry-surface-bg-alt) 80%) 0%,
        color-mix(in srgb, var(--telemetry-error-accent) 12%, var(--telemetry-surface-bg) 88%) 100%
      );
    }}
    .chat-node-telemetry-tool-main {{
      min-width: 0;
      display: flex;
      flex-direction: column;
      gap: 2px;
    }}
    .chat-node-telemetry-tool-label {{
      font-size: 11px;
      line-height: 1.25;
      font-weight: 700;
      color: var(--telemetry-surface-text);
    }}
    .chat-node-telemetry-tool-copy {{
      font-size: 10px;
      line-height: 1.25;
      color: var(--telemetry-surface-text-soft);
      word-break: break-word;
    }}
    .chat-node-telemetry-tool-actions {{
      display: flex;
      align-items: center;
      justify-content: flex-end;
      gap: 6px;
      flex-wrap: wrap;
    }}
    .chat-node-telemetry-tool-shortcut-btn,
    .chat-node-telemetry-tool-run-btn {{
      border: 1px solid color-mix(in srgb, var(--telemetry-surface-border) 72%, var(--telemetry-success-accent) 28%);
      background: linear-gradient(
        150deg,
        color-mix(in srgb, var(--telemetry-surface-bg-alt) 84%, white 16%) 0%,
        color-mix(in srgb, var(--telemetry-surface-bg) 96%, white 4%) 100%
      );
      color: color-mix(in srgb, var(--telemetry-surface-text) 84%, var(--telemetry-success-accent) 16%);
      border-radius: 999px;
      padding: 2px 9px;
      font-size: 10px;
      line-height: 1.2;
      font-weight: 600;
      cursor: pointer;
      white-space: nowrap;
    }}
    .chat-node-telemetry-tool-shortcut-btn {{
      border-color: color-mix(in srgb, var(--telemetry-surface-border) 78%, var(--telemetry-running-accent) 22%);
      color: color-mix(in srgb, var(--telemetry-surface-text) 80%, var(--telemetry-running-accent) 20%);
    }}
    .chat-node-telemetry-tool-shortcut-btn:hover,
    .chat-node-telemetry-tool-run-btn:hover {{
      background: linear-gradient(
        150deg,
        color-mix(in srgb, var(--telemetry-success-accent) 14%, var(--telemetry-surface-bg-alt) 86%) 0%,
        color-mix(in srgb, var(--telemetry-success-accent) 20%, var(--telemetry-surface-bg) 80%) 100%
      );
      border-color: color-mix(in srgb, var(--telemetry-success-accent) 58%, var(--telemetry-surface-border-strong) 42%);
      color: color-mix(in srgb, var(--telemetry-surface-text) 68%, var(--telemetry-success-accent) 32%);
    }}
    .chat-node-telemetry-tool-shortcut-btn:hover {{
      border-color: color-mix(in srgb, var(--telemetry-running-accent) 56%, var(--telemetry-surface-border-strong) 44%);
      color: color-mix(in srgb, var(--telemetry-surface-text) 66%, var(--telemetry-running-accent) 34%);
    }}
    .chat-node-telemetry-tool-run-btn:disabled {{
      opacity: 0.72;
      cursor: default;
      background: color-mix(in srgb, var(--telemetry-surface-bg) 88%, var(--ui-panel, #ffffff) 12%);
      border-color: var(--telemetry-surface-border);
      color: var(--telemetry-surface-text-soft);
    }}
    .chat-node-telemetry-tool-run-btn.is-running-trace {{
      --chat-node-telemetry-tool-progress: 0%;
      position: relative;
      overflow: hidden;
      isolation: isolate;
      min-width: 72px;
      padding: 0 8px;
    }}
    .chat-node-telemetry-tool-run-btn.is-running-trace:disabled {{
      opacity: 1;
      background: rgba(217, 236, 252, 0.20);
      border-color: rgba(69, 128, 188, 0.42);
      color: #1f4c78;
    }}
    .chat-node-telemetry-tool-progress-fill {{
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: var(--chat-node-telemetry-tool-progress);
      max-width: 100%;
      border-radius: inherit;
      background: linear-gradient(
        90deg,
        rgba(95, 163, 233, 0.60) 0%,
        rgba(68, 145, 223, 0.82) 100%
      );
      transition: width 180ms linear;
      pointer-events: none;
      z-index: 0;
    }}
    .chat-node-telemetry-tool-progress-text {{
      position: relative;
      z-index: 1;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 18px;
      min-width: 58px;
      font-size: 8px;
      line-height: 1;
      letter-spacing: 0.12px;
      text-transform: uppercase;
      text-shadow: 0 1px 0 rgba(255, 255, 255, 0.28);
      pointer-events: none;
    }}
    .chat-node-telemetry-tool-status {{
      grid-column: 1 / span 2;
      font-size: 10px;
      line-height: 1.25;
      color: var(--telemetry-surface-text);
      word-break: break-word;
    }}
    .chat-node-telemetry-tool-detail {{
      grid-column: 1 / span 2;
      font-size: 10px;
      line-height: 1.25;
      color: var(--telemetry-surface-text-soft);
      word-break: break-word;
    }}
    .chat-node-telemetry-tool-lines {{
      grid-column: 1 / span 2;
      display: flex;
      flex-direction: column;
      gap: 2px;
    }}
    .chat-node-telemetry-tool-line {{
      border: 1px solid color-mix(in srgb, var(--telemetry-surface-border) 84%, transparent);
      border-radius: 5px;
      background: color-mix(in srgb, var(--telemetry-surface-bg) 86%, var(--ui-panel, #ffffff) 14%);
      color: color-mix(in srgb, var(--telemetry-surface-text-soft) 82%, var(--telemetry-surface-text) 18%);
      font-size: 10px;
      line-height: 1.25;
      padding: 3px 5px;
      word-break: break-word;
    }}
    .chat-node-details-drawer .saved-node-details {{
      display: flex;
      flex: 1 1 auto;
      min-height: 0;
      border-color: var(--surface-tint-border);
      border-radius: 8px;
      background: var(--surface-tint-bg-soft);
      padding: 8px;
      overflow: hidden;
    }}
    .chat-node-details-drawer .saved-node-stack {{
      flex: 1 1 auto;
      min-height: 0;
    }}
    .chat-node-details-drawer .saved-node-main-scroll {{
      min-height: 0;
    }}
    .chat-node-details-drawer .saved-node-history-splitter,
    .chat-node-details-drawer .saved-node-history-host {{
      display: none !important;
    }}
    .chat-node-details-history-host #map-data-node {{
      display: flex !important;
      flex-direction: column;
      min-height: 0;
      height: 100%;
    }}
    .chat-node-details-history-host #node-history-caption {{
      display: none !important;
    }}
    .chat-node-details-history-host #tab-panel-signal,
    .chat-node-details-history-host #tab-panel-link,
    .chat-node-details-history-host #tab-panel-online,
    .chat-node-details-history-host #tab-panel-packets,
    .chat-node-details-history-host #tab-panel-names,
    .chat-node-details-history-host #tab-panel-overview {{
      display: flex;
      flex-direction: column;
      flex: 1 1 auto;
      min-height: 0;
      gap: 8px;
      overflow: auto;
    }}
    .chat-node-details-history-host #signal-chart-wrap,
    .chat-node-details-history-host #node-link-quality-chart-wrap,
    .chat-node-details-history-host #node-online-chart-wrap,
    .chat-node-details-history-host #node-packets-chart-wrap {{
      flex: 1 1 auto;
      height: auto;
      min-height: 0;
    }}
    .chat-left-section.chat-users-section.has-node-details.history-focus .chat-node-details-history-host {{
      padding: 4px 6px 6px 6px;
      gap: 4px;
    }}
    .chat-left-section.chat-users-section.has-node-details.history-focus .chat-node-details-history-host #tab-panel-signal,
    .chat-left-section.chat-users-section.has-node-details.history-focus .chat-node-details-history-host #tab-panel-link,
    .chat-left-section.chat-users-section.has-node-details.history-focus .chat-node-details-history-host #tab-panel-online,
    .chat-left-section.chat-users-section.has-node-details.history-focus .chat-node-details-history-host #tab-panel-packets,
    .chat-left-section.chat-users-section.has-node-details.history-focus .chat-node-details-history-host #tab-panel-names,
    .chat-left-section.chat-users-section.has-node-details.history-focus .chat-node-details-history-host #tab-panel-overview {{
      gap: 6px;
    }}
    .chat-left-section.chat-users-section.has-node-details.history-focus .chat-node-details-history-host .signal-timeline {{
      height: 34px;
      margin-top: 4px;
    }}
    .chat-left-section.chat-users-section.has-node-details.history-focus .chat-node-details-history-host .signal-legend {{
      margin-top: 4px;
      gap: 8px;
    }}
    .chat-member-item {{
      --chat-member-node-hue: var(--surface-tint-start-hue);
      --chat-member-node-tint-start-hue: var(--surface-tint-start-hue);
      --chat-member-node-tint-end-hue: var(--surface-tint-end-hue);
      --chat-member-node-sat-mult: 0;
      --chat-member-node-light-shift: 0%;
      --chat-member-node-outline-hue: var(--surface-tint-outline-hue);
      --chat-member-node-outline-sat-mult: 0;
      --chat-member-node-outline-light-shift: -3%;
      --chat-member-node-dark-sat-mult: 0;
      --chat-member-node-dark-light-shift: 0%;
      --chat-member-node-outline-dark-sat-mult: 0;
      --chat-member-node-outline-dark-light-shift: 4%;
      --chat-member-node-bg: var(--workspace-shell-bg);
      --chat-member-node-bg-hover: var(--workspace-shell-hover-bg);
      --chat-member-node-gradient: linear-gradient(transparent, transparent);
      --chat-member-node-gradient-hover: linear-gradient(transparent, transparent);
      --chat-member-node-border: var(--workspace-shell-border-muted);
      --chat-member-node-fg: var(--workspace-shell-text);
      --chat-member-node-selected-border: var(--workspace-shell-border-strong);
      display: grid;
      grid-template-columns: 18px minmax(0, 1fr);
      gap: 6px;
      align-items: center;
      border: 0;
      border-bottom: 1px solid var(--chat-member-node-border);
      background: var(--chat-member-node-gradient), var(--chat-member-node-bg);
      color: var(--chat-member-node-fg);
      border-radius: 0;
      padding: 4px 6px;
      font-size: 10px;
      line-height: 1.2;
      cursor: pointer;
    }}
    .chat-member-item.has-packet-spark {{
      grid-template-columns: 18px minmax(0, 1fr) 56px;
      align-items: stretch;
    }}
    .chat-member-item.status-hidden {{
      grid-template-columns: 0 minmax(0, 1fr);
      gap: 0;
    }}
    .chat-member-item.status-hidden.has-packet-spark {{
      grid-template-columns: 0 minmax(0, 1fr) 56px;
      gap: 3px;
    }}
    .chat-member-item.has-packet-spark .chat-member-status {{
      align-self: center;
    }}
    .chat-member-item:hover {{
      background: var(--chat-member-node-gradient-hover), var(--chat-member-node-bg-hover);
    }}
    .chat-member-item.status-warn {{
      background: var(--chat-member-node-gradient), var(--chat-member-node-bg);
      color: var(--chat-member-node-fg);
      border-color: var(--chat-member-node-border);
    }}
    .chat-member-item.status-stale {{
      background: var(--chat-member-node-gradient), var(--chat-member-node-bg);
      color: var(--chat-member-node-fg);
      border-color: var(--chat-member-node-border);
    }}
    .chat-member-item.status-unknown {{
      background: var(--chat-member-node-gradient), var(--chat-member-node-bg);
      color: var(--chat-member-node-fg);
      border-color: var(--chat-member-node-border);
    }}
    .chat-member-item.muted-node {{
      background:
        linear-gradient(
          90deg,
          rgba(215, 102, 102, 0.20),
          rgba(215, 102, 102, 0.11) 58%,
          rgba(215, 102, 102, 0.04) 100%
        ),
        var(--chat-member-node-gradient),
        var(--chat-member-node-bg);
      border-color: rgba(162, 70, 70, 0.62);
    }}
    .chat-member-item.muted-node .chat-member-name,
    .chat-member-item.muted-node .chat-member-meta-row,
    .chat-member-item.muted-node .chat-member-fields-row,
    .chat-member-item.muted-node .chat-member-tag-chip {{
      opacity: 0.72;
    }}
    .chat-member-item.muted-node.selected-node {{
      border-color: rgba(162, 70, 70, 0.82);
      box-shadow:
        inset 2px 0 0 rgba(215, 102, 102, 0.52),
        inset 0 0 0 1px rgba(162, 70, 70, 0.42);
    }}
    .chat-member-item.selected-node {{
      border-color: var(--chat-member-node-selected-border);
      box-shadow:
        inset 2px 0 0 hsl(var(--chat-member-node-hue) calc(32% * var(--chat-member-node-sat-mult, 1)) calc(52% + var(--chat-member-node-light-shift, 0%)) / 0.46),
        inset 0 0 0 1px hsl(var(--chat-member-node-outline-hue) calc(34% * var(--chat-member-node-outline-sat-mult, 1)) calc(63% + var(--chat-member-node-outline-light-shift, 0%)) / 0.24);
    }}
    .chat-member-item.auto-new-node:not(.tagged-node) {{
      box-shadow:
        inset 2px 0 0 rgba(101, 190, 106, 0.66),
        inset 0 0 0 1px rgba(101, 190, 106, 0.16);
    }}
    .chat-member-item.auto-new-node.selected-node:not(.tagged-node) {{
      box-shadow:
        inset 2px 0 0 rgba(101, 190, 106, 0.76),
        inset 0 0 0 1px hsl(var(--chat-member-node-outline-hue) calc(34% * var(--chat-member-node-outline-sat-mult, 1)) calc(63% + var(--chat-member-node-outline-light-shift, 0%)) / 0.26);
    }}
    .chat-member-item.tagged-node {{
      box-shadow:
        inset 2px 0 0 var(--node-tag-color, var(--accent)),
        inset 0 0 0 1px color-mix(in srgb, var(--node-tag-color, var(--accent)) 18%, transparent);
    }}
    .chat-member-item.tagged-node.selected-node {{
      box-shadow:
        inset 2px 0 0 var(--node-tag-color, var(--accent)),
        inset 0 0 0 1px hsl(var(--chat-member-node-outline-hue) calc(34% * var(--chat-member-node-outline-sat-mult, 1)) calc(63% + var(--chat-member-node-outline-light-shift, 0%)) / 0.26);
    }}
    #chat-room-pinned-shell {{
      border-left: 2px solid color-mix(in srgb, var(--surface-tint-border-strong) 76%, var(--surface-tint-text-soft) 24%);
    }}
    #chat-room-pinned-list .chat-member-item {{
      position: relative;
    }}
    #chat-room-pinned-list .chat-member-item::before {{
      content: "";
      position: absolute;
      left: 0;
      top: 1px;
      bottom: 1px;
      width: 2px;
      border-radius: 2px;
      background: color-mix(in srgb, var(--surface-tint-border-strong) 72%, transparent);
      pointer-events: none;
    }}
    #chat-room-pinned-list .chat-member-item:not(.muted-node) {{
      background: var(--chat-member-node-gradient), var(--chat-member-node-bg);
    }}
    #chat-room-pinned-list .chat-member-item:not(.muted-node):hover {{
      background: var(--chat-member-node-gradient-hover), var(--chat-member-node-bg-hover);
    }}
    .chat-member-status {{
      position: relative;
      width: 18px;
      min-width: 18px;
      height: 18px;
      text-align: center;
      font-size: 11px;
      font-weight: 700;
      line-height: 1;
      color: var(--accent);
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }}
    .chat-member-status.is-hidden {{
      visibility: hidden;
    }}
    .chat-member-status-dot {{
      font-size: 11px;
      transform: translateY(-0.4px);
    }}
    .chat-member-status-new {{
      font-size: 11px;
      font-weight: 900;
      border: 1.5px solid currentColor;
      border-radius: 3px;
      background: color-mix(in srgb, var(--chat-member-node-bg, var(--panel)) 88%, transparent);
      box-sizing: border-box;
      text-shadow:
        0 0 2px color-mix(in srgb, var(--chat-member-node-bg, var(--panel)) 86%, #000000 14%),
        0 0 3px color-mix(in srgb, currentColor 38%, transparent);
      transform: translateY(-0.2px);
    }}
    .chat-member-status-new-text {{
      display: block;
      color: #ffffff;
      font-family: "IBM Plex Mono", "Roboto Mono", monospace;
      font-size: 11px;
      font-weight: 900;
      line-height: 1;
      letter-spacing: 0;
      text-shadow:
        0 0 2px rgba(0, 0, 0, 0.95),
        0 0 4px currentColor;
      transform: translateY(-0.1px);
    }}
    .chat-member-status-emoji {{
      font-size: 13px;
      font-weight: 500;
      isolation: isolate;
    }}
    .chat-member-status-ring {{
      position: absolute;
      inset: 0;
      border-radius: 999px;
      border: 1.5px solid currentColor;
      background: color-mix(in srgb, var(--chat-member-node-bg, var(--panel)) 88%, transparent);
      box-sizing: border-box;
      opacity: 0.96;
      z-index: 0;
    }}
    .chat-member-status-emoji-glyph {{
      position: relative;
      z-index: 1;
      line-height: 1;
      transform: translateY(0.2px);
    }}
    @keyframes chat-member-trace-ring-spin {{
      to {{
        transform: rotate(1turn);
      }}
    }}
    .chat-member-item.is-traceroute-running .chat-member-status::after {{
      content: "";
      position: absolute;
      inset: -1px;
      border-radius: 999px;
      border: 1.5px solid currentColor;
      border-right-color: transparent;
      border-bottom-color: transparent;
      box-sizing: border-box;
      pointer-events: none;
      animation: chat-member-trace-ring-spin 900ms linear infinite;
      opacity: 0.92;
      z-index: 2;
    }}
    .chat-member-item.is-traceroute-running .chat-member-status.chat-member-status-emoji::after {{
      display: none;
    }}
    .chat-member-item.is-traceroute-running .chat-member-status.chat-member-status-emoji .chat-member-status-ring {{
      border-right-color: transparent;
      border-bottom-color: transparent;
      animation: chat-member-trace-ring-spin 900ms linear infinite;
    }}
    .chat-member-status.status-online {{
      color: #2aa85a;
    }}
    .chat-member-status.status-warn {{
      color: #d2a022;
    }}
    .chat-member-status.status-stale {{
      color: #d15757;
    }}
    .chat-member-status.status-unknown {{
      color: #99aaa0;
    }}
    .chat-member-main {{
      display: flex;
      flex-direction: column;
      min-width: 0;
      gap: 1px;
    }}
    .chat-member-name-row {{
      display: flex;
      align-items: center;
      gap: 6px;
      min-width: 0;
      max-width: 100%;
      width: 100%;
    }}
    .chat-member-name-left {{
      display: flex;
      align-items: center;
      gap: 5px;
      min-width: 0;
      flex: 1 1 auto;
      max-width: 100%;
    }}
    .chat-member-peer-badge {{
      border: 1px solid var(--surface-tint-border);
      background: color-mix(in srgb, var(--surface-tint-bg-alt) 82%, transparent);
      color: var(--surface-tint-text);
      border-radius: 999px;
      padding: 1px 5px;
      font-size: 8px;
      line-height: 1;
      text-transform: uppercase;
      letter-spacing: 0.2px;
      flex: 0 0 auto;
    }}
    .favorite-toggle-btn,
    .mute-toggle-btn {{
      border: 1px solid color-mix(in srgb, var(--surface-tint-border) 70%, transparent);
      background: transparent;
      color: var(--surface-tint-text-soft);
      border-radius: 999px;
      min-width: 16px;
      height: 16px;
      padding: 0 3px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      flex: 0 0 auto;
      line-height: 1;
      font-size: 9px;
      opacity: 0.76;
      transition: color 120ms ease, border-color 120ms ease, background 120ms ease, opacity 120ms ease;
    }}
    .favorite-toggle-btn {{
      --node-tag-color: var(--accent);
      --node-tag-vibrance: 0.7;
      padding: 0 4px;
    }}
    .favorite-toggle-btn:hover,
    .mute-toggle-btn:hover {{
      background: color-mix(in srgb, var(--surface-tint-bg-hover) 74%, transparent);
      border-color: color-mix(in srgb, var(--surface-tint-border-strong) 72%, transparent);
      color: var(--surface-tint-text);
      opacity: 1;
    }}
    .favorite-toggle-btn.active {{
      border-color: color-mix(in srgb, var(--node-tag-color, var(--surface-tint-border-strong)) 76%, var(--surface-tint-border-strong) 24%);
      background: color-mix(in srgb, var(--node-tag-color, var(--surface-tint-border-strong)) 18%, var(--surface-tint-bg-alt) 82%);
      color: var(--surface-tint-text);
      opacity: 0.95;
    }}
    .mute-toggle-btn.active {{
      border-color: rgba(162, 70, 70, 0.78);
      background: rgba(215, 102, 102, 0.22);
      color: #7b2020;
      opacity: 0.95;
    }}
    .favorite-toggle-icon {{
      line-height: 1;
      transform: none;
    }}
    .favorite-toggle-icon.node-tag-dot {{
      width: 8px;
      height: 8px;
      border-radius: 999px;
      border: 1px solid color-mix(in srgb, var(--surface-tint-text-soft) 36%, transparent);
      background: var(--node-tag-color, var(--accent));
      filter: saturate(calc(0.4 + (var(--node-tag-vibrance, 0.7) * 1.7)));
      box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.28);
      display: inline-block;
    }}
    .favorite-toggle-btn:not(.active) .favorite-toggle-icon.node-tag-dot {{
      background: transparent;
      border-style: dashed;
      border-color: currentColor;
      filter: none;
      box-shadow: none;
    }}
    .mute-toggle-icon {{
      line-height: 1;
      transform: translateY(-0.5px);
      font-size: 8px;
    }}
    .node-favorite-btn {{
      min-width: 16px;
      height: 16px;
      font-size: 9px;
    }}
    .chat-member-actions {{
      display: inline-flex;
      align-items: center;
      gap: 4px;
      margin-left: auto;
      flex: 0 0 auto;
    }}
    .chat-member-expand-btn {{
      border: 1px solid color-mix(in srgb, var(--surface-tint-border) 74%, transparent);
      background: transparent;
      color: var(--surface-tint-text-soft);
      border-radius: 999px;
      min-width: 15px;
      height: 15px;
      padding: 0;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      font-size: 9px;
      line-height: 1;
      opacity: 0.82;
    }}
    .chat-member-expand-btn:hover {{
      background: color-mix(in srgb, var(--surface-tint-bg-hover) 78%, transparent);
      border-color: var(--surface-tint-border-strong);
      color: var(--surface-tint-text);
      opacity: 1;
    }}
    .chat-member-expand-btn[aria-expanded="true"] {{
      background: color-mix(in srgb, var(--surface-tint-bg-hover) 88%, var(--surface-tint-border-strong) 12%);
      border-color: var(--surface-tint-border-strong);
      color: var(--surface-tint-text);
      opacity: 1;
    }}
    .chat-member-action-row {{
      display: flex;
      align-items: center;
      margin-top: 2px;
      min-width: 0;
    }}
    .chat-member-dm-link {{
      border: 0;
      background: transparent;
      color: color-mix(in srgb, var(--surface-tint-text) 74%, var(--surface-tint-border-strong) 26%);
      font-size: 10px;
      line-height: 1.2;
      text-decoration: underline;
      text-underline-offset: 2px;
      padding: 0;
      margin: 0;
      cursor: pointer;
    }}
    .chat-member-dm-link:hover {{
      color: var(--surface-tint-text);
    }}
    .chat-member-dm-link:focus-visible {{
      outline: 1px solid var(--surface-tint-border-strong);
      outline-offset: 2px;
      border-radius: 4px;
    }}
    .chat-peer-remove-btn {{
      border: 1px solid color-mix(in srgb, var(--surface-tint-border) 76%, transparent);
      background: transparent;
      color: var(--surface-tint-text-soft);
      border-radius: 999px;
      min-width: 15px;
      height: 15px;
      padding: 0;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      font-size: 10px;
      line-height: 1;
      opacity: 0.78;
    }}
    .chat-peer-remove-btn:hover {{
      background: rgba(209, 89, 89, 0.12);
      border-color: rgba(167, 68, 68, 0.56);
      color: #8d3a3a;
      opacity: 1;
    }}
    .favorites-card-star {{
      min-width: 18px;
      height: 18px;
      font-size: 9px;
      padding: 0 5px;
    }}
    .node-name-row .favorite-toggle-btn {{
      margin-left: 0;
      justify-self: end;
    }}
    .chat-member-name {{
      flex: 1 1 auto;
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      max-width: 100%;
      color: inherit;
    }}
    .chat-member-city {{
      flex: 0 1 auto;
      min-width: 0;
      max-width: 118px;
      overflow: hidden;
      color: var(--surface-tint-text-soft);
      font-size: 9px;
      line-height: 1.2;
      opacity: 0.76;
      text-overflow: ellipsis;
      white-space: nowrap;
    }}
    .chat-member-city.is-estimated {{
      color: color-mix(in srgb, #b57024 68%, var(--surface-tint-text-soft) 32%);
      font-style: italic;
    }}
    .chat-member-city[hidden] {{
      display: none;
    }}
    .chat-member-tag-chip {{
      --node-tag-color: var(--accent);
      --node-tag-vibrance: 0.7;
      flex: 0 0 auto;
      border: 1px solid var(--node-tag-color, var(--accent));
      color: var(--node-tag-color, var(--accent));
      background: color-mix(in srgb, var(--surface-tint-bg-alt) 74%, transparent);
      border-radius: 999px;
      padding: 0 5px;
      font-size: 8px;
      line-height: 1.35;
      letter-spacing: 0.14px;
      text-transform: uppercase;
      filter: saturate(calc(0.5 + (var(--node-tag-vibrance, 0.7) * 1.5)));
      white-space: nowrap;
      max-width: 96px;
      overflow: hidden;
      text-overflow: ellipsis;
    }}
    .chat-member-alert-chip {{
      flex: 0 0 auto;
      border: 1px solid rgba(191, 128, 34, 0.72);
      color: #7e520e;
      background: rgba(255, 244, 216, 0.88);
      border-radius: 999px;
      padding: 0 5px;
      font-size: 8px;
      line-height: 1.35;
      letter-spacing: 0.14px;
      text-transform: uppercase;
      white-space: nowrap;
    }}
    .chat-member-task-progress {{
      --chat-member-task-progress: 0%;
      position: relative;
      display: block;
      width: 100%;
      height: 12px;
      margin-top: 2px;
      border-radius: 999px;
      overflow: hidden;
      border: 1px solid rgba(69, 128, 188, 0.42);
      background: rgba(217, 236, 252, 0.20);
      box-sizing: border-box;
      isolation: isolate;
    }}
    .chat-member-task-progress-fill {{
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: var(--chat-member-task-progress);
      max-width: 100%;
      border-radius: inherit;
      background: linear-gradient(
        90deg,
        rgba(95, 163, 233, 0.60) 0%,
        rgba(68, 145, 223, 0.82) 100%
      );
      transition: width 180ms linear;
      pointer-events: none;
      z-index: 0;
    }}
    .chat-member-task-progress-text {{
      position: absolute;
      inset: 0;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 8px;
      line-height: 1;
      letter-spacing: 0.12px;
      text-transform: uppercase;
      color: #1f4c78;
      text-shadow: 0 1px 0 rgba(255, 255, 255, 0.28);
      pointer-events: none;
      z-index: 1;
    }}
    .chat-member-id {{
      font-size: 9px;
      color: var(--surface-tint-text-soft);
      opacity: 0.46;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      max-width: 100%;
    }}
    .chat-member-idle-time {{
      font-size: 9px;
      color: var(--surface-tint-text-soft);
      opacity: 0.72;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      max-width: 100%;
      min-width: 0;
      flex: 1 1 auto;
    }}
    .chat-member-hops-inline {{
      flex: 0 0 auto;
      font-size: 9px;
      color: var(--surface-tint-text-soft);
      opacity: 0.72;
      white-space: nowrap;
      text-align: right;
    }}
    .chat-member-snr-inline {{
      flex: 0 0 auto;
      font-size: 9px;
      color: var(--surface-tint-text-soft);
      opacity: 0.72;
      white-space: nowrap;
      text-align: right;
    }}
    .chat-member-link-quality {{
      flex: 0 0 auto;
      margin-left: auto;
      display: inline-flex;
      align-items: flex-end;
      gap: 5px;
      min-width: 0;
      white-space: nowrap;
      user-select: none;
    }}
    .chat-member-link-quality-label {{
      font-size: 8px;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      color: var(--surface-tint-text-soft);
      opacity: 0.72;
      line-height: 1;
      transform: translateY(-1px);
    }}
    .chat-member-link-quality-bars {{
      display: inline-flex;
      align-items: flex-end;
      gap: 2px;
      height: 12px;
      width: 22px;
    }}
    .chat-member-link-quality-bar {{
      width: 3px;
      border-radius: 1px 1px 0 0;
      background: color-mix(in srgb, var(--surface-tint-text-soft) 34%, transparent);
      transition: background-color 120ms ease, box-shadow 120ms ease, opacity 120ms ease;
      opacity: 0.72;
    }}
    .chat-member-link-quality-bar.level-1 {{ height: 4px; }}
    .chat-member-link-quality-bar.level-2 {{ height: 7px; }}
    .chat-member-link-quality-bar.level-3 {{ height: 10px; }}
    .chat-member-link-quality-bar.level-4 {{ height: 12px; }}
    .chat-member-link-quality.is-none .chat-member-link-quality-bar.is-on {{
      background: color-mix(in srgb, var(--surface-tint-text-soft) 72%, transparent);
      opacity: 0.7;
    }}
    .chat-member-link-quality.is-weak .chat-member-link-quality-bar.is-on {{
      background: #d38a58;
      box-shadow: 0 0 0 1px rgba(211, 138, 88, 0.14);
      opacity: 0.94;
    }}
    .chat-member-link-quality.is-fair .chat-member-link-quality-bar.is-on {{
      background: #d8c56a;
      box-shadow: 0 0 0 1px rgba(176, 162, 76, 0.16);
      opacity: 0.95;
    }}
    .chat-member-link-quality.is-good .chat-member-link-quality-bar.is-on {{
      background: #7ccf87;
      box-shadow: 0 0 0 1px rgba(98, 184, 111, 0.16);
      opacity: 0.96;
    }}
    .chat-member-link-quality.is-excellent .chat-member-link-quality-bar.is-on {{
      background: #3ce27b;
      box-shadow: 0 0 0 1px rgba(52, 206, 105, 0.2);
      opacity: 1;
    }}
    .chat-member-meta-row {{
      display: flex;
      align-items: center;
      gap: 6px;
      min-width: 0;
      width: 100%;
    }}
    .chat-member-fields-row {{
      display: flex;
      flex-wrap: wrap;
      gap: 4px;
      margin-top: 2px;
      min-width: 0;
      width: 100%;
    }}
    .chat-member-metadata-chip {{
      border: 1px solid color-mix(in srgb, var(--surface-tint-border) 58%, transparent);
      background: color-mix(in srgb, var(--surface-tint-bg-soft) 62%, transparent);
      border-radius: 999px;
      padding: 1px 6px;
      font-size: 9px;
      line-height: 1.25;
      display: inline-flex;
      align-items: baseline;
      gap: 4px;
      color: var(--surface-tint-text-soft);
      min-width: 0;
      max-width: 100%;
    }}
    .chat-member-metadata-key {{
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.01em;
      opacity: 0.78;
      white-space: nowrap;
    }}
    .chat-member-metadata-value {{
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      max-width: 118px;
    }}
    .chat-member-packet-trend.metric-ticker {{
      margin-top: 0;
      min-height: 0;
      gap: 0;
      margin-left: 0;
      flex: 1 1 auto;
      width: 100%;
      height: 100%;
      display: flex;
      align-items: stretch;
      justify-content: center;
    }}
    .chat-member-packet-spark-wrap {{
      display: flex;
      align-items: stretch;
      justify-content: center;
      min-width: 56px;
      min-height: 100%;
      margin-left: 0;
      align-self: stretch;
    }}
    .chat-member-packet-sparkline.metric-ticker-chart {{
      width: 56px;
      height: 100%;
      opacity: 1;
    }}
    .chat-member-packet-trend.metric-ticker {{
      color: #009E73;
    }}
    .chat-member-packet-trend.metric-ticker.trend-up {{
      color: #009E73;
    }}
    .chat-member-packet-trend.metric-ticker.trend-flat {{
      color: #56B4E9;
    }}
    .chat-member-packet-trend.metric-ticker.trend-down {{
      color: #D55E00;
    }}
    .chat-member-packet-trend.metric-ticker .chat-member-packet-sparkline-raw {{
      fill: none;
      stroke: color-mix(in srgb, currentColor 28%, transparent);
      stroke-width: 1.1;
      stroke-linecap: round;
      stroke-linejoin: round;
      vector-effect: non-scaling-stroke;
    }}
    .chat-member-packet-trend.metric-ticker .chat-member-packet-sparkline-trend {{
      fill: none;
      stroke: currentColor;
      stroke-width: 1.9;
      stroke-linecap: round;
      stroke-linejoin: round;
      vector-effect: non-scaling-stroke;
    }}
    .workspace-shell.chat-panel-collapsed .chat-left-panel .chat-member-list {{
      padding: 0;
    }}
    .workspace-shell.chat-panel-collapsed .chat-member-item {{
      grid-template-columns: 10px minmax(0, 1fr);
      gap: 4px;
      padding: 5px 4px;
    }}
    .workspace-shell.chat-panel-collapsed .chat-member-item.has-packet-spark {{
      grid-template-columns: 10px minmax(0, 1fr);
    }}
    .workspace-shell.chat-panel-collapsed .chat-member-name-row,
    .workspace-shell.chat-panel-collapsed .chat-member-name-left {{
      gap: 0;
    }}
    .workspace-shell.chat-panel-collapsed .chat-member-name {{
      font-size: 10px;
    }}
    .workspace-shell.chat-panel-collapsed .chat-member-actions,
    .workspace-shell.chat-panel-collapsed .chat-member-meta-row,
    .workspace-shell.chat-panel-collapsed .chat-member-fields-row,
    .workspace-shell.chat-panel-collapsed .chat-member-task-progress,
    .workspace-shell.chat-panel-collapsed .chat-member-packet-spark-wrap,
    .workspace-shell.chat-panel-collapsed .chat-member-tag-chip,
    .workspace-shell.chat-panel-collapsed .chat-member-alert-chip {{
      display: none !important;
    }}
    .chat-main-pane {{
      display: grid;
      grid-template-rows: minmax(0, 1fr) auto;
      row-gap: 0;
      flex: 1 1 auto;
      min-height: 0;
      overflow: hidden;
    }}
    .self-radio-menu {{
      position: absolute;
      top: calc(100% + 8px);
      right: 0;
      width: min(240px, 72vw);
      border: 1px solid #ccddcf;
      border-radius: 10px;
      background: #f4fbf5;
      padding: 7px 8px;
      display: flex;
      flex-direction: column;
      gap: 6px;
      box-shadow: 0 10px 28px rgba(21, 47, 33, 0.24);
      z-index: 45;
    }}
    .self-radio-menu[hidden] {{
      display: none !important;
    }}
    .self-radio-menu-item {{
      border: 1px solid #ccddcf;
      background: #ffffff;
      color: #274935;
      border-radius: 8px;
      padding: 6px 8px;
      text-align: left;
      font-size: 11px;
      line-height: 1.25;
      cursor: pointer;
      transition: border-color 120ms ease, background-color 120ms ease, color 120ms ease;
    }}
    .self-radio-menu-item:hover {{
      border-color: #95b6a2;
      background: #f3faf4;
      color: #1f4231;
    }}
    .self-radio-menu-item:focus-visible {{
      outline: 2px solid var(--surface-tint-border-strong);
      outline-offset: 2px;
    }}
    .self-radio-menu-item:disabled {{
      cursor: not-allowed;
      opacity: 0.66;
      border-color: #d8e5d8;
      color: #648073;
      background: #f3f8f4;
    }}
    .chat-change-menu {{
      position: absolute;
      top: calc(100% + 8px);
      right: 0;
      width: min(380px, 82vw);
      border: 1px solid #ccddcf;
      border-radius: 10px;
      background: #f4fbf5;
      padding: 7px 8px;
      display: flex;
      flex-direction: column;
      gap: 6px;
      box-shadow: 0 10px 28px rgba(21, 47, 33, 0.24);
      z-index: 45;
    }}
    .chat-change-menu[hidden] {{
      display: none !important;
    }}
    .chat-settings-menu {{
      position: absolute;
      top: calc(100% + 8px);
      right: 0;
      width: min(340px, 88vw);
      border: 1px solid #ccddcf;
      border-radius: 10px;
      background: #f4fbf5;
      padding: 8px 9px;
      display: flex;
      flex-direction: column;
      gap: 8px;
      box-shadow: 0 10px 28px rgba(21, 47, 33, 0.24);
      z-index: 45;
    }}
    .chat-settings-menu[hidden] {{
      display: none !important;
    }}
    .chat-settings-section {{
      display: flex;
      flex-direction: column;
      align-items: stretch;
      gap: 6px;
      min-width: 0;
    }}
    .chat-settings-section + .chat-settings-section {{
      border-top: 1px solid #d5e4d8;
      padding-top: 8px;
    }}
    .chat-settings-section-title {{
      margin: 0;
      font-size: 10px;
      line-height: 1.2;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.2px;
      color: var(--surface-tint-text-soft);
    }}
    .chat-settings-menu .theme-btn {{
      border: 1px solid #bfd4c3;
      background: #ffffff;
      color: #264634;
      border-radius: 999px;
      padding: 4px 10px;
      font-size: 11px;
      line-height: 1.2;
      cursor: pointer;
      white-space: nowrap;
    }}
    .chat-settings-menu .theme-btn:hover {{
      background: #f3faf4;
      border-color: #95b6a2;
      color: #1f4231;
    }}
    .chat-settings-theme-btn {{
      width: 100%;
      text-align: center;
    }}
    .chat-settings-menu .theme-preset-wrap {{
      display: grid;
      grid-template-columns: auto minmax(0, 1fr);
      align-items: center;
      gap: 8px;
      border: 1px solid #ccddcf;
      background: #ffffff;
      border-radius: 8px;
      padding: 6px 7px;
      color: #274935;
      white-space: normal;
    }}
    .chat-settings-menu .theme-preset-label {{
      font-size: 10px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.2px;
      color: var(--surface-tint-text-soft);
      opacity: 1;
    }}
    .chat-settings-menu .theme-preset-select {{
      border: 1px solid #bfd4c3;
      border-radius: 999px;
      background-color: #ffffff;
      color: #264634;
      font-size: 11px;
      line-height: 1.2;
      padding: 3px 22px 3px 9px;
      min-width: 0;
      width: 100%;
      cursor: pointer;
      appearance: none;
      -webkit-appearance: none;
      -moz-appearance: none;
      background-image:
        linear-gradient(45deg, transparent 50%, currentColor 50%),
        linear-gradient(135deg, currentColor 50%, transparent 50%);
      background-position:
        calc(100% - 12px) 9px,
        calc(100% - 7px) 9px;
      background-size: 5px 5px, 5px 5px;
      background-repeat: no-repeat;
      outline: none;
    }}
    .chat-settings-menu .theme-preset-select:hover {{
      border-color: #95b6a2;
      background-color: #f3faf4;
      color: #1f4231;
    }}
    .chat-settings-menu .theme-preset-select:focus-visible {{
      outline: 2px solid var(--surface-tint-border-strong);
      outline-offset: 2px;
    }}
    .chat-settings-menu .theme-preset-select:disabled {{
      cursor: default;
      opacity: 0.72;
    }}
    .chat-settings-meta-list {{
      display: grid;
      gap: 4px;
    }}
    .chat-settings-meta-item {{
      border: 1px solid #ccddcf;
      background: #ffffff;
      border-radius: 8px;
      padding: 4px 7px;
      font-size: 11px;
      line-height: 1.25;
      color: #274935;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }}
    .chat-settings-menu .disk-meter {{
      margin-left: 0;
      width: 100%;
      justify-content: space-between;
      border-color: #ccddcf;
      background: #ffffff;
      padding: 4px 8px;
    }}
    .chat-settings-menu .disk-label {{
      font-size: 11px;
      color: #274935;
    }}
    .chat-settings-menu .disk-track {{
      width: 132px;
      max-width: 48%;
      background: #d9e9de;
    }}
    .settings-system-disk-meter {{
      margin-left: 0;
      width: 100%;
      justify-content: space-between;
      border-color: #ccddcf;
      background: #ffffff;
      padding: 4px 8px;
    }}
    .settings-system-disk-meter .disk-label {{
      font-size: 11px;
      color: #274935;
    }}
    .settings-system-disk-meter .disk-track {{
      width: 132px;
      max-width: 48%;
      background: #d9e9de;
    }}
    .chat-settings-menu .chat-change-scope-wrap {{
      display: flex;
      flex-direction: column;
      align-items: stretch;
      gap: 6px;
      color: var(--surface-tint-text-soft);
      font-size: 10px;
      font-weight: 600;
      letter-spacing: 0.2px;
      text-transform: uppercase;
    }}
    .chat-settings-menu .chat-change-scope-select {{
      width: 100%;
    }}
    .chat-settings-menu .chat-bot-toggle-wrap {{
      display: inline-flex;
      align-items: center;
      gap: 7px;
      font-size: 11px;
      line-height: 1.25;
      color: #274935;
      cursor: pointer;
      user-select: none;
    }}
    .chat-settings-menu .chat-bot-toggle-wrap input[type="checkbox"] {{
      width: 14px;
      height: 14px;
      margin: 0;
      accent-color: var(--accent);
      cursor: pointer;
      flex: 0 0 auto;
    }}
    .chat-settings-menu .chat-bot-cheatsheet {{
      border: 1px solid #ccddcf;
      background: #ffffff;
      border-radius: 8px;
      padding: 6px 7px;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }}
    .chat-settings-menu .chat-bot-cheatsheet-title {{
      font-size: 10px;
      color: var(--surface-tint-text-soft);
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.2px;
    }}
    .chat-settings-menu .chat-bot-cheatsheet-row {{
      display: grid;
      grid-template-columns: auto minmax(0, 1fr);
      gap: 7px;
      align-items: baseline;
      font-size: 10px;
      line-height: 1.25;
      color: #2f4f3f;
    }}
    .chat-settings-menu .chat-bot-cheatsheet-row code {{
      font-family: "IBM Plex Mono", "Cascadia Mono", "Consolas", monospace;
      font-size: 10px;
      color: #1f4e35;
      background: #edf6ef;
      border: 1px solid #c9dece;
      border-radius: 6px;
      padding: 1px 5px;
      white-space: nowrap;
    }}
    .favorite-menu {{
      position: absolute;
      top: calc(100% + 8px);
      right: 0;
      width: min(320px, 88vw);
      border: 1px solid #ccddcf;
      border-radius: 10px;
      background: #f4fbf5;
      padding: 8px 9px;
      display: flex;
      flex-direction: column;
      gap: 7px;
      box-shadow: 0 10px 28px rgba(21, 47, 33, 0.24);
      z-index: 45;
    }}
    .favorite-menu[hidden] {{
      display: none !important;
    }}
    .favorite-menu-section {{
      display: flex;
      flex-direction: column;
      gap: 7px;
      min-width: 0;
    }}
    .favorite-menu-title {{
      margin: 0;
      font-size: 10px;
      line-height: 1.2;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.2px;
      color: var(--surface-tint-text-soft);
    }}
    .favorite-menu-saved-list {{
      display: flex;
      flex-direction: column;
      gap: 3px;
      max-height: min(42vh, 280px);
      overflow: auto;
      border: 1px solid #ccddcf;
      border-radius: 8px;
      background: #ffffff;
      padding: 4px;
    }}
    .favorite-menu-selected-list {{
      max-height: none;
      overflow: visible;
    }}
    .favorite-menu-node-search-wrap {{
      position: relative;
      width: 100%;
      min-width: 0;
    }}
    .favorite-menu-node-search-dropdown {{
      z-index: 56;
    }}
    .favorite-menu-saved-empty {{
      font-size: 11px;
      color: #5a7464;
      border: 1px dashed #c9dccc;
      border-radius: 6px;
      padding: 8px;
      text-align: center;
    }}
    .favorite-menu-saved-item {{
      border: 1px solid transparent;
      border-radius: 6px;
      background: transparent;
      color: #274935;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      text-align: left;
      padding: 5px 7px;
      cursor: pointer;
      width: 100%;
      font-size: 11px;
      line-height: 1.25;
    }}
    .favorite-menu-saved-item:hover {{
      border-color: #b6d4c1;
      background: var(--surface-tint-bg-soft);
      color: #1f4231;
    }}
    .favorite-menu-saved-item:focus-visible {{
      outline: 2px solid var(--surface-tint-border-strong);
      outline-offset: 1px;
    }}
    .favorite-menu-saved-item.active {{
      border-color: #98c3aa;
      background: #e4f3ea;
      color: #1b402d;
    }}
    .favorite-menu-selected-item {{
      border-color: #98c3aa;
      background: #e4f3ea;
      color: #1b402d;
    }}
    .favorite-menu-saved-main {{
      overflow: hidden;
      min-width: 0;
      display: flex;
      align-items: center;
      gap: 6px;
      flex: 1 1 auto;
    }}
    .favorite-menu-selected-stack {{
      display: flex;
      flex-direction: column;
      gap: 6px;
    }}
    .favorite-menu-tag-chip {{
      --node-tag-color: var(--accent);
      --node-tag-vibrance: 0.7;
      width: 10px;
      height: 10px;
      border-radius: 999px;
      border: 1px dashed rgba(99, 126, 112, 0.55);
      background: transparent;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      flex: 0 0 auto;
      overflow: hidden;
    }}
    .favorite-menu-tag-chip.is-tagged {{
      border-style: solid;
      border-color: rgba(62, 104, 81, 0.55);
      background: var(--node-tag-color, var(--accent));
      filter: saturate(calc(0.4 + (var(--node-tag-vibrance, 0.7) * 1.7)));
    }}
    .favorite-menu-tag-chip.has-emoji {{
      width: auto;
      min-width: 16px;
      height: 16px;
      padding: 0 3px;
    }}
    .favorite-menu-tag-chip.has-icon {{
      width: 17px;
      min-width: 17px;
      padding: 0;
    }}
    .favorite-menu-tag-chip-emoji {{
      display: inline-block;
      font-size: 11px;
      line-height: 1;
      transform: translateY(-0.5px);
    }}
    .favorite-menu-tag-chip-icon {{
      width: 13px;
      height: 13px;
      fill: #07130c;
    }}
    .favorite-menu-selected-chip {{
      margin-right: 2px;
    }}
    .favorite-menu-tag-chip-preview {{
      width: 12px;
      height: 12px;
      margin-left: auto;
    }}
    .favorite-menu-tag-editor {{
      border: 1px solid #c5d9ca;
      border-radius: 8px;
      background: #f7fcf8;
      padding: 7px;
      display: flex;
      flex-direction: column;
      gap: 6px;
    }}
    .favorite-menu-tag-editor-row {{
      display: grid;
      grid-template-columns: 48px minmax(0, 1fr);
      gap: 6px;
      align-items: center;
      min-width: 0;
    }}
    .favorite-menu-tag-editor-row-inline {{
      grid-template-columns: auto auto auto minmax(0, 1fr) auto;
      gap: 6px;
    }}
    .favorite-menu-tag-editor-label {{
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.2px;
      color: #4e6658;
      white-space: nowrap;
    }}
    .favorite-menu-tag-preset-select,
    .favorite-menu-tag-editor-input {{
      width: 100%;
      min-width: 0;
      border: 1px solid #c7d8ca;
      border-radius: 7px;
      background: #ffffff;
      color: #204332;
      font-size: 11px;
      padding: 4px 6px;
    }}
    .favorite-menu-tag-color-input {{
      width: 28px;
      min-width: 28px;
      height: 22px;
      border: 1px solid #c7d8ca;
      border-radius: 6px;
      background: #ffffff;
      padding: 1px;
    }}
    .favorite-menu-tag-vibrance-input {{
      width: 100%;
      min-width: 0;
      margin: 0;
      accent-color: var(--ui-accent, var(--accent));
    }}
    .favorite-menu-tag-vibrance-value {{
      font-size: 10px;
      color: #446151;
      min-width: 38px;
      text-align: right;
      font-family: ui-monospace, "SFMono-Regular", Menlo, Consolas, monospace;
    }}
    .favorite-menu-tag-editor-actions {{
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
    }}
    .favorite-menu-tag-editor-actions .btn {{
      min-height: 0;
      padding: 3px 8px;
      font-size: 10px;
    }}
    .favorite-menu-tag-status {{
      min-height: 13px;
      font-size: 10px;
      color: #496556;
      line-height: 1.2;
    }}
    .favorite-menu-selected-prefix {{
      display: inline-block;
      font-size: 9px;
      line-height: 1;
      font-weight: 700;
      letter-spacing: 0.35px;
      text-transform: uppercase;
      color: #2f6e4c;
      background: #d7ecdf;
      border: 1px solid #9fc2ad;
      border-radius: 999px;
      padding: 2px 6px;
      flex: 0 0 auto;
    }}
    .favorite-menu-saved-name {{
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-weight: 600;
      min-width: 0;
      flex: 1 1 auto;
    }}
    .favorite-menu-saved-id {{
      color: #5a7464;
      font-size: 10px;
      line-height: 1.2;
      white-space: nowrap;
      font-family: ui-monospace, "SFMono-Regular", Menlo, Consolas, monospace;
      flex: 0 0 auto;
    }}
    .chat-change-head {{
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
    }}
    .chat-change-title {{
      font-size: 11px;
      line-height: 1.2;
      color: #244733;
      font-weight: 600;
    }}
    .chat-change-clear-btn {{
      border: 1px solid #b8d0bf;
      background: #ffffff;
      color: #28533c;
      border-radius: 999px;
      padding: 2px 8px;
      font-size: 10px;
      line-height: 1.3;
      cursor: pointer;
      white-space: nowrap;
    }}
    .chat-change-clear-btn:hover {{
      background: #eaf5ed;
      border-color: #94b7a0;
    }}
    .chat-change-list {{
      display: flex;
      flex-direction: column;
      gap: 4px;
      max-height: min(44vh, 320px);
      overflow: auto;
      padding-right: 2px;
    }}
    .chat-change-empty {{
      border: 1px dashed #c5d7ca;
      border-radius: 8px;
      padding: 10px 9px;
      color: #4f6a5b;
      font-size: 11px;
      line-height: 1.35;
      text-align: center;
      background: var(--panel);
    }}
    .chat-change-item {{
      border: 1px solid #d3e3d6;
      background: var(--panel);
      color: #214836;
      border-radius: 8px;
      padding: 4px 6px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      text-align: left;
      width: 100%;
      cursor: pointer;
      font-size: 11px;
      line-height: 1.25;
    }}
    .chat-change-item:hover {{
      background: #edf6ef;
      border-color: #a8c6b0;
    }}
    .chat-change-item-main {{
      display: inline-flex;
      align-items: center;
      gap: 6px;
      min-width: 0;
      flex: 1 1 auto;
    }}
    .chat-change-kind {{
      border-radius: 999px;
      border: 1px solid #c6dccb;
      background: #eef7f0;
      color: #345d47;
      padding: 1px 6px;
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.25px;
      white-space: nowrap;
      flex: 0 0 auto;
    }}
    .chat-change-kind.reaction {{
      border-color: #d7cda9;
      background: #fdf7e7;
      color: #6e5521;
    }}
    .chat-change-kind.status {{
      border-color: #b6cfe2;
      background: #eaf3fb;
      color: #245173;
    }}
    .chat-change-item-text {{
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }}
    .chat-change-item-age {{
      color: #5c7868;
      font-size: 10px;
      line-height: 1.2;
      white-space: nowrap;
      flex: 0 0 auto;
    }}
    .peer-dm-popout-host {{
      position: fixed;
      right: max(12px, env(safe-area-inset-right));
      bottom: max(12px, env(safe-area-inset-bottom));
      z-index: 1300;
      pointer-events: none;
      display: flex;
      align-items: flex-end;
      justify-content: flex-end;
      width: min(420px, calc(100vw - 24px));
      max-width: calc(100vw - 24px);
      min-height: 0;
    }}
    .peer-dm-popout-window {{
      --peer-dm-channel-fill: #8da3b1;
      --peer-dm-channel-rgb: 141, 163, 177;
      pointer-events: auto;
      width: min(400px, calc(100vw - 24px));
      max-width: calc(100vw - 24px);
      max-height: min(520px, calc(100vh - 24px));
      border: 1px solid rgba(var(--peer-dm-channel-rgb), 0.44);
      border-radius: 12px;
      background:
        linear-gradient(
          180deg,
          rgba(var(--peer-dm-channel-rgb), 0.17) 0%,
          rgba(var(--peer-dm-channel-rgb), 0.08) 42%,
          rgba(var(--peer-dm-channel-rgb), 0.03) 100%
        ),
        #f6fcf7;
      box-shadow: 0 16px 34px rgba(21, 47, 33, 0.3);
      display: flex;
      flex-direction: column;
      overflow: hidden;
    }}
    .peer-dm-popout-window.is-embedded {{
      width: 100%;
      max-width: none;
      max-height: none;
      height: 100%;
      min-height: 0;
      border-radius: 10px;
      box-shadow: none;
    }}
    .peer-dm-popout-head {{
      border-bottom: 1px solid rgba(var(--peer-dm-channel-rgb), 0.35);
      background:
        linear-gradient(
          180deg,
          rgba(var(--peer-dm-channel-rgb), 0.22) 0%,
          rgba(var(--peer-dm-channel-rgb), 0.1) 100%
        ),
        #edf6ef;
      padding: 7px 9px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      min-width: 0;
    }}
    .peer-dm-popout-title {{
      min-width: 0;
      display: flex;
      flex-direction: column;
      gap: 1px;
    }}
    .peer-dm-popout-name {{
      font-size: 12px;
      font-weight: 700;
      color: #214836;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }}
    .peer-dm-popout-id {{
      font-size: 10px;
      color: #5a7464;
      font-family: ui-monospace, "SFMono-Regular", Menlo, Consolas, monospace;
    }}
    .peer-dm-popout-id-row {{
      display: inline-flex;
      align-items: center;
      gap: 6px;
      min-width: 0;
      flex-wrap: wrap;
    }}
    .peer-dm-popout-channel {{
      display: inline-flex;
      align-items: center;
      border: 1px solid rgba(var(--peer-dm-channel-rgb), 0.45);
      background: rgba(var(--peer-dm-channel-rgb), 0.16);
      color: #244936;
      border-radius: 999px;
      padding: 1px 7px;
      font-size: 9px;
      line-height: 1.2;
      font-weight: 700;
      letter-spacing: 0.02em;
      white-space: nowrap;
    }}
    .peer-dm-popout-actions {{
      display: inline-flex;
      align-items: center;
      gap: 6px;
      flex: 0 0 auto;
    }}
    .peer-dm-popout-btn {{
      border: 1px solid #bfd4c3;
      background: #ffffff;
      color: #28533c;
      border-radius: 999px;
      padding: 3px 9px;
      font-size: 10px;
      line-height: 1.2;
      cursor: pointer;
      white-space: nowrap;
    }}
    .peer-dm-popout-btn:hover {{
      background: #eaf5ed;
      border-color: #94b7a0;
    }}
    .peer-dm-popout-body {{
      flex: 0 1 auto;
      max-height: min(300px, calc(100vh - 180px));
      padding: 7px 8px;
      overflow: auto;
      background:
        linear-gradient(
          180deg,
          rgba(var(--peer-dm-channel-rgb), 0.1) 0%,
          rgba(var(--peer-dm-channel-rgb), 0.03) 46%,
          rgba(var(--peer-dm-channel-rgb), 0.01) 100%
        ),
        var(--panel);
      display: flex;
      flex-direction: column;
      gap: 6px;
      min-height: 0;
    }}
    .peer-dm-popout-window.is-embedded .peer-dm-popout-body {{
      flex: 1 1 auto;
      max-height: none;
    }}
    .peer-dm-popout-empty {{
      border: 1px dashed rgba(var(--peer-dm-channel-rgb), 0.42);
      border-radius: 8px;
      padding: 10px 9px;
      color: #4f6a5b;
      font-size: 11px;
      line-height: 1.35;
      text-align: center;
      background: rgba(var(--peer-dm-channel-rgb), 0.06);
    }}
    .peer-dm-popout-window.is-embedded .peer-dm-popout-empty {{
      min-height: 88px;
      display: flex;
      align-items: center;
      justify-content: center;
    }}
    .peer-dm-popout-msg {{
      display: inline-flex;
      flex-direction: column;
      gap: 3px;
      max-width: 85%;
      border: 1px solid rgba(var(--peer-dm-channel-rgb), 0.3);
      border-radius: 10px;
      background: rgba(var(--peer-dm-channel-rgb), 0.09);
      padding: 5px 7px;
      color: #214836;
      line-height: 1.3;
      word-break: break-word;
    }}
    .peer-dm-popout-msg.is-own {{
      margin-left: auto;
      border-color: rgba(var(--peer-dm-channel-rgb), 0.56);
      background: rgba(var(--peer-dm-channel-rgb), 0.2);
      color: #1f4231;
    }}
    .peer-dm-popout-msg.is-alert {{
      border-color: rgba(191, 66, 70, 0.54);
      background: rgba(191, 66, 70, 0.14);
      box-shadow: inset 2px 0 0 rgba(191, 66, 70, 0.72);
      color: #6d1f25;
    }}
    .peer-dm-popout-msg.is-own.is-alert {{
      border-color: rgba(191, 66, 70, 0.7);
      background: rgba(191, 66, 70, 0.18);
      color: #621b22;
    }}
    .peer-dm-popout-msg-meta {{
      font-size: 9px;
      color: #5c7868;
      line-height: 1.2;
      display: inline-flex;
      align-items: center;
      gap: 5px;
      white-space: nowrap;
    }}
    .peer-dm-popout-msg.is-alert .peer-dm-popout-msg-meta {{
      color: #8a3a40;
    }}
    .peer-dm-popout-alert-chip {{
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 13px;
      height: 13px;
      border-radius: 999px;
      margin-right: 4px;
      border: 1px solid rgba(191, 66, 70, 0.74);
      background: rgba(191, 66, 70, 0.16);
      color: #8f262c;
      font-size: 9px;
      line-height: 1;
      font-weight: 800;
      vertical-align: middle;
    }}
    .peer-dm-popout-msg-text {{
      font-size: 11px;
      line-height: 1.35;
    }}
    .peer-dm-popout-composer {{
      border-top: 1px solid rgba(var(--peer-dm-channel-rgb), 0.35);
      background:
        linear-gradient(
          180deg,
          rgba(var(--peer-dm-channel-rgb), 0.15) 0%,
          rgba(var(--peer-dm-channel-rgb), 0.08) 100%
        ),
        #f2f9f4;
      padding: 7px 8px;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 7px;
      align-items: center;
    }}
    .peer-dm-popout-input {{
      grid-column: 1;
      grid-row: 1;
      border: 1px solid rgba(var(--peer-dm-channel-rgb), 0.46);
      border-radius: 999px;
      background: #ffffff;
      color: #264634;
      padding: 5px 10px;
      font-size: 11px;
      line-height: 1.2;
      min-width: 0;
      width: 100%;
    }}
    .peer-dm-popout-input:focus-visible {{
      outline: 2px solid var(--surface-tint-border-strong);
      outline-offset: 1px;
    }}
    .peer-dm-popout-send-btn {{
      grid-column: 2;
      grid-row: 1;
      border: 1px solid #4b9a6d;
      background: #2f7f58;
      color: #ecfff4;
      border-radius: 999px;
      padding: 4px 10px;
      font-size: 11px;
      line-height: 1.2;
      cursor: pointer;
      white-space: nowrap;
      min-width: 56px;
    }}
    .peer-dm-popout-send-btn:hover {{
      background: #276e4c;
      border-color: #3f8e63;
    }}
    .peer-dm-popout-file-progress {{
      grid-column: 1 / -1;
      border: 1px solid rgba(var(--peer-dm-channel-rgb), 0.34);
      border-radius: 8px;
      background: rgba(var(--peer-dm-channel-rgb), 0.08);
      padding: 5px 7px;
      display: flex;
      flex-direction: column;
      gap: 4px;
      min-width: 0;
    }}
    .peer-dm-popout-file-progress[hidden] {{
      display: none !important;
    }}
    .peer-dm-popout-file-progress-head {{
      display: inline-flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      min-width: 0;
    }}
    .peer-dm-popout-file-progress-text {{
      font-size: 10px;
      line-height: 1.2;
      color: #3f604f;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      min-width: 0;
      flex: 1 1 auto;
    }}
    .peer-dm-popout-file-progress-pct {{
      font-size: 10px;
      line-height: 1.2;
      color: #31513f;
      font-weight: 700;
      flex: 0 0 auto;
    }}
    .peer-dm-popout-file-progress-meter {{
      height: 5px;
      border-radius: 999px;
      background: rgba(var(--peer-dm-channel-rgb), 0.23);
      overflow: hidden;
    }}
    .peer-dm-popout-file-progress-meter > span {{
      display: block;
      height: 100%;
      width: 0%;
      border-radius: inherit;
      background: #3a9263;
      transition: width 140ms ease;
    }}
    .peer-dm-popout-file-progress.is-error {{
      border-color: #e1b8b8;
      background: #fff5f5;
    }}
    .peer-dm-popout-file-progress.is-error .peer-dm-popout-file-progress-text,
    .peer-dm-popout-file-progress.is-error .peer-dm-popout-file-progress-pct {{
      color: #8d3a3a;
    }}
    .peer-dm-popout-file-progress.is-error .peer-dm-popout-file-progress-meter {{
      background: #f1d0d0;
    }}
    .peer-dm-popout-file-progress.is-error .peer-dm-popout-file-progress-meter > span {{
      background: #b24d4d;
    }}
    .peer-dm-popout-file-progress.is-success {{
      border-color: #a8d3b5;
      background: #ecf8ef;
    }}
    .peer-dm-popout-status {{
      grid-column: 1 / -1;
      min-height: 0;
      font-size: 10px;
      line-height: 1.2;
      color: #4f6f5d;
      padding: 0 4px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }}
    .peer-dm-popout-status[hidden] {{
      display: none !important;
    }}
    .peer-dm-popout-status.is-error {{
      color: #8d3a3a;
    }}
    @media (max-width: 900px) {{
      .peer-dm-popout-host {{
        right: 8px;
        bottom: max(8px, env(safe-area-inset-bottom));
        width: calc(100vw - 16px);
      }}
      .peer-dm-popout-window {{
        width: calc(100vw - 16px);
      }}
    }}
    .chat-log-scroll {{
      grid-row: 1;
      flex: 1 1 auto;
      max-height: none;
      min-height: 0;
      display: flex;
      flex-direction: column;
      padding: 0;
    }}
    .chat-log-scroll:not(.workspace-stack-list-shell) {{
      border: 0;
      border-radius: 0;
      background: transparent;
    }}
    .chat-feed {{
      display: flex;
      flex-direction: column;
      gap: 5px;
      width: 100%;
      min-height: 0;
      margin-top: auto;
      padding: 8px 8px 0 8px;
      box-sizing: border-box;
    }}
    .chat-feed:not(.chat-feed-view-monitor)::after {{
      content: "";
      flex: 0 0 5px;
      height: 5px;
      width: 100%;
    }}
    .chat-history-load-row {{
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      width: 100%;
      padding: 2px 0 4px 0;
      color: #5f7a6a;
      font-size: 10px;
      line-height: 1.2;
    }}
    .chat-history-load-btn {{
      border: 1px solid #9abda5;
      background: #eff8f1;
      color: #214434;
      border-radius: 999px;
      min-height: 24px;
      padding: 3px 10px;
      font-size: 10px;
      line-height: 1.2;
      cursor: pointer;
    }}
    .chat-history-load-btn:hover {{
      background: #e0f0e5;
    }}
    .chat-history-load-btn:disabled {{
      opacity: 0.7;
      cursor: default;
    }}
    .chat-history-load-detail {{
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }}
    .chat-history-load-error {{
      color: #9a3d3d;
    }}
    .chat-feed-item {{
      --chat-feed-node-emoji-tail-space: 0px;
      --chat-feed-node-emoji-tail-inset: 0px;
      --chat-feed-node-hue: var(--surface-tint-start-hue);
      --chat-feed-node-tint-start-hue: var(--surface-tint-start-hue);
      --chat-feed-node-tint-end-hue: var(--surface-tint-end-hue);
      --chat-feed-node-outline-hue: var(--surface-tint-outline-hue);
      --chat-feed-node-outline-sat-mult: 0;
      --chat-feed-node-outline-light-shift: -3%;
      --chat-feed-node-sat-mult: 0;
      --chat-feed-node-light-shift: 0%;
      --chat-feed-node-outline-dark-sat-mult: 0;
      --chat-feed-node-outline-dark-light-shift: 4%;
      --chat-feed-node-dark-sat-mult: 0;
      --chat-feed-node-dark-light-shift: 0%;
      --chat-feed-node-bg: color-mix(in srgb, var(--panel) 94%, var(--bg) 6%);
      --chat-feed-node-bg-hover: color-mix(in srgb, var(--panel) 84%, var(--bg) 16%);
      --chat-feed-border-color: color-mix(in srgb, var(--line) 88%, var(--ink) 12%);
      --chat-feed-channel-edge-bg: linear-gradient(90deg, transparent 0, transparent 100%);
      --chat-feed-node-gradient: linear-gradient(90deg, transparent 0, transparent 100%);
      --chat-feed-node-gradient-hover: linear-gradient(90deg, transparent 0, transparent 100%);
      width: fit-content;
      max-width: min(84%, 100%);
      min-width: 0;
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      position: relative;
      isolation: isolate;
      overflow: visible;
      border: 1px solid var(--chat-feed-border-color);
      border-radius: 8px;
      padding: 9px 12px;
      margin-right: auto;
      background: var(--chat-feed-channel-edge-bg), var(--chat-feed-node-gradient), var(--chat-feed-node-bg);
      background-origin: border-box;
      background-clip: border-box;
      color: color-mix(in srgb, var(--ink) 90%, var(--muted) 10%);
    }}
    .chat-feed-item.has-channel-tab {{
      --chat-feed-channel-edge-bg: linear-gradient(
        90deg,
        var(--chat-feed-channel-fill, #8da3b1) 0 4px,
        transparent 4px
      );
    }}
    .chat-feed-item.chat-feed-search-hidden {{
      display: none !important;
    }}
    .chat-feed-item.has-node-emoji {{
      --chat-feed-node-emoji-tail-space: 0px;
      --chat-feed-node-emoji-tail-inset: 0px;
      padding-right: 12px;
    }}
    .chat-feed-item.has-node-emoji.has-node-watermark-text {{
      --chat-feed-node-emoji-tail-space: 0px;
    }}
    .chat-feed-item.self-authored.has-node-emoji {{
      padding-left: 12px;
      padding-right: 12px;
    }}
    .chat-feed-item.has-node-emoji::after {{
      content: none;
      display: none;
    }}
    .chat-feed-item.has-node-emoji.has-node-watermark-text::after {{
      content: none;
      display: none;
    }}
    .chat-feed-item.self-authored.has-node-emoji::after {{
      left: var(--chat-feed-node-emoji-tail-inset);
      right: auto;
    }}
    .chat-feed-item.self-authored {{
      margin-left: auto;
      margin-right: 0;
      border-radius: 8px;
      background: var(--chat-feed-channel-edge-bg), var(--chat-feed-node-bg);
    }}
    .chat-feed-item.tagged-node {{
      background:
        var(--chat-feed-channel-edge-bg),
        linear-gradient(
          90deg,
          hsl(var(--chat-feed-node-hue) calc(58% * var(--chat-feed-node-sat-mult, 1)) calc(82% + var(--chat-feed-node-light-shift, 0%)) / calc(0.04 + var(--node-tag-vibrance, 0.7) * 0.10)),
          hsl(var(--chat-feed-node-hue) calc(54% * var(--chat-feed-node-sat-mult, 1)) calc(86% + var(--chat-feed-node-light-shift, 0%)) / calc(0.02 + var(--node-tag-vibrance, 0.7) * 0.055)) 34%,
          hsl(var(--chat-feed-node-hue) calc(50% * var(--chat-feed-node-sat-mult, 1)) calc(90% + var(--chat-feed-node-light-shift, 0%)) / 0) 68%
        ),
        var(--chat-feed-node-gradient),
        var(--chat-feed-node-bg);
    }}
    .chat-feed-item.tagged-node.self-authored {{
      background:
        var(--chat-feed-channel-edge-bg),
        linear-gradient(
          90deg,
          color-mix(in srgb, var(--accent) 4%, transparent),
          color-mix(in srgb, var(--accent) 2%, transparent) 28%,
          color-mix(in srgb, var(--accent) 1%, transparent) 52%
        ),
        linear-gradient(
          90deg,
          hsl(var(--chat-feed-node-hue) calc(58% * var(--chat-feed-node-sat-mult, 1)) calc(82% + var(--chat-feed-node-light-shift, 0%)) / calc(0.04 + var(--node-tag-vibrance, 0.7) * 0.10)),
          hsl(var(--chat-feed-node-hue) calc(54% * var(--chat-feed-node-sat-mult, 1)) calc(86% + var(--chat-feed-node-light-shift, 0%)) / calc(0.02 + var(--node-tag-vibrance, 0.7) * 0.055)) 34%,
          hsl(var(--chat-feed-node-hue) calc(50% * var(--chat-feed-node-sat-mult, 1)) calc(90% + var(--chat-feed-node-light-shift, 0%)) / 0) 68%
        ),
        var(--chat-feed-node-gradient),
        var(--chat-feed-node-bg);
    }}
    .chat-feed-item.chat-selectable {{
      cursor: pointer;
    }}
    .chat-feed-item.chat-selectable:hover {{
      background: var(--chat-feed-channel-edge-bg), var(--chat-feed-node-gradient-hover), var(--chat-feed-node-bg-hover);
    }}
    .chat-feed-item.chat-selectable.self-authored:hover {{
      background: var(--chat-feed-channel-edge-bg), var(--chat-feed-node-bg-hover);
    }}
    .chat-feed-item.has-change-marker {{
      background: var(--chat-feed-channel-edge-bg), #f9fcf8;
      transition: background-color 0.48s ease;
    }}
    .chat-feed-item.change-marker-fading.has-change-marker:not(.selected-node) {{
      background: var(--chat-feed-channel-edge-bg), transparent;
    }}
    .chat-feed-item.kind-status {{
      background:
        var(--chat-feed-channel-edge-bg),
        linear-gradient(90deg, rgba(225, 239, 248, 0.7), rgba(225, 239, 248, 0) 52%),
        var(--chat-feed-node-gradient),
        var(--chat-feed-node-bg);
    }}
    .chat-feed-item.kind-alert {{
      background:
        var(--chat-feed-channel-edge-bg),
        linear-gradient(90deg, rgba(191, 66, 70, 0.16), rgba(191, 66, 70, 0.08) 34%, rgba(191, 66, 70, 0.02) 62%, rgba(191, 66, 70, 0) 100%),
        var(--chat-feed-node-gradient),
        var(--chat-feed-node-bg);
    }}
    .chat-feed-item.kind-alert .chat-feed-author .chat-name {{
      color: #7f2328;
    }}
    .chat-feed-item.kind-alert .chat-feed-colon {{
      color: #934249;
    }}
    .chat-feed-item.change-focus {{
      animation: chat-change-focus-pulse 1.2s ease;
    }}
    @keyframes chat-change-focus-pulse {{
      0% {{
        background: #fff5da;
      }}
      100% {{
        background: transparent;
      }}
    }}
    [data-chat-view-mode="monitor"] .card.chat .chat-shell {{
      padding: 4px;
    }}
    [data-chat-view-mode="monitor"] .card.chat {{
      background: #191c22;
      border-color: #3a3f48;
      box-shadow: none;
    }}
    [data-chat-view-mode="monitor"] .card.chat .chat-main-pane {{
      row-gap: 2px;
    }}
    [data-chat-view-mode="monitor"] .card.chat .chat-compose-shell {{
      display: none;
    }}
    [data-chat-view-mode="monitor"] .card.chat .chat-log-scroll {{
      background: #20242b;
      border: 1px solid #3b404a;
      border-radius: 4px;
      box-shadow: none;
    }}
    .chat-feed.chat-feed-view-monitor {{
      font-family: ui-monospace, Menlo, Consolas, monospace;
      background: #20242b;
      gap: 0;
    }}
    .chat-feed.chat-feed-view-monitor .chat-feed-item {{
      width: 100%;
      max-width: 100%;
      margin-left: 0;
      margin-right: 0;
      padding: 4px 6px;
      overflow: visible;
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
      color: #d7dce3;
    }}
    .chat-feed.chat-feed-view-monitor .chat-feed-item.chat-selectable:hover {{
      background: rgba(90, 100, 115, 0.16);
    }}
    .chat-feed.chat-feed-view-monitor .chat-feed-item.selected-node,
    .chat-feed.chat-feed-view-monitor .chat-feed-item.chat-selectable.selected-node:hover {{
      background: rgba(112, 124, 141, 0.24);
      box-shadow: inset 2px 0 0 #8ea5c1;
    }}
    .chat-feed.chat-feed-view-monitor .chat-feed-item.self-authored,
    .chat-feed.chat-feed-view-monitor .chat-feed-item.has-change-marker,
    .chat-feed.chat-feed-view-monitor .chat-feed-item.kind-status {{
      background: transparent;
      box-shadow: none;
    }}
    .chat-feed.chat-feed-view-monitor .chat-feed-item.has-node-emoji::after {{
      content: none;
    }}
    .chat-feed.chat-feed-view-monitor .chat-feed-line {{
      gap: 0;
      align-items: baseline;
    }}
    .chat-feed.chat-feed-view-monitor .chat-feed-summary {{
      display: block;
      line-height: 1.45;
      white-space: normal;
    }}
    .chat-feed.chat-feed-view-monitor .chat-feed-side,
    .chat-feed.chat-feed-view-monitor .chat-reaction-summary-row,
    .chat-feed.chat-feed-view-monitor .chat-reaction-row,
    .chat-feed.chat-feed-view-monitor .chat-feed-colon,
    .chat-feed.chat-feed-view-monitor .chat-feed-status-pill,
    .chat-feed.chat-feed-view-monitor .chat-feed-new-pill {{
      display: none !important;
    }}
    .chat-feed.chat-feed-view-monitor .chat-entry-msg {{
      font-family: ui-monospace, Menlo, Consolas, monospace;
      font-size: 12px;
      line-height: 1.45;
      color: #d7dce3;
      white-space: normal;
      overflow-wrap: anywhere;
    }}
    .chat-feed.chat-feed-view-monitor .chat-monitor-prefix {{
      color: #98a3b2;
      font-weight: 700;
      margin-right: 5px;
    }}
    .chat-feed.chat-feed-view-monitor .chat-monitor-network-tag {{
      color: #b2bac6;
      margin-right: 6px;
      font-weight: 600;
    }}
    .chat-feed.chat-feed-view-monitor .chat-monitor-alert-tag {{
      color: #e07882;
      margin-right: 6px;
      font-weight: 700;
    }}
    .chat-feed.chat-feed-view-monitor .short-name {{
      display: inline-block;
      border-radius: 4px;
      padding: 0 2px;
      color: #000000;
      font-family: ui-monospace, Menlo, Consolas, monospace;
      margin-right: 5px;
    }}
    .chat-feed.chat-feed-view-monitor .chat-entry-reply {{
      color: #9aa6b5;
      font-style: italic;
      margin-right: 4px;
    }}
    .chat-feed.chat-feed-view-monitor .chat-entry-reply.missing {{
      opacity: 0.78;
    }}
    .chat-feed.chat-feed-view-monitor .chat-monitor-text {{
      color: #d7dce3;
      white-space: pre-wrap;
      overflow-wrap: anywhere;
    }}
    .chat-feed.chat-feed-view-monitor .chat-link {{
      color: #9abef3;
    }}
    .chat-feed-line {{
      display: flex;
      align-items: flex-start;
      gap: 8px;
      width: 100%;
      position: relative;
      z-index: 1;
    }}
    .chat-feed-summary {{
      min-width: 0;
      flex: 1 1 auto;
      display: flex;
      align-items: flex-start;
      flex-wrap: wrap;
      gap: 4px 5px;
      line-height: 1.45;
    }}
    .chat-feed-channel-tab {{
      display: none;
    }}
    .chat-feed-author {{
      flex: 0 0 auto;
      min-width: 0;
      max-width: 100%;
      display: inline-flex;
      align-items: flex-start;
      gap: 0;
      line-height: 1.45;
      user-select: text;
    }}
    .chat-feed-author .chat-name {{
      color: #2f4b3a;
      font-size: 14px;
      font-weight: 700;
      letter-spacing: 0.01em;
    }}
    .chat-feed-colon {{
      color: #6a8572;
      font-size: 14px;
      font-weight: 600;
      line-height: 1.45;
      flex: 0 0 auto;
    }}
    .chat-feed-side {{
      flex: 0 0 auto;
      margin-left: auto;
      display: inline-flex;
      flex-direction: column;
      align-items: flex-end;
      gap: 3px;
      min-width: 0;
    }}
    .chat-feed-time {{
      font-size: 10px;
      color: #6a8572;
      white-space: nowrap;
      flex: 0 0 auto;
    }}
    .mesh-channel-badge,
    .chat-feed-mesh {{
      --mesh-channel-badge-fill: #0072b2;
      --mesh-channel-badge-rgb: 0, 114, 178;
      display: inline-flex;
      align-items: center;
      gap: 5px;
      font-size: 10px;
      color: #2f5369;
      background: rgba(var(--mesh-channel-badge-rgb), 0.12);
      border: 1px solid rgba(var(--mesh-channel-badge-rgb), 0.32);
      border-radius: 999px;
      padding: 1px 7px 1px 6px;
      line-height: 1.3;
      white-space: nowrap;
    }}
    .mesh-channel-badge::before,
    .chat-feed-mesh::before {{
      content: "";
      width: 7px;
      height: 7px;
      border-radius: 50%;
      flex: 0 0 auto;
      background: var(--mesh-channel-badge-fill);
      border: 1px solid rgba(19, 36, 26, 0.28);
      box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.2);
    }}
    .bots-history-channel {{
      margin-right: 1px;
    }}
    .chat-feed-actions {{
      display: inline-flex;
      align-items: center;
      gap: 4px;
      margin-left: 2px;
    }}
    .chat-feed-new-pill {{
      border: 1px solid #d3b167;
      background: #fff5db;
      color: #7c5a1b;
      border-radius: 999px;
      padding: 1px 6px;
      font-size: 9px;
      line-height: 1.3;
      letter-spacing: 0.25px;
      font-weight: 700;
      white-space: nowrap;
      text-transform: uppercase;
      opacity: 1;
      transform: translateY(0) scale(1);
      transition: opacity 0.48s ease, transform 0.48s ease, filter 0.48s ease;
    }}
    .chat-feed-status-pill {{
      border: 1px solid #c7d8e6;
      background: #ebf2f8;
      color: #315674;
      border-radius: 999px;
      padding: 1px 6px;
      font-size: 9px;
      line-height: 1.3;
      letter-spacing: 0.25px;
      font-weight: 700;
      white-space: nowrap;
      text-transform: uppercase;
    }}
    .chat-feed-alert-pill {{
      border: 1px solid #d78084;
      background: #ffe8e9;
      color: #8f262c;
      border-radius: 999px;
      padding: 1px 6px;
      font-size: 9px;
      line-height: 1.3;
      letter-spacing: 0.25px;
      font-weight: 700;
      white-space: nowrap;
      text-transform: uppercase;
    }}
    .chat-feed-item.change-marker-fading .chat-feed-new-pill {{
      opacity: 0;
      transform: translateY(-1px) scale(0.93);
      filter: saturate(0.5);
    }}
    .chat-reply-btn {{
      border: 1px solid #bfd6c3;
      background: rgba(244, 251, 246, 0.78);
      color: #24533a;
      border-radius: 999px;
      padding: 1px 7px;
      font-size: 10px;
      line-height: 1.4;
      cursor: pointer;
      white-space: nowrap;
      display: inline-flex;
      align-items: center;
      gap: 4px;
      transition: color 140ms ease, background-color 140ms ease, opacity 140ms ease;
    }}
    .chat-reply-btn:hover {{
      background: #e6f2e9;
      border-color: #9dc3a7;
      color: #214734;
    }}
    .chat-reply-btn:focus-visible {{
      outline: 2px solid rgba(90, 135, 108, 0.34);
      outline-offset: 1px;
    }}
    .chat-reply-btn-icon {{
      font-size: 12px;
      line-height: 1;
    }}
    .chat-reply-btn-text {{
      font-size: 10px;
      font-weight: 600;
      letter-spacing: 0.01em;
    }}
    .chat-hop-watermark-inline {{
      display: inline-flex;
      align-items: center;
      font-size: 10px;
      line-height: 1.3;
      font-weight: 700;
      letter-spacing: 0.01em;
      font-variant-numeric: tabular-nums;
      color: #5e7d6c;
      opacity: 0.58;
      white-space: nowrap;
      user-select: none;
    }}
    button.chat-hop-watermark-inline.chat-hop-reply-btn {{
      appearance: none;
      border: 0;
      background: transparent;
      padding: 0;
      font-family: inherit;
      font-size: 10px;
      line-height: 1.4;
      font-weight: 400;
      letter-spacing: 0.01em;
      font-variant-numeric: normal;
      color: #24533a;
      cursor: pointer;
      gap: 3px;
      opacity: 1;
    }}
    .chat-hop-reply-icon {{
      font-size: 11px;
      line-height: 1;
      font-weight: 400;
      opacity: 0.9;
    }}
    .chat-hop-reply-text {{
      font-size: 10px;
      line-height: 1.4;
      font-weight: 600;
      letter-spacing: 0.01em;
      font-variant-numeric: normal;
    }}
    button.chat-hop-watermark-inline.chat-hop-reply-btn:hover {{
      opacity: 0.84;
    }}
    button.chat-hop-watermark-inline.chat-hop-reply-btn:focus-visible {{
      outline: 2px solid rgba(90, 135, 108, 0.34);
      outline-offset: 1px;
      border-radius: 4px;
    }}
    .chat-whois-btn {{
      border: 1px solid #bccddd;
      background: #f1f7fc;
      color: #1d4968;
      border-radius: 999px;
      padding: 1px 7px;
      font-size: 10px;
      line-height: 1.4;
      cursor: pointer;
      white-space: nowrap;
    }}
    .chat-whois-btn:hover {{
      background: #e2eef8;
      border-color: #95b1ca;
    }}
    .chat-react-btn {{
      border: 1px solid #bdd4c1;
      background: #f5fbf6;
      color: #26543a;
      border-radius: 999px;
      padding: 1px 7px;
      font-size: 11px;
      line-height: 1.4;
      cursor: pointer;
      white-space: nowrap;
    }}
    .chat-react-btn:hover {{
      background: #e8f4eb;
      border-color: #9ec2a8;
    }}
    .chat-retry-btn {{
      border: 1px solid #d5bb80;
      background: #fff8e7;
      color: #7a4e12;
      border-radius: 999px;
      padding: 1px 8px;
      font-size: 10px;
      line-height: 1.4;
      cursor: pointer;
      white-space: nowrap;
    }}
    .chat-retry-btn:hover {{
      background: #fff2d3;
      border-color: #be9c54;
    }}
    .chat-delivery-pill {{
      display: inline-flex;
      align-items: center;
      border: 1px solid #cfdccc;
      border-radius: 999px;
      padding: 1px 7px;
      font-size: 10px;
      line-height: 1.35;
      white-space: nowrap;
      margin-left: 2px;
    }}
    .chat-delivery-pill.state-pending {{
      background: #fff8e5;
      border-color: #e0c88f;
      color: #7a5a20;
    }}
    .chat-delivery-pill.state-sent {{
      background: #eff6ef;
      border-color: #c5d8c7;
      color: #3c6648;
    }}
    .chat-delivery-pill.state-acked {{
      background: #e7f5eb;
      border-color: #9ec8aa;
      color: #1f6138;
    }}
    .chat-delivery-pill.state-nak,
    .chat-delivery-pill.state-timeout,
    .chat-delivery-pill.state-error {{
      background: #fff0f0;
      border-color: #dfadad;
      color: #8a2c2c;
    }}
    .chat-feed-text {{
      font-size: 14px;
      color: #173827;
      line-height: 1.45;
      font-weight: 500;
      letter-spacing: 0.005em;
      white-space: pre-wrap;
      overflow-wrap: anywhere;
      word-break: break-word;
      user-select: text;
    }}
    .chat-feed-text-inline {{
      display: inline-block;
      min-width: 0;
      margin: 0;
      line-height: inherit;
      vertical-align: top;
      white-space: pre-wrap;
      overflow-wrap: anywhere;
      word-break: break-word;
      user-select: text;
    }}
    .chat-feed-text-inline > :first-child {{
      margin-top: 0;
    }}
    .chat-feed-text-inline > :last-child {{
      margin-bottom: 0;
    }}
    .chat-feed-text-inline.chat-feed-text-emoji-only {{
      font-size: 18px;
      line-height: 1.1;
    }}
    .chat-inline-emoji {{
      position: relative;
      display: inline-grid;
      place-items: center;
      min-width: 1.08em;
      font-family: "Apple Color Emoji", "Segoe UI Emoji", "Noto Color Emoji", "Twemoji Mozilla", "EmojiOne Color", sans-serif;
      font-size: 1.28em;
      line-height: 1;
      text-align: center;
      vertical-align: -0.16em;
      filter:
        saturate(1.2)
        contrast(1.08)
        drop-shadow(0 1px 0 rgba(255, 255, 255, 0.72))
        drop-shadow(0 0 4px rgba(255, 214, 94, 0.32));
    }}
    .chat-inline-emoji::before {{
      content: "";
      position: absolute;
      inset: 12% 8% 10%;
      z-index: -1;
      pointer-events: none;
      background: radial-gradient(
        circle,
        rgba(255, 255, 255, 0.92) 0%,
        rgba(255, 248, 213, 0.72) 34%,
        rgba(255, 255, 255, 0) 72%
      );
      transform: scale(1.18);
      opacity: 0.9;
    }}
    .chat-feed-text-inline.chat-feed-text-emoji-only .chat-inline-emoji {{
      min-width: 1.16em;
      font-size: 1.72em;
      vertical-align: -0.18em;
      filter:
        saturate(1.26)
        contrast(1.1)
        drop-shadow(0 1px 0 rgba(255, 255, 255, 0.78))
        drop-shadow(0 0 6px rgba(255, 214, 94, 0.38));
    }}
    .chat-feed-text-inline p,
    .chat-feed-text-inline div {{
      display: inline;
      margin: 0;
    }}
    .chat-feed-text .chat-link,
    #history-chat-table .chat-link {{
      color: #1b6bc9;
      text-decoration: underline;
      text-underline-offset: 1px;
      word-break: break-all;
    }}
    .chat-feed-text .chat-link:hover,
    #history-chat-table .chat-link:hover {{
      color: #144f93;
    }}
    .chat-reply-inline {{
      flex: 1 0 100%;
      min-width: 0;
      margin-bottom: 1px;
      padding-left: 7px;
      border-left: 2px solid rgba(112, 147, 126, 0.34);
      color: #62796c;
      font-size: 11px;
      line-height: 1.35;
      display: inline-flex;
      align-items: baseline;
      gap: 6px;
      opacity: 0.84;
      user-select: text;
    }}
    button.chat-reply-inline {{
      appearance: none;
      border-top: 0;
      border-right: 0;
      border-bottom: 0;
      background: transparent;
      padding: 0 0 0 7px;
      font-family: inherit;
      text-align: left;
      cursor: pointer;
    }}
    button.chat-reply-inline:hover .chat-reply-inline-label,
    button.chat-reply-inline:hover .chat-reply-inline-text {{
      text-decoration: underline;
      text-underline-offset: 2px;
    }}
    button.chat-reply-inline:focus-visible {{
      outline: 2px solid rgba(72, 163, 213, 0.58);
      outline-offset: 2px;
    }}
    .chat-reply-inline .chat-reply-inline-label {{
      color: #6a8778;
      font-weight: 600;
      flex: 0 1 auto;
      min-width: 0;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }}
    .chat-reply-inline .chat-reply-inline-text {{
      color: #789084;
      flex: 1 1 auto;
      min-width: 0;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }}
    .chat-reply-inline .chat-reply-inline-meta {{
      color: #6b8578;
      font-size: 11px;
      line-height: 1.35;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }}
    .chat-reply-inline.missing {{
      border-left-color: rgba(132, 140, 136, 0.6);
      color: #70807a;
    }}
    .chat-reply-inline.missing .chat-reply-inline-label {{
      color: #83908b;
    }}
    .chat-reply-inline.missing .chat-reply-inline-text,
    .chat-reply-inline.missing .chat-reply-inline-meta {{
      color: #75817d;
    }}
    .chat-reaction-row {{
      margin-top: 6px;
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      align-items: center;
      opacity: 1;
      transition: opacity 140ms ease;
      position: relative;
      z-index: 1;
    }}
    .chat-reaction-summary-row {{
      position: relative;
      z-index: 1;
    }}
    .chat-feed-item.self-authored .chat-reaction-row {{
      justify-content: flex-end;
    }}
    .chat-feed-item:hover .chat-reaction-row,
    .chat-feed-item:focus-within .chat-reaction-row,
    .chat-feed-item.selected-node .chat-reaction-row {{
      opacity: 1;
    }}
    .chat-reaction-summary-row {{
      margin-top: 5px;
      display: flex;
      align-items: center;
    }}
    .chat-reaction-summary {{
      border: 1px solid rgba(123, 160, 135, 0.4);
      background: rgba(236, 247, 239, 0.62);
      color: #2a523d;
      border-radius: 999px;
      padding: 1px 8px 1px 5px;
      font-size: 11px;
      line-height: 1.3;
      cursor: pointer;
      white-space: nowrap;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      transition: background 140ms ease, border-color 140ms ease, color 140ms ease, box-shadow 180ms ease, transform 180ms ease;
    }}
    .chat-reaction-summary:hover {{
      background: rgba(223, 240, 228, 0.92);
      border-color: rgba(108, 151, 122, 0.56);
    }}
    .chat-reaction-summary:focus-visible {{
      outline: 2px solid #8fbca0;
      outline-offset: 1px;
    }}
    .chat-reaction-summary.is-empty {{
      background: transparent;
      border-style: dashed;
      border-color: rgba(123, 160, 135, 0.32);
      color: #587162;
      padding-right: 9px;
    }}
    .chat-reaction-summary.is-empty:hover {{
      background: rgba(232, 244, 235, 0.55);
      border-color: rgba(108, 151, 122, 0.46);
      color: #214734;
    }}
    .chat-reaction-summary.is-empty.is-reaction-preview,
    .chat-reaction-summary.is-empty.is-reaction-expanded {{
      background: rgba(232, 244, 235, 0.78);
      border-color: rgba(108, 151, 122, 0.56);
      color: #214734;
      position: relative;
      z-index: 10;
      box-shadow: 0 6px 14px rgba(42, 82, 61, 0.12);
      transform: translateY(-1px);
    }}
    .chat-reaction-summary.is-empty.is-reaction-expanded {{
      background: rgba(223, 240, 228, 0.96);
      border-color: rgba(97, 144, 113, 0.64);
      color: #173726;
      box-shadow: 0 10px 20px rgba(31, 70, 53, 0.18);
    }}
    .chat-reaction-summary-icons {{
      display: inline-flex;
      align-items: center;
      padding-right: 2px;
    }}
    .chat-reaction-summary-emoji {{
      width: 16px;
      height: 16px;
      margin-left: -5px;
      border-radius: 999px;
      border: 1px solid rgba(113, 142, 124, 0.58);
      background: transparent;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 11px;
      line-height: 1;
      box-sizing: border-box;
    }}
    .chat-reaction-summary-emoji-placeholder {{
      color: currentColor;
      font-size: 10px;
      font-weight: 700;
    }}
    .chat-reaction-summary-emoji:first-child {{
      margin-left: 0;
    }}
    .chat-reaction-summary-total {{
      font-size: 10px;
      font-weight: 700;
      color: #355f48;
    }}
    .chat-reaction-summary-label {{
      font-size: 10px;
      font-weight: 600;
      letter-spacing: 0.01em;
      transition: color 140ms ease, letter-spacing 140ms ease;
    }}
    .chat-reply-tail {{
      margin-left: auto;
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }}
    .chat-reaction-stack {{
      display: inline-flex;
      flex-wrap: wrap;
      gap: 5px;
      align-items: center;
    }}
    .chat-reaction-row .chat-react-btn {{
      border: 1px solid #c6d7cb;
      background: transparent;
      color: #4c6858;
      border-radius: 999px;
      min-width: 24px;
      height: 22px;
      padding: 0 8px;
      font-size: 13px;
      font-weight: 700;
      line-height: 1;
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }}
    .chat-reaction-row .chat-react-btn:hover {{
      background: #e8f4eb;
      border-color: #9ec2a8;
      color: #1f4e35;
    }}
    .chat-reaction-row .chat-react-btn.has-reactions {{
      background: var(--surface-tint-bg-alt);
      border-color: #94bd9f;
      color: #1f4e35;
    }}
    .chat-reaction-row .chat-react-btn.has-reactions:hover {{
      background: #dff0e4;
      border-color: #7eab8b;
      color: #1a432e;
    }}
    .chat-reaction-chip {{
      border: 1px solid #b9d2bf;
      background: var(--surface-tint-bg-alt);
      color: #1f4e35;
      border-radius: 999px;
      padding: 1px 8px;
      font-size: 11px;
      line-height: 1.3;
      cursor: pointer;
      white-space: nowrap;
      display: inline-flex;
      align-items: center;
      gap: 4px;
    }}
    .chat-reaction-chip:hover {{
      background: #dff0e4;
      border-color: #94bd9f;
    }}
    .chat-reaction-chip:focus-visible {{
      outline: 2px solid #8fbca0;
      outline-offset: 1px;
    }}
    .chat-reaction-emoji {{
      font-size: 12px;
      line-height: 1;
    }}
    .chat-reaction-count {{
      font-size: 10px;
      font-weight: 700;
      color: #355f48;
    }}
    .chat-reaction-popover {{
      position: fixed;
      z-index: 1300;
      min-width: 220px;
      max-width: min(420px, calc(100vw - 16px));
      border: 1px solid color-mix(in srgb, var(--workspace-shell-border, #bdd4c3) 72%, var(--ui-border, transparent));
      border-radius: 8px;
      background:
        linear-gradient(
          180deg,
          color-mix(in srgb, var(--workspace-shell-bg-alt, #fbfefc) 76%, #07121f 24%),
          color-mix(in srgb, var(--workspace-shell-bg, #fbfefc) 88%, #050b13 12%)
        ),
        #0b1930;
      backdrop-filter: blur(14px) saturate(138%);
      -webkit-backdrop-filter: blur(14px) saturate(138%);
      box-shadow: 0 18px 38px rgba(0, 0, 0, 0.48);
      color: var(--workspace-shell-text, #193926);
      padding: 8px;
    }}
    .chat-reaction-popover[hidden] {{
      display: none !important;
    }}
    .chat-reaction-popover-head {{
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      font-size: 12px;
      font-weight: 600;
      color: var(--workspace-shell-text, #1f4f35);
      margin-bottom: 6px;
      padding-bottom: 6px;
      border-bottom: 1px solid var(--workspace-shell-border-muted, #deebe1);
    }}
    .chat-reaction-popover-section-label {{
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      color: var(--workspace-shell-text-soft, #63806f);
      margin: 6px 2px 4px;
    }}
    .chat-reaction-popover-quick-shell {{
      margin-bottom: 2px;
    }}
    .chat-reaction-popover-quick-row {{
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      align-items: center;
    }}
    .chat-reaction-popover-quick-btn,
    .chat-reaction-popover-more-btn {{
      border: 1px solid transparent;
      background: color-mix(in srgb, var(--workspace-shell-bg-alt, #f4fbf6) 72%, transparent);
      color: var(--workspace-shell-text, #1f4e35);
      border-radius: 8px;
      min-width: 30px;
      height: 30px;
      padding: 0 9px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
    }}
    .chat-reaction-popover-quick-btn {{
      font-size: 15px;
      line-height: 1;
      cursor: grab;
      touch-action: none;
    }}
    .chat-reaction-popover-more-btn {{
      font-size: 11px;
      font-weight: 700;
      letter-spacing: 0.01em;
    }}
    .chat-reaction-popover.is-emoji-dragging,
    .chat-reaction-popover.is-emoji-dragging .chat-reaction-popover-quick-btn {{
      cursor: grabbing;
    }}
    .chat-reaction-popover-quick-btn.dragging {{
      opacity: 0.35;
      transform: scale(0.9);
      pointer-events: none;
      box-shadow: none;
    }}
    .chat-reaction-popover-quick-btn:hover,
    .chat-reaction-popover-more-btn:hover {{
      background: color-mix(in srgb, var(--workspace-shell-hover-bg, #e6f2e9) 78%, transparent);
      border-color: color-mix(in srgb, var(--workspace-shell-border-strong, #9dc3a7) 42%, transparent);
      color: var(--workspace-shell-text, #123523);
    }}
    .chat-reaction-popover-empty {{
      font-size: 11px;
      line-height: 1.45;
      color: var(--workspace-shell-text-soft, #688274);
      padding: 4px 2px 2px;
    }}
    .chat-reaction-popover-emoji {{
      font-size: 15px;
      line-height: 1;
    }}
    .chat-reaction-popover-count {{
      color: var(--workspace-shell-text-soft, #4c705b);
      font-size: 11px;
      font-weight: 600;
    }}
    .chat-reaction-popover-list {{
      display: flex;
      flex-direction: column;
      gap: 4px;
      max-height: 220px;
      overflow: auto;
      padding-right: 2px;
    }}
    .chat-reaction-popover-row {{
      display: flex;
      align-items: center;
      gap: 7px;
      font-size: 12px;
      line-height: 1.25;
      color: var(--workspace-shell-text, #20452f);
      padding: 2px 1px;
    }}
    .chat-reaction-popover-avatar {{
      width: 24px;
      height: 24px;
      border-radius: 8px;
      border: 1px solid var(--workspace-shell-border-muted, #bfd7c7);
      background: color-mix(in srgb, var(--workspace-shell-text, #25523a) 10%, transparent);
      color: var(--workspace-shell-text, #25523a);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 10px;
      font-weight: 700;
      line-height: 1;
      flex: 0 0 auto;
    }}
    .chat-reaction-popover-name {{
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }}
    .chat-reaction-popover-more {{
      margin-top: 3px;
      padding-top: 6px;
      border-top: 1px solid var(--workspace-shell-border-muted, #deebe1);
      font-size: 11px;
      color: var(--workspace-shell-text-soft, #597767);
    }}
    .chat-feed-empty {{
      border: 1px dashed #d2e2d4;
      border-radius: 8px;
      padding: 10px;
      font-size: 12px;
      color: #5d7566;
      background: #f8fcf8;
    }}
    .chat-compose-shell {{
      --chat-send-channel-fill: #0072b2;
      --chat-send-channel-rgb: 0, 114, 178;
      margin-top: 6px;
      border: 1px solid color-mix(in srgb, var(--line) 88%, var(--ink) 12%);
      background: color-mix(in srgb, var(--panel) 78%, var(--bg) 22%);
