# First public release surface: chat + network + console + settings.
# This includes the full runtime package because those views currently share
# the same templates and modules.

README.md
mesh_dashboard.py
mesh_connection.py
meshdash/
pyproject.toml
requirements-dev.txt
docs/CHANNELS.md
